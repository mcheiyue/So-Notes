# SoNotes

SoNotes 是一个以“便签（Note）”为核心、按“看板（Board）”组织，并提供“废纸篓（Trash）”回收机制的桌面笔记应用。

## Language

**Note（便签）**:
用户创建的最小信息单元，包含标题与正文，并拥有在画布上的位置与显示属性。
_Avoid_: 卡片、条目

**Text Note（文本便签）**:
用于记录文字内容的 **Note**。文本便签不承载图片或文件附件；当用户粘贴或拖入图片时，应创建独立的 **Image Note**。
_Avoid_: 带附件便签、普通便签附件

**Image Note（图片便签）**:
用于展示单张图片的独立 **Note**。图片便签与文本便签同属一等便签节点，可被选择、移动、软删除、还原，并参与 **Undo/Redo**。
_Avoid_: 图片附件、附件便签、把图片挂到文本便签上

**Image file（图片文件）**:
图片便签所引用的本地图片文件。图片文件是图片便签的存储资源，不是用户可编辑的文本便签附件。
_Avoid_: 附件（面向用户或领域讨论时）

**Image file reference（图片文件引用）**:
图片便签与图片文件之间的引用关系。引用缺失时，图片便签应显示缺失占位，但不改变其他便签。
_Avoid_: 附件引用（面向用户或领域讨论时）

**Board（看板）**:
用于组织便签的工作空间与集合边界；同一时刻存在一个当前看板。
_Avoid_: 工作区、页面

**Trash（废纸篓）**:
存放已从看板删除但尚未永久清除的便签集合。
_Avoid_: 回收站（如需统一叫法，以“废纸篓”为准）

**Soft delete（软删除）**:
将便签从看板移入废纸篓，但仍可被还原。
_Avoid_: 删除（不区分软/硬）

**Permanent delete（永久删除 / 硬删除）**:
从废纸篓彻底移除便签，之后不可还原。
_Avoid_: 粉碎（如需统一叫法，以“永久删除”为准）

**ViewMode（视图模式）**:
决定用户当前处于“看板视图（BOARD）”还是“废纸篓视图（TRASH）”。
_Avoid_: 模式（不指明是视图模式）

**Domain state（领域状态）**:
需要持久化并代表用户数据真实来源的状态（如便签、看板及其关系）。
_Avoid_: 业务状态、数据层

**Viewport state（视口状态）**:
用于描述用户当前看到的画布范围与交互视角的状态（平移、缩放、可视区域等），不作为用户数据持久化来源。
_Avoid_: 相机、画面状态

**UI state（界面状态）**:
用于控制界面开关与短暂交互的状态（面板显隐、右键菜单、选区等），不作为用户数据持久化来源。
_Avoid_: 临时状态（不指明是界面层）

**Undo/Redo（撤销/重做）**:
对领域状态变更的可逆操作历史，不覆盖视口状态与界面状态。
_Avoid_: 回退（语义不清）

## Relationships

- 一个 **Board** 包含零个或多个 **Note**
- 一个 **Note** 在任一时刻要么属于某个 **Board**，要么属于 **Trash**
- 一个 **Image Note** 引用一个 **Image file**，但不是 **Text Note** 的附件
- 一个 **Text Note** 不包含图片或文件附件
- **Soft delete** 会把 **Note** 从 **Board** 移入 **Trash**
- **Permanent delete** 会把 **Note** 从 **Trash** 永久移除
- **ViewMode** 决定用户当前查看 **Board** 还是 **Trash**
- **Undo/Redo** 仅回滚/重放 **Domain state** 的变更

## Example dialogue

> **开发：** “用户在 **TRASH** 里按删除键，会触发 **Permanent delete** 吗？”
> **领域专家：** “不会。**TRASH** 是只读浏览与管理入口；永久删除必须通过明确的废纸篓操作触发。”

## Flagged ambiguities

- “删除”在历史讨论中既指 **Soft delete** 也指 **Permanent delete** —— 约定：默认指 **Soft delete**，需要不可逆时必须明确说“永久删除”。
- “附件”在历史计划中曾指图片文件能力 —— 当前领域语言中，用户可见概念统一为 **Image Note**、**Image file** 与 **Image file reference**；“附件”只可作为内部实现或历史文档术语出现。
