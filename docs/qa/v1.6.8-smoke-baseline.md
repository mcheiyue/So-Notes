# SoNotes v1.6.8 冒烟基线

> 基线版本：v1.6.8（Commit 5a）
> 创建时间：2026-08-25
> 校验脚本：`scripts/check-smoke-baseline.mjs`（preset v1.6.8）
> 范围：Coordinator 生命周期与锁语义（C-R3/C-R4/C-S7/C-S10/C-WF1/2/3）
>
> 注：`requiredById` 只匹配「自动化命令」+「证据」字段行；`all` 数组每项子串须出现在这些字段正文中。本版无 N/A 项。

---

## COORD-TIMEOUT-001

coordinator 超时释放 + 再启动 + 取消超时 + 不丢数据

- 类型: 自动化
- 自动化命令: npx vitest run src/services/backup/BackupJobCoordinator.test.ts -t "coordinator 超时:"
- 人工步骤: 无
- 证据: BackupJobCoordinator.test.ts；coordinator 超时: 超时后释放 activeJob；coordinator 超时: 超时后可再次启动；coordinator 超时: release 前主动释放取消超时；coordinator 超时: 超时不丢已提交数据
- 跳过原因: 无
- Result: PASS

---

## CLIFF-CLEAR-001

cliff deferred board 切换 + 退出清理 + 重检测

- 类型: 自动化
- 自动化命令: npx vitest run src/services/backup/RetentionCleanupOrchestrator.test.ts src/services/backup/quitHandler.test.ts -t "cliff 清理:"
- 人工步骤: 无
- 证据: RetentionCleanupOrchestrator.test.ts；quitHandler.test.ts；cliff 清理: board 切换时清除 cliffDropDeferred；cliff 清理: 退出路径清除 cliffDropDeferred；cliff 清理: 清除后下次备份重新检测断崖
- 跳过原因: 无
- Result: PASS

---

## LOCK-ADR-001

三层锁 ADR 矩阵 + 状态字

- 类型: 自动化
- 自动化命令: node -e "const t=require('fs').readFileSync('docs/adr/0002-three-layer-lock-semantics.md','utf8'); if(!t.includes('三层锁职责矩阵')||!t.includes('已澄清')||!t.includes('有意降级')){process.exit(1)}; console.log('OK ADR')"
- 人工步骤: 无
- 证据: 0002-three-layer-lock-semantics.md；三层锁职责矩阵；已澄清；有意降级
- 跳过原因: 无
- Result: PASS

---

## WEBDAV-FAKEIP-001

trust_host 跳 S2 + fake-ip 提示 + 四入口报错带出

- 类型: 自动化
- 自动化命令: cargo test webdav::tests::trust_host_skips_s2_resolved_blacklist && cargo test webdav::tests::benchmark_hit_returns_fakeip_hint_when_untrusted && cargo test webdav::tests::private_ip_literal_still_rejected_even_with_trust_host && cargo test webdav::tests::webdav_entry_errors_carry_inner_reason && cargo test webdav::tests::redirect_check_stays_untrusting && node scripts/check-cw7-entry-order.mjs && node scripts/check-dod18-commit2-3.mjs && node scripts/check-c68-scope.mjs ops-hard
- 人工步骤: 无
- 证据: tests.rs；trust_host_skips_s2_resolved_blacklist；benchmark_hit_returns_fakeip_hint_when_untrusted；private_ip_literal_still_rejected_even_with_trust_host；webdav_entry_errors_carry_inner_reason；redirect_check_stays_untrusting；check-cw7-entry-order.mjs；check-dod18-commit2-3.mjs；ops.rs 内容硬门
- 跳过原因: 无
- Result: PASS
