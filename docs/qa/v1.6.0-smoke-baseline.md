# SoNotes v1.6.0 冒烟基线

> 基线版本：v1.6.0（Commit 6a）
> 创建时间：2026-07-20
> 校验脚本：`scripts/check-smoke-baseline.mjs`（preset v1.6.0）
> 范围：捕获与整理体验（C16/C24/C25/C26/C27；C29 Optional 未交付 → QC-LOCATE-001 N/A）
>
> 注：`requiredById` 只匹配「自动化命令」+「证据」字段行；`all` 数组每项子串须出现在这些字段正文中。

---

## QC-BOARD-001

目标看板 label：空名回退「当前看板」、长名截断 + emoji 不抛错、禁用「页面/工作区/项目」产品词。

- 类型: 自动化
- 自动化命令: npx vitest run src/components/QuickCaptureOverlay.test.tsx -t "文案守卫"
- 人工步骤: 无
- 证据: QuickCaptureOverlay.test.tsx；空名回退当前看板；长名截断且 emoji 不抛错；不使用页面工作区项目（it：文案守卫: Quick Capture 显示当前看板且不使用页面工作区项目 / 文案守卫: Quick Capture 空名回退当前看板 / 文案守卫: Quick Capture 长名截断且 emoji 不抛错）
- 跳过原因: 无
- Result: PASS

---

## QC-FAIL-001

捕获失败保留输入：batch 抛错时 value 与浮层保持；成功时清空并关闭。

- 类型: 自动化
- 自动化命令: npx vitest run src/components/QuickCaptureOverlay.test.tsx -t "失败保留"
- 人工步骤: 无
- 证据: QuickCaptureOverlay.test.tsx；batch 抛错时保留输入且浮层不关；batch 成功时清空并关闭（it：失败保留: batch 抛错时保留输入且浮层不关 / 失败保留: batch 成功时清空并关闭）
- 跳过原因: 无
- Result: PASS

---

## QC-HIGHLIGHT-001

batch 创建高亮+选区契约，且 TRASH 视图下仍写入 currentBoardId（不进废纸篓）。

- 类型: 自动化
- 自动化命令: npx vitest run src/store/useStore.test.ts -t "store 契约"
- 人工步骤: 无
- 证据: useStore.test.ts；高亮与选区；TRASH 视图下 batch 仍写入 currentBoardId（it：store 契约: addNotesWithContentBatch 高亮与选区 / store 契约: TRASH 视图下 batch 仍写入 currentBoardId 不进废纸篓）
- 跳过原因: 无
- Result: PASS

---

## QC-UNDO-001

batch Undo/Redo 整批（store 真源）：undo/redo/空 batch/混合栈四条 it。

- 类型: 自动化
- 自动化命令: npx vitest run src/store/useStore.test.ts -t "store 历史: "
- 人工步骤: 无
- 证据: useStore.test.ts；addNotesWithContentBatch undo 整批撤销；addNotesWithContentBatch redo 整批恢复；空 batch 不 push history；batch 与单条 add 混合栈（it：store 历史: 四条）
- 跳过原因: 无
- Result: PASS

---

## QC-LOCATE-001

捕获后程序定位 + Viewport Action gate（C29 Optional）。

- 类型: 自动化
- 自动化命令: N/A 见 §1.2
- 人工步骤: 无
- 证据: 未交付 Optional 定位；见实施稿 §1.2 / C29；不宣称 viewport gate it 已绿
- 跳过原因: C29 Optional 未实施；捕获后程序定位未交付（§1.2 N/A）
- 原因: 本版本未交付 Optional 捕获后程序定位（C29）；冒烟诚实记 N/A
- Result: N/A

---
