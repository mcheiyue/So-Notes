# SoNotes v1.6.2 实施稿：备份敏感信息与错误脱敏 + keyring 依赖修正

> **状态**：可执行实施源（相对 `v1.6.1-webdav-ssrf-hardening.md` 的下一版）  
> **版本基线**：五处版本号 = `1.6.1` → 本版目标 `1.6.2`  
> **总规划依据**：[`v1.6.1-to-v1.7.0-planning-recommendations.md`](./v1.6.1-to-v1.7.0-planning-recommendations.md) §5  
> **方案标准**：`D:\OpenCode\docs\可验证型 Commit 实施方案编写指南.md`（横切矩阵、每约束可执行验证、基础件先建、收尾「无法自动证明」）  
> **封装原则**：`D:\OpenCode\docs\代码抽象与方法封装规范.md`（反碎片化、拒过度设计、可读性优先）  
> **权威性**：以**本文件**为 v1.6.2 执行与验收唯一真源。  
> **冲突仲裁（硬）**：总规划 §5 与本文件冲突时，**一律以本文件 §1 范围 + §3 矩阵 + §4 构造规格 + §5 commit 为准**。  
> **关单状态字**：R1/K1 → **已修复**；R2 → **已收窄**（禁止写「已修复」）。

---

### 执行速查（实施入口）

| 顺序 | Commit | 必改路径（摘要） | 必跑验收（摘要） |
| --- | --- | --- | --- |
| 1 | 共享敏感词 JSON SSOT | `src/shared/sensitive-words.json` + TS/Rust 引用 + BoardDock 删本地 sanitize | cargo `backup_activity` 测 + vitest `sanitizeActivityInput` ≥1 passed + SSOT 引用门 |
| 2 | createRemoteBackup catch 脱敏 | `WebDavBackupService.ts` + Runner throw 核对 | vitest it「createRemoteBackup catch 脱敏」≥1 passed |
| 3 | 移除 keyring 4 | `Cargo.toml` + `lib.rs` + `webdav.rs` 初始化 | `cargo tree -i keyring` 含 did not match + tauri build + exe 体积记录 |
| 4 | 版本 + CHANGELOG | 五处版本 → `1.6.2`、`CHANGELOG.md` | 版本断言 + `npm run check` + 体积对比记入 CHANGELOG |

> **Commit 风格（与仓库历史对齐，硬）**：中文描述 + Conventional Commits 前缀，参考已落地提交：  
> `feat(backup): 共享敏感词 JSON 供 TS/Rust 引用` / `fix(backup): createRemoteBackup catch 错误字段脱敏` / `fix(deps): 移除 keyring 4，改用 keyring-core + 平台 store` / `chore(release): prepare v1.6.2`  
> **禁止**本版实施沟通中改用纯英文长句标题（如 `drop keyring 4 sample crate; use keyring-core + platform store`）作为合入 message；§6 历史参考若与已落地不一致，**以 git log 为准**。

---

## 0. 一句话目标

**让备份失败信息不再泄露 URL/用户名/路径，并去掉 keyring 4 误依赖以回收约 5MB 发布体积；R2 仅收窄 createRemoteBackup 路径，不宣称全链路已修复。**

---

## 1. 范围

### 1.1 In Scope（必做）

| 能力 | 用户可感知结果 | 主要落点 | 约束 ID |
| --- | --- | --- | --- |
| **敏感词 SSOT** | TS/Rust 活动日志与错误脱敏同源词表 | `src/shared/sensitive-words.json` + `BackupActivityLogService` + `backup_activity.rs` | C-R1 |
| **createRemoteBackup 错误字段脱敏** | 远程备份失败返回的 `error` 无 URL/user/path 原文 | `WebDavBackupService.createRemoteBackup` catch | C-R2, C-R3 |
| **Runner throw 兜底** | throw 路径仍走 `sanitizeBackupErrorMessage` | `RemoteBackupRunner` | C-R2 |
| **BoardDock 删本地 sanitize** | 无第二套脱敏实现 | `BoardDock.tsx` → 复用 service 导出 | C-R1 |
| **keyring 依赖收敛** | 安装包体积显著下降；凭据仍走系统密钥链 | `Cargo.toml` + `lib.rs` setup + `webdav.rs` | C-K1, C-K2, C-K3 |

### 1.2 Optional（本版不做）

| 能力 | 说明 |
| --- | --- |
| 完整体积 CI 门禁 | v1.6.9；本版仅一次 `tauri build` 对比 |
| reqwest/image/zip 二次瘦身 | 全期 Non-goals |
| Scheduled / Retention / Orchestrator 全路径脱敏 | R2 收窄残留；见 Out of Scope |

### 1.3 Out of Scope（Non-goals）

- 备份业务逻辑（保留/调度/恢复 UI）变更
- WebDAV SSRF（v1.6.1 已做）/ `webdav.rs` 模块拆分（v1.6.5）
- 锁语义 / coordinator 看门狗（v1.6.8）
- ViewMode（v1.6.3）/ useStore 迁移（v1.6.4+）
- `ScheduledRemoteBackupService` / `RemoteBackupRetentionService` / Orchestrator 对外错误脱敏
- 宣称 R2「已修复」或「全量备份失败不再泄露」

### 1.4 与总规划的偏差（显式）

| 总规划 §5 | 本实施稿 | 原因 |
| --- | --- | --- |
| R2 叙述偏「备份失败不再泄露」 | **已收窄**：仅 service `error` 字段 + Runner **throw** 兜底 | 实测 Scheduled/Retention/Orchestrator 仍有裸传；诚实关单 |
| Commit 英文长标题 | **中文 Conventional**（与 git log 已落地一致） | 仓库历史风格 |
| 体积输入路径 `建议.md` | 以实测 exe 为准；CHANGELOG 记前后 MB | 可验证 |

---

## 2. 代码基线（实施前事实；改前再读源文件）

### 2.1 Cargo / keyring

| 位置 | 现状（实施前） |
| --- | --- |
| `src-tauri/Cargo.toml` | `keyring = "4"` + `keyring-core = "1"`；**无** `windows-native-keyring-store` |
| `src-tauri/src/lib.rs` setup | `keyring::use_native_store(false)` |
| `webdav.rs` 生产 | `keyring_core::Entry::new` 读写 |
| `webdav.rs` 集成测（ignore） | `keyring::use_windows_native_store` |

keyring 4.x 官方：**Do not depend on this crate**；会拉 clap / db-keystore→turso 等。

### 2.2 脱敏 / 错误路径

| 位置 | 现状（实施前） |
| --- | --- |
| `BackupActivityLogService.ts` | 窄 `SENSITIVE_PATTERN`；无共享 JSON |
| `BoardDock.tsx` | 本地 sanitize 副本 |
| `WebDavBackupService.createRemoteBackup` | catch **裸传** `err.message` 写入 `error` |
| `RemoteBackupRunner` | throw 路径已有/应有 `sanitizeBackupErrorMessage` |
| `backup_activity.rs` | `sanitize_message`；词表未与 TS SSOT 对齐 |

### 2.3 实施后目标形状（钉死）

| 项 | 目标 |
| --- | --- |
| 词表 | `src/shared/sensitive-words.json` 为唯一词源；至少含 password / token / authorization / secret / username / serverurl / url / 密码 / 令牌 |
| TS helper | 导出 `sanitizeErrorMessage`（或等价：经 `sanitizeActivityInput` 处理 message）；**禁止**声称 Rust 有 camelCase `sanitizeErrorMessage` |
| Rust | 继续 `sanitize_message` + `include_str!` / 构建期读同一 JSON |
| createRemoteBackup | catch → sanitize → 写入返回 `error`；**return 不 throw** |
| Runner | **仅** throw 路径 sanitize；**不**对 `result.error` 二层包装；**不以双保险验收** |
| keyring | 无 `keyring = "4"`；`keyring-core` + `windows-native-keyring-store`（cfg windows）；`keyring_core::set_default_store(...)` |
| 体积 | release `so-notes.exe` 相对 v1.6.1 ~11.4MB **显著下降**（预期回收 keyring 误依赖主因 ~5MB；以实测记 CHANGELOG） |

---

## 3. 本版本约束 ID 表

| ID | 含义 | 与总规划关系 |
| --- | --- | --- |
| **C-R1** | 敏感词 JSON SSOT；TS/Rust 各自引用 + 各自单测；**不**验收跨端逐字节同一输入同一输出 | 总规划 C-R1 |
| **C-R2** | sanitize 覆盖已知敏感模式（含 username / serverurl / URL 形态） | 总规划 C-R2 |
| **C-R3** | `createRemoteBackup` catch **禁止**裸传 `err.message` | 总规划 C-R2b |
| **C-K1** | Cargo **无** `keyring` 4.x；tree 无 keyring v4 | 总规划 C-K1 |
| **C-K2** | 无 `keyring::use_native_store` / `keyring::use_windows_native_store` | 总规划 C-K2 |
| **C-K3** | 凭据 roundtrip 不回归（测 + 手动） | 总规划 C-K3 |
| **C-R4** | 范围纪律：不扩 Scheduled/Retention/Orchestrator 脱敏、不拆 webdav、不二次瘦身 | 本版新增（对应 v1.6.0 C28 精神） |

**禁止**把 C-R* 与 v1.6.1 的 C-W*、v1.6.0 的 C24–C29 混用编号。

---

## 4. 横切约束矩阵

> 形态对齐可验证型指南：**(1) 接入点 × 约束** → **(2) 约束构造/操作/期望 + 一行验证** → **(3) 精确 it 索引**。

### 4.0 接入点 × 约束交叉表

| 接入点 ↓ \ 约束 → | **C-R1** 词表 SSOT | **C-R2** 模式覆盖 | **C-R3** catch 禁止裸传 | **C-K1** 无 keyring4 | **C-K2** set_default_store | **C-K3** roundtrip | **C-R4** 范围 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **`sensitive-words.json`** | 唯一词源；改词只改此文件 | 词条覆盖审查对照项 | — | — | — | — | 允许新建 `src/shared/` |
| **`BackupActivityLogService`** | import JSON；导出 sanitize helper | 边界用例测 | 被 service 复用 | — | — | — | 禁止第二套 pattern |
| **`backup_activity.rs`** | `include_str!` 同路径 | Rust 侧单测 | — | — | — | — | 禁止硬编码第二词表 |
| **`BoardDock.tsx`** | 删本地 sanitize；改调 service 导出 | 复用 | — | — | — | — | 禁止再复制 pattern |
| **`WebDavBackupService.createRemoteBackup`** | 复用 helper | catch 写入 `error` 已脱敏 | **硬**：catch 无裸 `err.message` | — | — | — | **单点**；不扩 Scheduled |
| **`RemoteBackupRunner`** | 复用 | **仅 throw** 路径 sanitize | 不对 `result.error` 二层 | — | — | — | 禁止「双保险」验收 |
| **`Cargo.toml` / `lib.rs` / `webdav` 初始化** | — | — | — | 无 `keyring =`；tree did not match | 无 `keyring::use_`；用 set_default_store | Entry roundtrip / 手动 | 禁止动 image/zip/reqwest |

**先行基础件**：

| 横切 | 接入点数 | 基础 Commit | 单一实现 | 后续强制 |
| --- | --- | --- | --- | --- |
| **C-R1/C-R2** | JSON + TS + Rust + BoardDock | **1** | `sensitive-words.json` + service/Rust sanitize | 禁止各文件自写词表/正则副本 |
| **C-R3** | createRemoteBackup | **2** | catch 内 `sanitizeErrorMessage`（或等价） | 禁止只改 Runner 假装关 R2 |
| **C-K*** | Cargo + setup | **3** | keyring-core + 平台 store + set_default_store | 禁止残留 keyring 4 |

### 4.0.1 约束清单（构造 → 操作 → 期望）

| ID | 构造（摘要） | 操作 | 期望（可断言） | Commit | 验证（输入 → 输出） |
| --- | --- | --- | --- | --- | --- |
| **C-R1** | JSON 存在；TS import + Rust include | 跑两端既有/扩展单测 | 两路径均引用 `sensitive-words.json`；各自测绿 | 1 | 验证：`Select-String` 两文件均命中 `sensitive-words.json` 且 Count≥2；cargo/vitest 假绿门过 |
| **C-R2** | 含 `user:pass@host/path`、`password=`、`username=`、`serverurl=`、中文「密码」等 | sanitize | 输出**无**原文敏感值；可含 `[REDACTED]` 或等价 | 1 | 验证：见 §5.1 边界表 B-R2-*；vitest/cargo 覆盖 |
| **C-R3** | mock createRemoteBackup 抛含 `https://user:pass@host/path` 的 Error | 调 createRemoteBackup | 返回对象 `error` 字段无 URL/user/path 原文；catch 源码无裸 `err.message` 写入 | 2 | 验证：it「createRemoteBackup catch 脱敏」；可选 `Select-String` catch 块 |
| **C-K1** | 改后 Cargo.toml | `cargo tree -i keyring` | 输出含 `did not match any packages`；全量 tree 无 `keyring v4.` | 3 | 验证：§5.3 PowerShell 硬门（**勿**把 exit 101 当成功） |
| **C-K2** | lib.rs + webdav | 静态扫描 | 无 `keyring::use_` | 3 | 验证：`Select-String ... 'keyring::use_'` Count=0 |
| **C-K3** | 平台 store 初始化后 | Entry set/get 或手动 | 读写成功；或文档写明需重输一次密码 | 3 | 验证：保留 ignore 集成测可编译；手动：存密码→重启→备份可用 |
| **C-R4** | 相对 base 的 diff | 人工 + 路径审阅 | 无 Scheduled/Retention/Orchestrator 脱敏「顺手修」；无 webdav 拆分；无二次瘦身 | 全程 | 验证：PR 审阅；§8 残留列表仍成立 |

### 4.1 验收索引表（C# ↔ 文件 ↔ it/命令 ↔ commit）

| C# | 目标 | 精确锚点 | Commit | 假绿防护 |
| --- | --- | --- | --- | --- |
| C-R1 | `backup_activity` 测 | `cargo test --manifest-path src-tauri/Cargo.toml --lib backup_activity::tests`；禁 `running 0 tests` | 1 | exit≠0 或 0 tests → throw |
| C-R1/C-R2 | `BackupActivityLogService.test.ts` | `-t "sanitizeActivityInput"`；摘要 `Tests [1-9]… passed` | 1 | 全 skip / 0-hit → throw |
| C-R1 | SSOT 引用 | `Select-String` 两文件 `sensitive-words.json` Count≥2 | 1 | 仅注释命中风险 → §8 |
| C-R3 | `WebDavBackupService.test.ts` | it 全名 **`createRemoteBackup catch 脱敏`**（一字不差） | 2 | 同上假绿门 |
| C-K1 | cargo tree | `-i keyring` 含 `did not match any packages` | 3 | 无该句 → throw |
| C-K2 | 静态 | 无 `keyring::use_` | 3 | Count>0 → throw |
| C-K3 | 手动 + 测 | 存密码→重启→备份；ignore 测可编译 | 3 | 仅文档不够 → DoD 勾选手测 |
| 发布 | 五处版本 + CHANGELOG | 版本 = 1.6.2；体积前后 MB | 4 | `npm run check` |

---

## 5. 约束 → 构造规格与可执行验证

### 5.0 通用假绿门（PowerShell；仓库根）

```powershell
# 工作目录：D:\Github\So-Notes
$env:NO_COLOR = "1"
node -e "if(!require('fs').existsSync('.tmp'))require('fs').mkdirSync('.tmp',{recursive:true})"
```

### 5.1 C-R1 / C-R2 — 词表 SSOT + 模式覆盖（Commit 1）

**实现要点（写死）**

- 新建 `src/shared/sensitive-words.json`（string 数组）。
- TS：`import sensitiveWords from '../../shared/sensitive-words.json'` 构建 `SENSITIVE_PATTERN`；导出 `sanitizeErrorMessage(err: unknown): string`（内部走 `sanitizeActivityInput` 的 message 路径即可）。
- Rust：`include_str!("../../shared/sensitive-words.json")` 或与现网 `backup_activity` 路径一致的相对路径；解析后喂既有 `sanitize_message`。
- `BoardDock.tsx`：**删除**本地 sanitize 副本，改为 import service 导出版。
- **禁止**为「抽象」再抽 `src/utils/sanitize.ts`（封装规范：无多调用方前不碎片化；本版调用方已集中在 service）。
- **禁止**验收「TS 与 Rust 同一输入字节级同一输出」——各自单测即可。

**C-R2 边界穷举（模式匹配；并入既有 describe 或邻接 it，禁止 it 名漂移导致 0-hit）**

| 子 ID | 输入 message（摘要） | 期望 |
| --- | --- | --- |
| B-R2-1 | `password=secret123` | 无 `secret123` |
| B-R2-2 | `token: abc` | 无 `abc`（或按现网 redact 规则） |
| B-R2-3 | `username=alice` | 无 `alice` |
| B-R2-4 | `serverurl=https://dav.example/path` | 无完整 URL 原文 |
| B-R2-5 | `https://user:pass@host/path` | 无 `user`/`pass`/`host` 原文组合泄露（按现网 redact 强度；至少无 `pass@`） |
| B-R2-6 | 中文 `密码=测试` | 无 `测试` 明文（若词表含「密码」） |
| B-R2-7 | 空串 / 无敏感 | 原样或空；不抛 |
| B-R2-8 | 多匹配同一串 | 全部敏感段处理，无残留明文密码 |

**验收命令（Commit 1）**

```powershell
$ct = cargo test --manifest-path src-tauri/Cargo.toml --lib backup_activity::tests 2>&1 | Out-String
if ($LASTEXITCODE -ne 0) { throw "cargo test failed: $LASTEXITCODE" }
if ($ct -match 'running 0 tests') { throw "cargo: 0 tests matched filter (假绿)" }
if ($ct -notmatch 'test result:\s*ok') { throw "cargo: not ok" }

$env:NO_COLOR = "1"
$r = npx vitest run src/services/backup/BackupActivityLogService.test.ts -t "sanitizeActivityInput" --reporter=verbose 2>&1 | Out-String
if ($LASTEXITCODE -ne 0) { throw "vitest failed: $LASTEXITCODE" }
if ($r -notmatch 'Tests\s+[1-9]\d*\s+passed') { throw "vitest: need >=1 passed (假绿: skip/0-hit)" }
if ($r -match 'No test files found') { throw "vitest: no test files" }

$ssot = Select-String -Path src/services/backup/BackupActivityLogService.ts,src-tauri/src/backup_activity.rs -Pattern "sensitive-words.json"
if (@($ssot).Count -lt 2) { throw "SSOT ref missing (need both TS and Rust)" }

# BoardDock 不得残留独立 SENSITIVE 大段副本（启发式；以 diff 审阅为准）
$bd = Select-String -Path src/components/BoardDock.tsx -Pattern "SENSITIVE_PATTERN|function sanitize" -ErrorAction SilentlyContinue
# 允许 import sanitizeErrorMessage；禁止本地 pattern 定义 — 人工确认 diff
```

### 5.2 C-R3 — createRemoteBackup catch 脱敏（Commit 2）

**实现要点（写死）**

- **仅**改 `WebDavBackupService.createRemoteBackup` 的 catch：将错误经 `sanitizeErrorMessage`（或 `sanitizeActivityInput`）后写入返回值的 **`error` 字段**；保持 **return 不 throw** 的现网契约。
- `RemoteBackupRunner`：**只核对** throw 路径仍调用 `sanitizeBackupErrorMessage`；**禁止**对已写入的 `result.error` 再包一层作为本版验收。
- **不改** Scheduled / Retention / Orchestrator。
- 新增/扩展 it，全名必须为：

  **`createRemoteBackup catch 脱敏`**

**构造规格**

| 用例 | 前置 | 操作 | 期望 |
| --- | --- | --- | --- |
| E1 | mock 底层抛 `Error('fail https://alice:s3cret@dav.example/repo')` | `createRemoteBackup(...)` | 返回失败结果；`error` **不含** `alice` / `s3cret` / `dav.example` 原文（或按 redact 后不可逆读出密码） |
| E2 | mock 抛普通 `Error('network down')` | 同上 | `error` 仍可读业务含义；不无故清空 |

**验收命令（Commit 2）**

```powershell
$env:NO_COLOR = "1"
$r = npx vitest run src/services/backup/WebDavBackupService.test.ts -t "createRemoteBackup catch 脱敏" --reporter=verbose 2>&1 | Out-String
if ($LASTEXITCODE -ne 0) { throw "vitest failed: $LASTEXITCODE" }
if ($r -notmatch 'Tests\s+[1-9]\d*\s+passed') { throw "vitest: need >=1 passed (假绿: skip/0-hit)" }
if ($r -match 'No test files found') { throw "vitest: no test files" }

# 回归：Commit 1 脱敏测仍绿
$r2 = npx vitest run src/services/backup/BackupActivityLogService.test.ts -t "sanitizeActivityInput" --reporter=verbose 2>&1 | Out-String
if ($r2 -notmatch 'Tests\s+[1-9]\d*\s+passed') { throw "C-R1 regress" }
```

### 5.3 C-K1 / C-K2 / C-K3 — drop keyring 4（Commit 3）

**实现要点（写死）**

- `Cargo.toml`：删除 `keyring = "4"`；保留 `keyring-core = "1"`；Windows 增加 `windows-native-keyring-store`（或官方等价名，以 crates.io 为准）。
- `lib.rs` setup：`keyring_core::set_default_store(windows_native_keyring_store::Store::new()? )`（失败策略：log 后继续或显式 Err——**与现网可启动性一致**，写进 PR 说明）。
- `webdav.rs`：生产继续 `keyring_core::Entry`；所有 `keyring::use_*` 改为 set_default_store；`#[ignore]` 测仍须**可编译**。
- **禁止** `cargo bloat` 作为验收；**禁止**动 image/zip/reqwest/rustls/profile。
- 体积：`npm run tauri build` 后 `Get-Item src-tauri\target\release\so-notes.exe`，与改前基线对比，**CHANGELOG/PR 记前后 MB**。

**验收命令（Commit 3）**

```powershell
# C-K1
$tree = cargo tree --manifest-path src-tauri/Cargo.toml -i keyring 2>&1 | Out-String
if ($tree -notmatch 'did not match any packages') {
  throw "keyring still resolvable or cargo tree failed:`n$tree"
}
$full = cargo tree --manifest-path src-tauri/Cargo.toml 2>&1 | Out-String
if ($LASTEXITCODE -ne 0) { throw "cargo tree full failed: $LASTEXITCODE" }
if ($full -match 'keyring\s+v4\.') { throw "keyring v4 still in full tree" }

$tomlHit = Select-String -Path src-tauri/Cargo.toml -Pattern '^\s*keyring\s*='
if ($tomlHit) { throw "Cargo.toml still has keyring = dependency line" }

# C-K2
$useHit = Select-String -Path src-tauri/src/lib.rs,src-tauri/src/webdav.rs -Pattern 'keyring::use_'
if ($useHit) { throw "keyring::use_ still present:`n$($useHit -join "`n")" }

# 编译 + 体积
npm run tauri build
if ($LASTEXITCODE -ne 0) { throw "tauri build failed: $LASTEXITCODE" }
Get-Item src-tauri\target\release\so-notes.exe | Select-Object FullName, Length
# 人工：与 v1.6.1 基线对比，写入 CHANGELOG（预期显著下降）

cargo test --manifest-path src-tauri/Cargo.toml --lib 2>&1 | Out-String | ForEach-Object {
  if ($_ -notmatch 'test result:\s*ok') { throw "cargo test lib not ok" }
}
```

### 5.4 发布 Commit 4

**改动**：`package.json` / `package-lock.json`（双 version）/ `src-tauri/Cargo.toml` / `Cargo.lock`（`cargo update -p so-notes`）/ `tauri.conf.json` → `1.6.2`；`CHANGELOG.md` 写 R1 已修复、R2 **已收窄**、K1 已修复 + 体积前后 MB。

```powershell
node -e "const p=require('./package.json'); if(p.version!=='1.6.2') process.exit(1)"
npm run check
if ($LASTEXITCODE -ne 0) { throw "npm run check failed" }
```

---

## 6. 分 Commit 方案

| 顺序 | 建议 message（与 git 历史风格一致） | 范围 | 依赖 |
| --- | --- | --- | --- |
| 1 | `feat(backup): 共享敏感词 JSON 供 TS/Rust 引用` | JSON SSOT + TS/Rust 引用 + BoardDock 删副本 + 边界测 | 无（基础件） |
| 2 | `fix(backup): createRemoteBackup catch 错误字段脱敏` | service catch + it「createRemoteBackup catch 脱敏」+ Runner throw 核对 | **必须** Commit 1（复用 sanitize） |
| 3 | `fix(deps): 移除 keyring 4，改用 keyring-core + 平台 store` | Cargo + setup + webdav 初始化 + 体积记录 | 可与 1/2 并行开发，**同一发布列车**合入 |
| 4 | `chore(release): prepare v1.6.2` | 五处版本 + CHANGELOG | 1–3 全绿 |

> **发布列车**：三功能 commit **勿**拆成可单独对用户发布的中间 tag；半合入相对基线不更差，但 createRemoteBackup 在仅合 1 未合 2 时仍可能裸 error。

---

## 7. DoD（完成定义）

- [ ] **C-R1**：§5.1 cargo + vitest 假绿硬门 + SSOT 双端引用
- [ ] **C-R2**：边界表关键行有测（username/serverurl/URL 形态）
- [ ] **C-R3**：it「createRemoteBackup catch 脱敏」假绿硬门；关单状态字 **已收窄**
- [ ] **C-K1/C-K2**：§5.3 tree + Select-String 硬门
- [ ] **C-K3**：手动 roundtrip 或 CHANGELOG 诚实写「store 切换后可能需重输一次 WebDAV 密码」
- [ ] 体积：CHANGELOG 记前后 MB（禁止虚假宣称回收 5MB）
- [ ] **C-R4**：diff 无 Scheduled/Retention/Orchestrator「顺手全脱敏」、无 webdav 拆分、无二次瘦身
- [ ] `npm run check` 全绿
- [ ] 审查台账：R1 **已修复**；R2 **已收窄**；K1 **已修复**

---

## 8. 收尾自检（无法自动证明 / 最可能返工）

按可验证型指南第 4 条，下列**说了要什么但自动化证明偏弱**——合入前人工勾选：

1. **SSOT 真加载**：`Select-String` 可被注释/死字符串满足 → 须确认 import/`include_str!` 在运行路径上。
2. **R2 残留诚实**：Scheduled / Retention / Orchestrator / `lastFailureReason` 仍可能裸传 → **禁止** CHANGELOG 写「备份失败全链路不再泄露」。
3. **体积无同机改前基线门**：依赖人工记录；CI 无硬门（v1.6.9 再建）。
4. **C-K3 手测**：自动化 ignore 测不替代「存密码→重启→备份」。
5. **keyring 初始化失败策略**：仅 log 继续时，运行时无 store 的行为须在 PR 说明。
6. **多行 PowerShell 无 set -e**：前失败后仍跑 → 须逐条看 throw。
7. **平台 store crate 名漂移**：以官方 crate 名为准，计划写「平台 store」占位。
8. **R1 跨端算法一致**：本版**明确不验收**；勿把「词表相同」写成「过滤结果字节一致已修复」。

---

## 9. 可带风险（开工知情，不挡开工）

- 三 commit 同一发布列车；勿单独发中间版。
- store 切换可能导致已存凭据读失败 → 文档/CHANGELOG 可写「需重输一次 WebDAV 密码」。
- 体积未必刚好 5MB；以实测为准。
- vitest 须 `NO_COLOR=1` + `Tests [1-9]… passed` 假绿门。
- `cargo tree -i keyring` 成功移除时看 **did not match** 句，**勿**把任意 exit 101 当成功。

---

## 10. 风险与对策

| 风险 | 等级 | 对策 |
| --- | --- | --- |
| Rust 构建找不到 JSON | 低 | CI/`cargo test` 验证路径；相对 `backup_activity.rs` 钉死 |
| sanitize 模式遗漏 | 中 | §5.1 B-R2 边界表 |
| 平台 store API 与文档不一致 | 中 | 对照 keyring-core 当前文档；先改初始化再改测 |
| 已存凭据读不到 | 高 | 手动 roundtrip；CHANGELOG 说明重输 |
| 体积未达预期 | 中 | `cargo tree` 确认 clap/turso 是否仍在图；不写假 changelog |
| 把 R2 标成已修复 | 高 | DoD 状态字硬门；§8 自检 |

---

**本文件为 v1.6.2 唯一执行真源。** 实施顺序：Commit 1 → 2 → 3 → 4；每步跑完对应 §5 命令再合入。
