## [v1.6.8] - 2026-08-25

本版本聚焦备份协调器生命周期与 WebDAV fake-ip 兼容：备份卡死时超时自动释放、cliff deferred 在看板切换/退出时清理，以及代理 fake-ip 环境下 WebDAV 可用且报错可自诊。三层锁职责以 ADR 文档化（R3 有意降级，未合并锁源）。

关单状态字：R3 **已澄清（有意降级）**；R4/S7 **已修复**；C-WF1/2/3 **已修复**。

### ✨ 新特性 (Features)

* **代理 fake-ip 环境下 WebDAV 可用且报错可自诊（C-WF1/2/3）**
  > Clash TUN fake-ip 解析到 198.18 段时，勾选「信任此主机」可跳过 S2 黑名单；未信任时给出 fake-ip 签名提示；四入口报错带出内层原因。不改 WebDAV SSRF 主语义（v1.6.1 已做；本版仅 fake-ip 兼容增补）。

* **coordinator 超时自动释放（5min 默认；可配置）**
  > 备份卡死时自动超时释放 activeJob，无需重启即可再次备份。

### 🐛 问题修复 (Bug Fixes)

* **cliff deferred 在看板切换/退出时清理**
  > 切换看板或退出时清理延迟标记，下次备份重新检测断崖。

### 🔧 内部改进 (Internal)

* **三层锁职责矩阵 ADR（R3 有意降级）**
  > 文档化三层锁职责边界；R3 有意降级，未合并锁源，未按审查报告统一单一锁源。

* **C-D68 SSOT 脚本**
  > whitelist / doc-guard / soft / all / ops-hard。

* **v1.6.8 冒烟基线**
  > 4 ID：COORD / CLIFF / LOCK-ADR / WEBDAV-FAKEIP；`check:smoke` 三串联（v1.5.9 + v1.6.0 + v1.6.8）。

* **lint 扩展含 backup 服务路径**
  > lint 覆盖 BackupJobCoordinator / RetentionCleanupOrchestrator / quitHandler。

### 可选手测

* **开启 Clash TUN（fake-ip）→ WebDAV 测试连接**
  > 勾选信任主机后应成功；不勾时应出现 fake-ip 签名提示。

### 明确不做

* **不合并三层锁（R3 有意降级）**
  > 未合并锁源；不以单一锁源替代现有三层。

* **不改 WebDAV SSRF 主语义**
  > v1.6.1 已做；本版仅 fake-ip 兼容增补。

## [v1.6.7] - 2026-08-20

本版本聚焦 BoardDock / ContextMenu 订阅边界与关键表单可访问性：去掉 Dock 顶层整表 `notesById` 渲染订阅，菜单关闭时不再挂载热路径 Content；WebDAV 与便签/看板关键输入补齐 `aria-label`（UX P0-08 / P1-08 已修复）。B1 默认 **已收窄**（不强制目录大搬家）。

关单状态字：UX P0-08 **已修复**；UX P1-08 **已修复**；CODE B1 **已收窄**。

### 🐛 问题修复 (Bug Fixes)

* **平移/拖拽时 Dock 不再因整表 notes 热 reconcile（UX P0-08 已修复）**
  > BoardDock 顶层取消 `notesById` 整表订阅；看板角标改为派生 active count + `useShallow`；事件路径用 `getState()`。

  - 硬测：`P0-08 单 note 坐标更新…`（含源码级无整表渲染订阅断言）

* **右键菜单关闭时不再随 viewport 热更新（UX P0-08 已修复）**
  > 外壳仅订 `isOpen`；关闭不挂载 Content；打开态再订 viewport/shellRect。

  - 硬测：`P0-08 菜单关闭时 viewport 变化不增加 Content 渲染次数`

* **空标题便签显示文案统一为「无标题」**
  > 空标题占位与标题栏展示对齐，不再出现「未命名」混用。

### 🚀 优化 (Optimizations)

* **关键输入可被读屏识别（UX P1-08 已修复）**
  > WebDAV 四输入、新建/重命名看板、NoteCard 标题/正文/图片说明补 `aria-label`（不改 testid/布局）。

### 🔧 内部改进 (Internal)

* **审查收束：P0-08 可带风险补测**
  > 为角标派生 count 与空标题锚点补充回归测试；含 2 行源码微调（无行为变化）。

* **B1 已收窄**
  > 以订阅粒度 + a11y 收敛关单；未做目录大拆分，故不写「已拆分」；目录拆分为可选附录。

## [v1.6.6] - 2026-08-20

本版本聚焦 Store 写路径收口：生产代码对旧 `useStore` 的静态 import 压到 5 个白名单文件（写路径达标 ≤5），并修复单便签热更新时全表镜像导致的卡顿（UX P0-07 已修复，策略 A）。同步修复去 import 后 BoardDock 死循环导致界面/托盘不可用，以及 P0-07 热路径漏通知持久化桥的问题。

关单状态字：CODE S6 **写路径达标（≤5）**；UX P0-07 **已修复**（策略 A）。

### 🐛 问题修复 (Bug Fixes)

* **单便签热更新不再全表镜像 domain（UX P0-07 已修复）**
  > 仅改一条便签位置/内容时，桥改为单 note patch，不再默认 `clone + replaceDomainState` 全表，拖动与输入更跟手。

  - 策略 A：boards / 结构变化 / 多 note 仍走全表（有测锁定）。
  - 硬测：`P0-07 单 note moveNote…` / `…内容更新…` 与多 note 全表路径。

* **P0-07 patch 后仍会通知持久化**
  > 单 note 镜像经 `mirrorPatchNote` 调用持久化桥，避免热路径改完不 dirty、退出丢改动。

* **修复 BoardDock 无限重渲染导致界面卡死/托盘拉不起**
  > `useSaveStatusSlice` 改为 `useShallow`，消除 getSnapshot 每次新对象触发的 Maximum update depth；前端恢复后托盘单击显隐恢复正常。

### 🚀 优化 (Optimizations)

* **写路径 import 白名单压到 5**
  > Canvas / NoteCard / CanvasEngine / BoardDock / main 去掉静态 `from useStore`；读写经 appController / domainStore / uiStore / viewportStore。

  - 保留：`uiStore`、`viewportStore`、`legacyDomainBridge`、`appController`、`PersistenceController`。
  - `legacyDomainBridge` 仍保留（退役条件见依赖地图）；domain 仍经桥镜像，非最终唯一写源。

### ⚡ 性能优化 (Performance)

* **单 note 热路径 0 次全表 replace**
  > 与 P0-07 同源：高频 move/内容更新不再每步整表克隆镜像。

### 🔧 内部改进 (Internal)

* **appController domain 写门面补齐**
  > 组件侧统一薄委托；含恢复快照、拖拽落位坐标批写、isLoaded/saveStatus 订阅钩子。

* **useStore 与 domainStore 共享 apply* mutator**
  > 核心 add/move/软删/硬删等单一实现体；`domainHistory` 仍在 useStore。

* **依赖地图回写 v1.6.6 期末白名单**
  > `v1.5.5-use-store-dependency-map.md` COUNT=5 + 桥退役条件更新。

### 🧹 清理 (Cleanup)

* **Rust webdav/persistence 编译告警清理**
  > unused import、测试桩 `cfg(test)`、可见性与 timeout 常量；dev 构建噪音下降（不改 WebDAV 对外行为）。

## [v1.6.5] - 2026-08-12

本版本完成 WebDAV 后端模块化（CODE W1）：将原单文件 `webdav.rs` 拆为目录叶模块，行为与 invoke 表面保持不变，降低后续维护与审查成本。

关单状态：CODE W1 **已拆分**。

### 🔧 内部改进 (Internal)

* **WebDAV 后端模块化（CODE W1 已拆分）**
  > 原 `src-tauri/src/webdav.rs` 改为 `src-tauri/src/webdav/` 目录模块；配置、SSRF、凭据、传输与上传下载分文件承载，前端命令名与 lib 注册路径不变。

  - 叶模块：`types` / `error` / `ssrf` / `credential` / `config` / `transport` / `ops` + 外置 `tests`。
  - `lib.rs` 对 `webdav::*` 的 11 处注册保持原路径；生产 rs ≤ 8。
  - 单测：`cargo test --lib webdav` 295 passed（基线不塌）。
  - 技术债：overengineering #9 中 **webdav 过大** 记为已拆分；`backup.rs` 仍 open。

## [v1.6.4] - 2026-08-11

本版本完成 Store 读路径迁移 + Viewport 热路径 SSOT，将生产代码对旧 `useStore` 的直接 import 收敛到 10 个白名单文件，并修复多便签平移「卡一半」的 Viewport 双写问题（UX P0-06）。

关单状态：CODE M1/M2 **读路径达标（≤10）**；P0-06 **已修复**（策略 A）。

### 🐛 问题修复 (Bug Fixes)

* **Viewport 热路径仅写 useViewportStore（UX P0-06 硬 0）**
  > pan / edgePush / 拖拽等热更新不再每帧反向双写 `useStore.setState`，多便签平移不再「卡一半」；switchBoard 落盘硬 it 通过。

  - reverse 桥保留 stickyDrag / shellRect / canvas / 尺寸与非 edge interaction，仅跳过 pan(x/y) 与纯 edgePush。
  - 新增 `runtimePan` 断开 useStore ↔ viewportStore 循环依赖。

* **生产 import `useStore` 文件数 ≤10（白名单保留）**
  > 读路径全部迁分 store（uiStore / viewportStore / domainStore）；写入口经 appController 或分 store action。

  - App、ContextMenu、QuickCapture、SmartPaste、SelectionActionBar、ShortcutsManager、TrashGrid、DiagnosticsPanel、detachedNoteSnapshotSync 均去除 `useStore` import。
  - 托盘 tooltip 逻辑随 `saveStatus` 迁至 PersistenceController。

* **Overlay 死代码删除**
  > `DetachedNoteOverlay.tsx` + test 完全移除，无生产引用。

* **FPS/diagnostics 采集门控**
  > `App.tsx` startFPS 仅在 DEV / VITE_ENABLE_PROFILING 下开启，生产默认不常开采集。

### 🔧 内部改进 (Internal)

* **依赖地图 + ≤10 列表回写**
  > `v1.5.5-use-store-dependency-map.md` 更新；C-M3/C-M5 验收全绿。

* **appController 薄委托**
  > 为去 import 追加 undo/redo、trash、智能粘贴等单行薄委托，不新建 service/facade 文件。



本版本统一 ViewMode 切换语义，并修复托盘唤起窗口空白、TRASH↔BOARD 视口恢复边界等问题。关单状态：CODE V1 **已统一**。

### 🐛 问题修复 (Bug Fixes)

* **ViewMode 语义统一（CODE V1 已统一）**
  > 生产入口经 uiStore 的 setViewMode 现具备与旧 useStore 路径一致的完整副作用；TRASH↔BOARD 保存/恢复 `board.viewport`（仅 `{x,y}`）；TRASH 时清理选区/菜单/粘性拖拽等。

  - 权威实现落在 `uiStore.setViewMode`。
  - `useStore.setViewMode` 薄委托权威并 `@deprecated`。
  - 无 `board.viewport` 时不强制清零 runtime pan（优先不破坏）。

* **修复托盘点击后主窗口空白/不显示**
  > 点击系统托盘图标时窗口可正常显示界面，避免 webview 空白、看似托盘失灵。

  - 去掉阻塞 Vite 模块图的 top-level await，改为 microtask 绑定 `uiSetViewModeRef`。
  - 托盘切换后短时内 blur 不误 hide，降低 show/hide 竞态。

## [v1.6.2] - 2026-07-29

本版本聚焦备份链路敏感信息脱敏与依赖瘦身：共享敏感词词表供 TS/Rust 引用，远程备份 catch 错误字段脱敏，移除 keyring 4 并改用 keyring-core + 平台 store，显著减小安装包体积。

### ✨ 新特性 (Features)

* **共享敏感词词表（SSOT）**
  > 新增 `src/shared/sensitive-words.json`，供前端与 Rust 备份活动日志统一脱敏；覆盖 password/token/authorization/secret/username/serverurl/url 等常见字段。

  - BoardDock 删除本地 sanitize 副本，改用共享导出。
  - TS `BackupActivityLogService` 与 Rust `backup_activity` 均引用同一词表。

### 🐛 问题修复 (Bug Fixes)

* **远程备份 catch 错误字段脱敏**
  > `WebDavBackupService.createRemoteBackup` 的 catch 路径对写入活动日志的 `error` 字段做脱敏，避免密码/令牌等明文落盘。

  - 阶段判断仍用原始错误；仅落库字段脱敏（R2 已收窄，非全链路已修复）。
  - 补充单测「createRemoteBackup catch 脱敏」。

### 🚀 优化 (Optimizations)

* **移除 keyring 4，改用 keyring-core + 平台 store**
  > 依赖树去掉 keyring v4；Windows 使用 `windows-native-keyring-store`，启动时 `set_default_store`。

  - 发布构建体积约 **6.68 MB**（较 v1.6.1 基线约 11.4 MB 回收约 5 MB）。
  - 升级后可能需重输一次 WebDAV 密码（store 切换，C-K3 手动未验）。

## [v1.6.1] - 2026-07-28

本版本聚焦 WebDAV HTTPS 备份链路的 SSRF 硬化：扩展特殊用途与隧道相关 IP 黑名单、连接阶段 DNS 失败改为 fail-closed、解析结果 pin 防 rebinding，并提供「信任此主机」仅用于 DNS 失败时的一次重试。

### ✨ 新特性 (Features)

* **「信任此主机」配置**
  > WebDAV 设置可勾选「信任此主机（DNS 失败时重试；IP 黑名单仍生效）」。仅在 DNS 解析失败时再解析一次；仍失败则拒绝连接。勾选**不会**放行内网或黑名单 IP，也不会放宽重定向校验。

  - 旧配置缺省为未信任（`trust_host=false`）。
  - 主机名变更时需重新确认信任；同 host 规范化（大小写 / trailing-dot）不误清。

### 🐛 问题修复 (Bug Fixes)

* **WebDAV 连接 DNS 失败不再静默放行**
  > 连接前解析失败时默认拒绝，避免 fail-open 绕过 SSRF 检查。

* **扩展不可达 / 特殊用途目标 IP 拦截**
  > 在原有私网、回环等基础上，拒绝 CGNAT、TEST-NET、240/4、组播/广播、IETF Shared、文档与废弃站点本地 IPv6、NAT64 非 WKP、Teredo 前缀，以及对 6to4 / NAT64 嵌入私网地址的递归判定。

* **连接时固定解析 IP，降低 DNS rebinding 风险**
  > 校验通过后的地址表 pin 到 HTTP 客户端；后续请求不因二次解析换 IP 而绕过首检。

* **HTTPS 重定向仅允许同 host**
  > 重定向目标完整走 SSRF 校验；不同 host / 不同 port / trailing-dot 主机名拒绝；信任开关对重定向无效。

### 🔧 内部改进 (Internal)

* **SSRF 验收门禁与 DoD runner**
  > 新增范围白名单、normalize 无 DNS、Commit2+3 同树、入口顺序，以及 `run-v161-dod` 封装关键单测与静态门。

* **可 mock 的 DNS 解析抽象**
  > `HostResolver` 支持单测注入，覆盖 fail-closed、信任重试、pin 与 rebinding 逻辑证明。

## [v1.6.0] - 2026-07-20

本版本聚焦「捕获与整理体验增强」：明确快速捕获目标看板、失败不丢输入、批量创建可一次撤销/重做，并补齐 v1.6.0 冒烟基线与 C28 范围门禁。捕获后程序定位未交付。

### ✨ 新特性 (Features)

* **Quick Capture 目标看板边界**
  > 快速捕获浮层展示 `快速捕获 · {label}`；看板名为空时回退为「当前看板」，过长名称截断显示，emoji 不抛错；禁用「页面/工作区/项目」产品词。

* **捕获失败保留输入**
  > 提交失败时浮层不关、输入不丢，并展示「创建失败，输入已保留」；仅成功时清空并关闭。

* **批量捕获 Undo/Redo**
  > `addNotesWithContentBatch` 一次写入一条 compound 历史，一次撤销/重做整批便签。画布智能粘贴 / 控制器智能粘贴与快速捕获共享同一 batch Undo 语义。

### 🔧 内部改进 (Internal)

* **batch 创建高亮/选区契约**
  > 批量创建后锁定 `noteHighlights[id].reason==='created'`、`recentlyCreatedIds` 与 `selectedIds` 一致。

* **TRASH 视图下捕获落点**
  > `viewMode==='TRASH'` 时 batch 仍写入 `currentBoardId`，便签不进入废纸篓。

* **v1.6.0 冒烟基线与 C28 门禁**
  > 新增 `docs/qa/v1.6.0-smoke-baseline.md`；`check:smoke` 双基线串联；`scripts/check-c28-scope.mjs` 作为范围白名单 SSOT；lint 覆盖 `QuickCaptureOverlay`。

* **v1.6.0 手动测试回归清单**
  > 新增 `docs/qa/v1.6.0-manual-test-checklist.md`，覆盖 QC-BOARD / QC-FAIL / QC-HIGHLIGHT / QC-UNDO 主路径及 QC-LOCATE N/A 说明，供发布前手测与回归复用。

* **Optional：捕获后程序定位**
  > 捕获后程序定位与 Viewport Action gate（C29）本版本未交付；冒烟 `QC-LOCATE-001=N/A`。

## [v1.5.9] - 2026-07-11

本版本为进入 v1.6.0 前的预备与体验基线：建立关键流程冒烟与文案守卫，定稿「捕获与整理体验增强」主线，收口低风险反馈，并补齐备份链路安全/可观测补丁与发布证据门禁。

### ✨ 新特性 (Features)

* **Quick Capture 目标看板反馈**
  > 快速捕获浮层展示当前看板名称；名称为空时回退为「当前看板」，过长名称截断显示。

### 🐛 问题修复 (Bug Fixes)

* **图片缺失反馈领域语言**
  > 图片便签缺失占位继续使用「图片不可用」表述，并增加自动化守卫，避免误写成「附件」语义。

* **活动日志敏感值脱敏（空格赋值 / 引号值）**
  > 修复 `password = secret`、`password="a b c"` 等可漏脱敏的组合路径；TS 与 Rust 两侧同步收紧，并补充回归测试。

* **WebDAV 同 key 写配置失败时凭据回滚**
  > 旧 secret 为 MissingSecret 时，写盘失败会删除新写入的同 key 凭据；load 非 Missing 错误则中止保存，避免不可回滚覆盖。

* **活动日志清空失败不被成功轮询抹掉**
  > 清空活动失败后，列表轮询成功不再清除 mutation 错误提示。

* **退出前 fallback 备份耗时可信**
  > before-exit fallback 在 runner 前记录 `startedAt`、完成后记录 `finishedAt`，避免活动日志/状态中时长恒为 0。

### 🔧 内部改进 (Internal)

* **关键流程冒烟基线**
  > 新增 `docs/qa/v1.5.9-smoke-baseline.md` 与 `scripts/check-smoke-baseline.mjs`，按 ID 分段校验自动化证据与 Result。

* **冒烟结果诚实化与证据锚点**
  > DET / 混合项不再用 PASS 暗示未执行的手测；恢复相关 ID 锚定 BoardDock 恢复与缓存刷新路径，并写明分测边界。

* **冒烟校验防假绿**
  > 短语校验仅匹配「自动化命令 / 证据」字段；拒绝重复 ID 与同段多个 Result，避免文档合并导致发布门禁假绿。

* **冒烟基线进入发布门禁**
  > `npm run check` 串联 `check:smoke`；标签发布流程执行 `npm run check` 时一并校验冒烟文档。

* **WebDAV / 备份文案守卫**
  > 设置区、自动备份区、活动日志区增加「不暗示同步 / 不暗示审计遥测」具名测试与 helper。

* **活动日志与 Undo 契约锚定**
  > 本地备份/恢复路径绑定活动日志具名测试；程序定位路径断言不写入 Undo 历史。

* **v1.6.0 单主题计划与原型**
  > 定稿 Primary Theme「捕获与整理体验增强」，并输出轻量流程原型；Spotlight 与图片查看记为 Deferred。
  > 程序触发 Viewport Action gate 记入 v1.6.0 依赖，不在本版本预建。

## [v1.5.8] - 2026-07-08

本版本新增备份活动日志系统，提供备份/恢复/保留清理的完整可观测性，并修复 WebDAV 配置安全、敏感信息脱敏、退出前备份语义等多项稳定性问题。

### ✨ 新特性 (Features)

* **备份活动日志系统**
  > 新增完整的备份活动记录与 UI 展示，支持轮询刷新与手动清空，覆盖自动备份、手动备份、保留清理、恢复等全场景。
  - Rust 侧活动日志持久化模块，含敏感信息脱敏
  - TS 侧 BackupActivityLogService，含脱敏辅助函数
  - 接入自动备份、手动 WebDAV、本地备份/恢复、保留策略等操作
  - 活动日志 UI 支持轮询刷新与一键清空

* **设置浮层组件抽取**
  > SettingsPanelShell 统一设置浮层容器，修复内容遮挡问题。

### 🐛 问题修复 (Bug Fixes)

* **WebDAV 配置安全**
  > 修复 WebDAV 配置写入失败时凭据不回滚、Windows 崩溃后 orphan `.bak` 残留等问题。
  - 保存配置前自动恢复 orphan `.bak` 文件
  - 写入失败时回滚凭据
  - 清空活动日志时同步删除 orphan `.bak`

* **敏感信息脱敏边界**
  > 修复活动日志中 Unicode 大小写扩展、全角冒号、路径空格、Bearer/Basic token 等脱敏绕过问题。
  - 修复 Unicode 大小写转换偏移绕过
  - 修复全角冒号脱敏值边界
  - 修复路径空格泄露
  - 补充 Bearer/Basic token 替换
  - 补充中文敏感词匹配

* **退出前备份语义**
  > 修复退出前备份 fallback 路径、早退分支、single-flight 等场景的失败原因丢失问题。
  - fallback 备份使用 capturedStorageUpdatedAt 替代重新读取
  - 保留早退分支的原始失败原因
  - single-flight 补充 reasonCode/stage/trigger

* **活动日志 UI 状态**
  > 修复手动刷新 loading 卡住、并发备份中文提示缺失、busy 状态未同步等问题。
  - 手动刷新始终清掉 loading 状态
  - 并发备份时显示"已有备份运行中"
  - 手动保留清理同步 lastRetentionCleanupBusy 字段

* **备份恢复稳定性**
  > 修复远端恢复成功后 scheduler 内存态未重置、活动轮询关闭竞态等问题。
  - 恢复成功后 reload scheduler 内存态
  - 活动轮询关闭时正确失效旧结果
  - Rust 状态结构补充 lastRetentionCleanupBusy 字段

* **图片便签尺寸**
  > 修复极端图片比例导致的尺寸计算异常。

### 🔧 内部改进 (Internal)

* **CI 测试步骤**
  > release.yml 添加测试步骤，lint 白名单补充 BoardDock.tsx。

* **活动刷新判定**
  > 拆分活动刷新判定 helper 以通过 lint 检查。

* **保留清理 metrics**
  > 调整保留清理结构化 metrics 与断崖警告语义。

## [v1.5.7] - 2026-06-25

本版本为远端备份系统新增保留策略，支持自动清理过期备份、断崖式数据骤降保护，并在看板设置区提供手动预览与清理入口。

### ✨ 新特性 (Features)

* **远端备份保留策略**
  > 在看板设置区的定时远端备份区域新增保留策略开关，支持设置保留数量（5/10/20/50），自动备份成功后按策略清理最旧的备份文件。

  - 仅对严格命名的 `SoNotes_Backup_YYYYMMDDHHMMSS.zip` 文件生效，不匹配的文件不受影响。
  - 首次自动成功备份仅建立健康基线，不执行清理，确保至少有一次健康参照。
  - 支持手动预览将删除/保留的文件列表、时间边界，确认后立即执行。

* **断崖式数据骤降保护**
  > 每次自动备份成功后，系统会对比健康基线的多维指标（便签数、画板数、图片便签数、图片附件数、zip 大小），检测是否存在数据骤降。

  - 5 个维度独立检测，各有独立阈值（便签 ≥10 用 30%、画板 ≥3 用 50% 等）。
  - 检测到异常时暂停自动清理，标记为"待确认"，等待用户审查后决定。
  - 用户可在设置区查看可疑备份详情，选择确认健康基线或清除警告。

* **手动清理入口与预览**
  > 在看板设置区提供"预览清理"和"执行清理"按钮，支持在自动清理之外手动管理远端备份。

  - 预览展示待删除文件列表、保留文件列表、最旧删除时间与最新保留时间。
  - 执行前展示确认弹窗，清理后自动刷新远端备份列表。
  - 预览与执行间配置/目标变更时自动检测并要求重新预览。

* **备份结果摘要透传**
  > 远端备份结果现在携带备份摘要（便签/画板/图片数量）和 zip 大小，用于断崖检测和状态展示。

### 🐛 问题修复 (Bug Fixes)

* **修复 Rust 反序列化旧状态文件兼容性**
  > 新增的 `cliff_drop_deferred` 等 `bool` 字段在旧状态 JSON 中缺失时会导致加载失败，已补充 `#[serde(default)]` 注解。

* **修复 5xx 错误误匹配为幂等成功**
  > 远端清理执行服务将含 "5" 的错误消息误判为 5xx 错误，已修正为精确匹配 3 位状态码。

* **修复退出前备份 fallback 路径状态不完整**
  > 调度服务不可用时的 fallback 路径缺少 `lastStartedAt`、`nextRunAt`、`credentialFailures` 等关键状态字段，已补齐。

* **修复手动清理 retainedCount 始终显示为 0**
  > 传入候选列表时 `keepCount` 未正确传入，已修正。

* **修复确认健康基线后调度器内存状态未同步**
  > 确认基线和手动清理后调用 `reloadState()` 防止后续定时备份覆盖刚写入的状态。

* **修复清理刷新降级逻辑过度移除**
  > 清理后刷新远端列表失败时的 fallback 逻辑，从"移除全部候选"改为"只移除实际已删除的前 N 个"。

* **修复预览执行竞态保护**
  > 预览时快照保留数量、保护集合和 WebDAV 目标，执行前校验一致性，配置变更时要求重新预览。

* **修复 Rust 删除兜底校验不校验日历合法性**
  > `webdav_delete_backup` 的文件名校验从"匹配 14 位数字"升级为完整日历合法性验证。

* **修复小样本场景下断崖保护失效**
  > 纯笔记用户（无画板）不再因 `boardCount=0` 永久阻塞清理；小样本场景（基线 <5 条便签）下可疑空备份不再被误提升为健康基线。

* **修复清理失败后无法重试**
  > orchestrator 条件从"从未清理"改为"从未清理或上次清理失败"，清理失败后下次有新备份时自动重试。

* **修复单个删除绕过全局 single-flight**
  > 单个远端备份删除现在经由 `BackupJobCoordinator` 获取全局锁，防止与定时备份/清理并发执行。

### 🚀 优化 (Optimizations)

* **保留策略默认关闭且仅对严格命名文件生效**
  > 保留策略默认 `retentionEnabled: false`，避免误删；仅匹配 `SoNotes_Backup_YYYYMMDDHHMMSS.zip` 的文件进入清理范围。

### 🔧 内部改进 (Internal)

* **新增保留策略回归测试**
  > 新增 77+ 个测试用例覆盖保留策略引擎、清理执行服务、清理编排逻辑和调度服务集成。

* **CI 加入 Rust 测试**
  > `build.yml` 和 `npm run check` 脚本新增 `cargo test`，确保 Rust 侧测试在 CI 中执行。

## [v1.5.6] - 2026-06-17

本版本新增定时远端备份功能，支持固定频率自动上传备份快照到 WebDAV 服务器，并提供退出前备份提示与完整的失败恢复流程。同时优化撕下窗口交互体验。

### ✨ 新特性 (Features)

* **定时远端备份**
  > 支持按 6 小时 / 12 小时 / 每天 / 每周的固定频率，自动将完整备份快照上传到 WebDAV 服务器。默认关闭，需用户显式启用。
  - 自动备份仅使用已保存的系统凭据，不依赖会话密码
  - 支持静默期机制：用户活跃时延后上传，空闲时自动补跑
  - 凭据连续失败 3 次后自动暂停调度，需用户重新保存密码
  - 备份状态实时展示于 BoardDock 设置面板

* **退出前备份提示**
  > 退出应用时，若检测到本地有未备份的变化，弹出确认对话框提供"先备份再退出"、"直接退出"、"取消"三个选项。
  - 备份失败时可选择"仍然退出"或"取消"返回应用
  - 退出提示仅在自动备份已启用且有未备份变化时触发

* **撕下便签拖拽区域限制**
  > 撕下窗口的拖拽区域从整个便签表面缩小到标题区域，正文内容区域现在可以正常选中和复制文本。
  - 文本便签和图片便签均通过标题区域拖拽移动
  - 控制按钮（定位/置顶/关闭）仍保持原有交互逻辑

* **图片便签撕下支持异步加载**
  > 撕下的图片便签窗口现在支持异步解析附件 URL，缓存未命中时自动加载并显示 loading 状态。

### 🐛 问题修复 (Bug Fixes)

* **修复定时远端备份调度快速重试循环**
  > 备份失败或跳过后不再立即重试，改为按固定频率等待下一次调度。

* **修复退出前备份判断逻辑**
  > 退出提示现在正确检查磁盘快照是否已被成功备份覆盖，移除了不一致的阈值判断。

* **修复 before-exit 备份失败被静默吞没**
  > 退出前备份的所有失败路径现在正确传播到用户确认流程，不再直接退出。

* **修复手动备份状态记录不完整**
  > 手动远端备份成功/失败后正确写入 `lastStartedAt`、`lastManualSuccessAt`、`lastAttemptCapturedStorageUpdatedAt` 等状态字段。

* **修复 Windows 原子写入崩溃恢复**
  > 配置文件写入改为带备份恢复的覆盖流程，崩溃后可自动恢复孤立的 `.bak-*` 文件。

* **修复 errorStage 映射不完整**
  > WebDAV 凭据错误（401/403）正确映射为 `credential` 阶段，触发凭据退避机制；锁冲突映射为 `single-flight`。

* **修复调度器内存态不同步**
  > 手动备份成功后自动刷新调度器内部状态，确保下次定时触发使用最新时间戳。

### 🔧 内部改进 (Internal)

* **新增 v1.5.6 定时远端备份计划文档**
  > 完整的设计文档记录了定时备份的架构决策、状态模型、调度策略和测试覆盖要求。

## [v1.5.5] - 2026-06-13

本版本完成首批低风险状态迁移（UI/Viewport 入口），建立旧 useStore 依赖地图作为后续迁移基线，并修复撕下窗口"悬浮"徽章在快照更新后不消失的竞态问题。

### 🐛 问题修复 (Bug Fixes)

* **修复撕下窗口"悬浮"徽章在快照更新后不消失**
  > 当第二张快照在 1600ms 窗口内到达时，React effect cleanup 会清掉隐藏定时器，导致"悬浮"永久停留。
  - 定时器改为仅在组件卸载时清理，快照更新不再取消已有定时器
  - 新增回归测试覆盖 1600ms 内二次快照场景

### 🚀 优化 (Optimizations)

* **迁移 appController UI-only 入口到 useUIStore**
  > Spotlight、ViewMode、Dock、Pinned、QuickCapture 等 UI-only 入口从旧 useStore 迁移到 useUIStore，减少对旧 store 的直接依赖。
  - 新增 `getUIState()` helper 统一 UI state 读取
  - 双向同步桥保留，旧 store 订阅不受影响

* **迁移 appController Viewport-only 入口到 useViewportStore**
  > `resetViewport` 和 `syncShellViewport` 从旧 useStore 迁移到 useViewportStore。
  - 新增 `getViewportState()` helper 统一 Viewport state 读取

* **迁移 useCanvasGlobalListeners Spotlight 只读路径**
  > Space 键处理中的 `isSpotlightOpen` 读取从旧 useStore 改为 useUIStore。

### 🔧 内部改进 (Internal)

* **新增旧 useStore 依赖地图**
  > 建立生产代码与测试代码的全量依赖地图，标记迁移批次与风险等级，作为后续版本迁移执行索引。
  - 覆盖 20 个生产模块约 54 条依赖、22 个测试文件
  - 明确 legacyDomainBridge 删除四前提

* **补充 appController 入口迁移测试**
  > 为首批迁移入口新增新旧 Store 镜像断言，确保迁移前后行为一致。

* **补充 openQuickCapture TRASH 模式用例**
  > 锁住"先回 BOARD，再异步打开 Quick Capture"的行为路径。

* **修复 v1.5.5 文档回归命令**
  > 移除文档中不存在的 `useCanvasGlobalListeners.test.tsx` 路径，实际覆盖在 `Canvas.test.tsx`。

## [v1.5.4] - 2026-06-12

本版本将 WebDAV "记住密码"从占位状态推进到系统密钥链闭环，密码不再写入配置文件，改为通过系统凭据管理器安全存储；同时补充凭据读取校验与脱敏测试，防止 secret 泄漏或被复用到不匹配的目标。

### ✨ 新特性 (Features)

* **WebDAV 凭据存储抽象与系统密钥链集成**
  > 新增 `WebDavCredentialStore` trait 及系统密钥链实现，"记住密码"功能从占位状态升级为真实系统凭据管理器读写。
  - 密码/令牌不再写入配置文件，改为通过 keyring crate 存入系统凭据管理器
  - 默认测试使用内存 store，系统密钥链测试标记 `#[ignore]`

* **保存/加载配置接入密钥链语义**
  > 配置保存时计算 SHA-256 凭据标识，加载时通过标识从密钥链读取密码；切换配置时自动清理旧 secret。

* **远端操作自动读取已保存凭据**
  > 连接测试、列表、上传、下载、删除五个远端操作在密码为空时自动从密钥链加载已保存凭据，无需重复输入。

* **前端保存配置与凭据状态联动**
  > 保存配置后前端正确维护 `passwordSaved` 状态；清除配置时同步删除密钥链 secret 并反馈清理结果。

* **前端远端操作凭据失败处理**
  > 远端操作缺少可用凭据时立即提示用户输入密码，不再发起注定失败的网络请求。

### 🐛 问题修复 (Bug Fixes)

* **修复 Windows 密钥链初始化缺失**
  > keyring 4.x 的 `use_windows_native_store` 需要 `HashMap` 参数，初始化缺失导致保存凭据失败。

* **收紧已保存凭据的读取校验**
  > 读取已保存 secret 前校验当前操作的 URL/用户名/目录是否与保存配置一致，防止旧 secret 被发送到用户临时修改的 WebDAV 目标。

* **修复取消"记住密码"后前端状态未清除**
  > `rememberPassword=false` 保存成功或返回 warning 时，前端正确清除 `passwordSaved` 状态。

* **修复 BoardDock 测试中 passwordSaved 模拟值**
  > 远端操作测试的 `loadConfig` mock 改为 `passwordSaved: true`，匹配凭据校验逻辑。

### 🔧 内部改进 (Internal)

* **新增凭据脱敏断言**
  > 新增 4 个测试验证 secret 不会泄漏到错误消息、Debug 输出或用户可见文案中。

* **mock server header 解析统一为大小写不敏感**
  > `Depth:` 解析从大小写敏感的 `strip_prefix` 改为 case-insensitive 匹配，与 `Authorization`/`Content-Length` 处理方式一致。

* **补充 v1.5.4 计划文档实施记录**
  > 记录实际 keyring 依赖、Windows 平台初始化要点、测试覆盖情况与计划偏差。

## [v1.5.3] - 2026-06-12

本版本聚焦 WebDAV 传输层可靠性与协议边界加固，让远端备份操作在各种异常服务端响应下的错误语义更精确、更安全、更可测。

### 🐛 问题修复 (Bug Fixes)

* **收紧上传重试边界，仅允许同名冲突重试**
  > 上传遇到 405 Method Not Allowed、423 Locked、507 Insufficient Storage 等非冲突状态时立即失败，不再无差别重试；仅 409 Conflict 和 412 Precondition Failed 保留重试逻辑。

* **上传前置目录检查接入错误分类**
  > PROPFIND/MKCOL 阶段的 405/409/423/507 等状态码现在返回精确错误（如"远端资源被锁定"），不再统一折叠为"远端备份上传失败"。

* **PROPFIND 解析失败使用内部错误分类**
  > XML 解析失败返回 `InvalidPropfindResponse` 分类，不再直接透传字符串；测试可断言错误类型而非脆弱的文案匹配。

* **修复删除 401/403 覆盖缺口与注释精确性**
  > 补充删除操作 401/403 mock 测试；简化 `NotFound` 冗余分支；明确 `retryable` 字段仅是分类标签而非执行策略。

### 🔧 内部改进 (Internal)

* **新增 WebDAV 错误分类模型**
  > 内部新增 `WebDavErrorKind`、`WebDavOperation`、`WebDavOperationError` 类型，将 401/403/404/405/408/409/412/423/429/507/5xx 等状态码映射为结构化分类，作为后续测试与错误处理的统一锚点。

* **拆分请求入口支持 mock 测试**
  > 新增 `WebDavRequestTarget` 与 `*_with_client` 函数，移除内部 `reqwest::Client` 创建，使测试可通过 `http://127.0.0.1:PORT` 注入 mock server。

* **新增 mock 测试基础设施与 fixture 覆盖**
  > 新增 `MockWebDavServer`（单请求/单响应/记录 method/path/Depth/Authorization presence）和 8 个 PROPFIND fixture，覆盖标准 207、命名空间变体、目录自身条目、混合状态、缺失/非法 size、URL 编码文件名、畸形 XML 等边界。

* **全面接入错误分类与 mock 集成测试**
  > 连接/列表/上传/下载/删除操作全部接入错误分类模型，新增覆盖 401/403/405/423/507/500 及传输错误的 mock 集成测试（共 218 个 WebDAV 测试）。

* **新增真实服务端兼容记录模板**
  > 新增 `docs/plans/webdav-real-server-compatibility-template.md`，为后续记录不同 WebDAV 服务端的实际行为提供标准化模板。

## [v1.5.2] - 2026-06-11

本版本在 v1.5.1 基础上新增备份验证与恢复摘要能力，让本地 zip 恢复和 WebDAV 远端恢复在覆盖当前数据前先验证备份包完整性并展示结构化摘要，验证失败时本地数据不受影响。本版本不改变 zip 备份格式。

### ✨ 新特性 (Features)

* **恢复前备份验证与结构化摘要**
  > 本地 zip 恢复和 WebDAV 远端恢复现在会在覆盖当前数据前先对备份包做只读验证，并展示结构化摘要；验证失败时本地数据不受影响。

  - 新增 `validateLocalBackup` 只读验证命令，读取 zip 后校验 manifest、data.json、图片文件 hash 与大小，不修改本地数据。
  - 验证通过后展示摘要：看板数、便签数、文本便签数、图片便签数、废纸篓便签数、图片文件数与图片文件总大小。
  - 验证失败返回结构化错误码与错误说明，不调用恢复命令，本地数据不受影响。
  - 本地 zip 恢复流程调整为：选择 zip → 验证 → 展示摘要与确认 → 恢复。
  - WebDAV 远端恢复流程调整为：下载 → 验证 → 展示摘要与确认 → 恢复；验证失败或用户取消会清理已下载临时文件。

### 🐛 问题修复 (Bug Fixes)

* **自绘确认对话框替换 Tauri 原生确认**
  > 使用 Zustand + React 自绘确认对话框，避免 Tauri 原生 confirm 在部分环境下阻塞或样式不一致。

* **展示所有备份验证错误而非仅首条**
  > 验证失败时展示全部错误条目（含数量前缀），不再只显示第一条导致用户反复修复。

* **修复图片校验错误匹配与远端删除成功文案**
  > 图片文件大小/哈希校验错误现在正确映射到对应错误码，不再落入通用兜底；远端删除成功时固定展示正确文案。

* **修复备份摘要统计语义与创建时间时区显示**
  > 摘要中"文本/图片便签"仅统计活跃便签，总数与分类之和一致；备份创建时间显示本地时间而非 UTC。

* **修复确认对话框连续调用时旧 Promise 未正确拒绝**
  > 连续调用 `open()` 时旧 Promise 正确收到 `false`，不再悬挂；`set` 回调中不再执行副作用。

* **修复错误路径 staging 目录冗余清理**
  > 恢复失败时 staging 目录由 guard 统一清理，不再手动重复删除。

### 🔧 内部改进 (Internal)

* **补充备份验证与恢复测试覆盖**
  > 新增覆盖 Rust 验证命令、前端服务层、本地恢复和 WebDAV 远端恢复的验证摘要交互测试。

  - 覆盖验证成功、验证失败、用户取消确认和远端恢复 token 清理路径。
  - 验证失败路径覆盖不可读文件、非 zip 文件、损坏 zip、缺失或非法 manifest/data、hash 不匹配和大小超限。

* **补充备份验证稳定错误码与测试时区稳定性**
  > 稳定错误码清单补入 `image_file_size_mismatch`；测试断言使用动态本地格式化，跨时区通过。

## [v1.5.1] - 2026-06-10

本版本在 v1.5.0 基础上集中修复备份恢复可靠性、WebDAV 安全边界、Undo/Redo 历史覆盖、图片缓存与撕下便签状态问题，让已有功能从"能用"推进到"可验证、可回滚、可防御"。

### 🐛 问题修复 (Bug Fixes)

* **修复 zip 恢复伪造大小绕过风险**
  > zip 恢复现在按实际读取字节校验大小，不再信任 `entry.size()` 声明值。

  - manifest.json、data.json 和附件条目都改用实际读取字节与声明大小双重校验。
  - 附件总大小按实际读取字节累计，不再只看 header 元数据。

* **修复恢复后图片显示不可用**
  > 从备份恢复后，Image Note 的图片资源会在首次渲染前预热缓存。

  - 恢复流程现在会先失效旧缓存，再异步预热所有 Image Note 的 asset URL。
  - 不再需要重启程序才能看到恢复后的图片。

* **修复 `restoreNote` 撤销看板归属错误**
  > 废纸篓便签恢复到 fallback 看板后，撤销会正确回到原始看板。

  - 撤销 patch 现在记录移动前的真实 `boardId`，而非 fallback 目标。
  - 重做仍恢复到 fallback 看板并提升 z-order。

* **修复 undo/redo 多次 `set()` 导致中间态渲染**
  > `undoDomainChange` / `redoDomainChange` 合并为单次原子 store 更新。

  - 领域 patch、历史栈、悬挂 UI 清理和导航/选区/viewport 都在一次 Zustand 更新中完成。
  - 消除了 `selectedIds` 中间悬挂渲染窗口。

* **修复撕下便签置顶状态读取旧 store 引用**
  > `detachNote` 打开窗口前会重新读取最新置顶状态。

  - 不再使用 `addDetachedNote` 之前的旧快照判断 `keepAlwaysOnTop`。

* **修复 WebDAV 配置 `Debug` 泄漏密码**
  > `WebDavConfig` 和 `WebDavConfigSaveRequest` 的 `Debug` 输出不再包含明文密码。

* **修复普通交互污染撤销历史**
  > 点击或拖拽便签时的 `bringToFront` 不再写入领域撤销栈。

  - 只有右键菜单显式"置顶"才记录历史。
  - 程序性定位（locate）也跳过历史记录。

* **修复导入 `.bak` 与恢复 staging 临时资源残留**
  > `atomic_import` 成功后会清理 `.bak` 文件，`restore_local_backup` 异常路径会清理 `.restore_staging_*` 目录。

* **修复图片文件 hash 锁表只增不清理**
  > 附件写入完成后，若无其他并发引用，会自动移除对应 hash 锁表条目。

### 🔧 安全加固 (Security)

* **拒绝指向内网地址的 HTTPS WebDAV URL**
  > `https://` WebDAV 目标若指向本机、私网、链路本地或未指定地址，会被拒绝。

  - HTTP 例外仅保留 `localhost`、`127.0.0.1`、`::1` 开发场景。
  - DNS 解析失败按 fail-open 处理，避免离线域名回归。

* **WebDAV redirect 防护**
  > WebDAV 请求的 30x 重定向目标也会经过内网地址校验。

  - redirect 不允许 HTTP 或内网目标。
  - 最多跟随 10 次重定向。

* **使用安全随机下载 token**
  > WebDAV 下载 token 从时间戳改为 128-bit 密码学强随机 hex。

* **原子化写入 WebDAV 配置**
  > WebDAV 配置保存改为同目录临时文件 + `sync_all` + `rename`，失败路径自动清理。

### 🔧 内部改进 (Internal)

* **补齐批量操作领域撤销/重做历史**
  > `duplicateNote`、`duplicateSelectedNotes`、`moveNoteToBoard`、`copyNoteToBoard`、`moveSelectedNotesToBoard`、`copySelectedNotesToBoard`、`batchToggleCollapse`、`bringToFront`、`batchBringToFront`、`batchSendToBack` 现在都支持撤销/重做。

  - 每个用户意图生成一条历史条目。
  - 复制/跨看板移动/批量折叠/置顶/置底均可用 Ctrl+Z 撤销。

* **补充备份计划与撕下便签视觉原型文档**

## [v1.5.0] - 2026-06-09

本版本正式接入 WebDAV 远端备份能力，在本地 zip 全量备份/恢复基础上增加远端创建、列出、恢复和删除备份入口；同时补齐旧数据兼容、图片文件清理、抓手模式交互和远端备份性能边界，让备份能力从“本地兜底”推进到“本地优先 + 用户自有远端仓库”的可靠闭环。

### ✨ 新特性 (Features)

* **新增 WebDAV 远端备份仓库**
  > 数据管理区新增 WebDAV 远端备份/恢复入口，可将本地 zip 快照上传到用户自己的 WebDAV 服务，并从远端列表选择恢复。

  - 支持保存 WebDAV 配置、测试连接、创建远端备份、刷新远端列表和从远端备份恢复。
  - 创建远端备份前会先刷新当前本地数据，避免上传过期快照。
  - WebDAV 只作为远端备份仓库，不作为同步系统、远端运行时数据源或多端合并机制。

* **支持删除单个远端备份**
  > 远端备份列表新增删除入口，可显式删除用户选中的单个远端备份文件。

  - 删除前需要二次确认，并提示远端备份文件删除后不可恢复。
  - 只允许删除符合 `SoNotes_Backup_YYYYMMDDHHMMSS.zip` 命名的单个备份。
  - 删除远端备份不会影响当前本地看板和便签。

### 🐛 问题修复 (Bug Fixes)

* **修复旧数据缺少便签类型导致备份失败**
  > 旧版文本便签缺少 `kind` 字段时，现在会按文本便签兼容迁移，避免创建或恢复备份时失败。

  - 前端旧数据迁移会为缺失或非法 `kind` 的文本便签补齐 `kind: "text"`。
  - Rust 备份恢复可兼容旧 zip 中缺少 `kind` 的文本便签。
  - 显式非法的便签类型仍会被恢复校验拒绝。

* **修复永久删除图片便签后图片文件残留**
  > Permanent delete Image Note 后，若对应图片文件不再被任何 Note 引用，会自动清理该图片文件。

  - 仍被看板或 Trash 中其他 Note 引用的图片文件不会被误删。
  - 批量永久删除和清空废纸篓使用同一套引用安全清理规则。
  - 图片文件清理失败不会回滚已经完成的永久删除。

* **修复抓手模式下便签标题和按钮隐藏卡住**
  > 抓手模式点击或拖拽便签后，标题、复制按钮和操作按钮会按指针位置恢复显示。

  - 指针仍在便签内时，拖拽结束后会恢复便签 chrome。
  - 指针离开便签时继续保持隐藏状态。
  - 抓手模式下可从便签表面直接平移画布，不再形成交互死区。

* **修复拖拽未选中便签时旧框选态残留**
  > 框选多张便签后拖拽另一张未选中便签时，选区会收敛到当前便签。

  - 拖拽多选范围内的便签时仍保留组选拖拽。
  - 该修复只影响界面选择状态，不改变领域数据。

### 🚀 优化 (Optimizations)

* **优化 WebDAV 远端备份时间显示**
  > 远端备份文件名与列表时间现在按系统本地时区显示。

  - 创建远端备份的文件名使用本机本地时间生成。
  - 列表中的服务端修改时间会转换为本机本地时间格式。
  - 无法解析的服务端时间会回退显示原文。

* **降低 WebDAV 上传内存峰值**
  > 远端备份上传不再跨重试持有并复制完整 zip 内容。

  - 每次上传重试都会重新打开临时 zip 文件并以流式 body 发送。
  - 保留 `If-None-Match: *` 防覆盖语义和同名冲突后等待下一秒重试的行为。

### 🔧 内部改进 (Internal)

* **补齐 WebDAV 安全与测试边界**
  > WebDAV 远端备份链路补齐配置、传输、临时文件和测试边界。

  - 默认要求 HTTPS，仅允许本机开发地址使用 HTTP。
  - 远端目录限制为单级相对目录，远端备份文件名使用严格正则校验。
  - 下载 token 具备生命周期管理，下载阶段设置 1 GiB 上限，并在启动时清理过期临时文件。
  - 新增连接测试、远端列表、上传、下载、删除、前端服务和数据管理区交互测试。

## [v1.4.9] - 2026-06-08

本版本正式接入本地 zip 备份与恢复能力，并围绕 StorageData 契约、图片文件引用、WAL / 磁盘启动仲裁和恢复回滚路径补齐安全边界，让全量备份从“可用入口”推进到“可验证、可恢复、可防错”。

### ✨ 新特性 (Features)

* **新增本地 zip 备份与恢复**
  > 数据管理入口现在支持创建本地 zip 全量备份，并可从 zip 覆盖恢复看板、便签与图片文件。

  - 备份包包含 `data.json`、`manifest.json` 与图片文件。
  - 恢复前会先刷新当前持久化状态，恢复期间暂停自动保存。
  - 恢复成功后会水合看板、便签、图片路径缓存，并清理 Undo/Redo 与 WAL。

### 🐛 问题修复 (Bug Fixes)

* **修复 zip 备份恢复的契约不一致风险**
  > 备份端现在会复用恢复侧 StorageData 契约校验，避免生成恢复端拒绝的备份包。

  - 校验 `config`、`boards`、`currentBoardId`、`notes`、重复 note id 与图片文件引用字段。
  - 图片便签必须携带完整图片文件引用，并校验 hash、文件名、大小、MIME、创建时间和相对路径。
  - 同一路径被多张图片便签共享时，会逐条校验所有引用的 hash 与 size。

* **修复 WAL / 磁盘启动仲裁的数据回退风险**
  > 启动加载现在按候选来源逐个迁移与校验，避免坏 WAL 阻断有效磁盘数据，也避免旧磁盘覆盖更新的空 WAL。

  - 较新空 WAL 可保留“全部删除便签”的合法结果。
  - 较新空磁盘可防止旧 WAL 复活已删除便签。
  - DISK 与 WAL 时间戳相同时优先选择 WAL，坏 WAL 会被安全忽略并回退到可用来源。

* **修复废纸篓视图下备份/恢复持久化句柄不可用**
  > 持久化生命周期上移到应用级控制器，不再依赖画布视图是否挂载。

  - 废纸篓视图中也可以正常执行 zip 备份与恢复前刷新。
  - 应用启动后会用当前领域状态初始化持久化句柄，避免首次 `flushNow()` 空跑。

### 🚀 优化 (Optimizations)

* **强化 zip 读写安全边界**
  > 本地 zip 备份与恢复补齐路径、大小、数量和 manifest 一致性边界。

  - 恢复时拒绝危险路径、超限 `manifest.json`、超限 `data.json`、超限图片文件和附件总量超限。
  - 备份写入使用唯一临时 zip 文件，成功后再 rename 到目标路径，失败路径会自动清理临时文件。
  - 图片文件 hash、manifest SHA-256、文件名 stem 与真实内容必须一致。

### 🔧 内部改进 (Internal)

* **补强本地备份恢复测试覆盖**
  > 新增覆盖 zip 备份、zip 恢复、StorageData 契约、图片文件引用、WAL 仲裁和临时文件清理的回归测试。

  - 覆盖缺字段、重复 id、坏 WAL、空快照、同时间戳仲裁和坏来源回退。
  - 覆盖超限 zip、同路径多引用、半成品 zip 清理和废纸篓视图备份/恢复入口。

## [v1.4.8] - 2026-06-06

本版本在 v1.4.7 图片文件存储底座之上，补齐图片粘贴、图片拖入、独立图片便签、本地预览和图片文件一致性管理的闭环，让图片从“存得下”推进到“贴得进、看得到、清得掉”。

### ✨ 新特性 (Features)

* **Rust 原生剪贴板图片粘贴**
  > 画布现在支持 `Ctrl+V` 直接粘贴系统剪贴板中的图片，由 Rust 侧读取剪贴板位图并写入附件目录，前端无需处理二进制数据。

  - 单选一个未软删除便签时，图片会在旁边创建为独立图片便签。
  - 未选中或多选便签时，图片会在当前视口创建为独立图片便签。
  - 剪贴板无图片时保持普通文本智能粘贴，不产生空图片便签。

* **本地图片文件拖放**
  > 支持从操作系统文件管理器将图片文件拖入画布，自动创建独立图片便签。

  - 拖入文件走 Rust 侧图片文件写入链路，不在前端读取图片二进制。
  - PNG、JPEG、GIF、WebP 会进入图片入口；SVG 和非图片文件会被忽略。
  - 多图拖入会按层叠偏移创建多张独立便签，并写入一条 compound Undo/Redo 历史。

* **Asset Protocol 本地图片预览**
  > 图片便签通过 Tauri Asset Protocol + `convertFileSrc` 直接预览本地图片文件，不通过 IPC 把图片 bytes 读回前端。

  - `data.json` 仍只保存图片文件引用和相对路径，不保存绝对路径、asset URL 或二进制内容。
  - 看板图片便签、撕下便签只读窗口和旧浮层都能展示图片预览。
  - 图片文件缺失时显示占位提示，不影响其他便签渲染。

* **独立图片便签创建路径**
  > 图片粘贴与拖入都会创建独立图片便签，不再把图片挂到文本便签上。

  - 图片便签作为一等 Note 节点参与选择、移动、软删除、废纸篓还原和 Undo/Redo。
  - 图片文件仍由底层内容寻址存储管理，避免重复写入同一图片。
  - 旧备份中的文本便签图片引用会按当前规则迁移或剥离。

* **图片缺失占位**
  > 图片文件引用指向的文件不存在时，图片便签内显示缺失占位提示。

  - 占位提示不修改领域状态。
  - 其他文本便签与图片便签不受单个缺失图片影响。

* **图片文件一致性扫描与孤儿清理**
  > 新增图片文件一致性检查入口，可扫描全部图片文件引用并检测孤儿图片，支持一键清理。

  - 孤儿清理会删除未被任何便签引用的图片文件。
  - 清理操作会清空 Undo/Redo 历史，避免历史引用指向已删除图片文件。
  - 废纸篓中的便签仍计入存活引用，避免误删共享图片文件。

### 🐛 问题修复 (Bug Fixes)

* **后端图片文件命令与 Asset Protocol 作用域**
  > Rust 后端补齐剪贴板图片保存、图片文件路径解析、图片文件目录列举和安全删除命令，并启用 Asset Protocol 读取图片文件目录。

  - Asset Protocol scope 被限制在 `$DOCUMENT/SoNotes/attachments/**/*`。
  - 前端通过后端路径解析命令获取受校验的运行时路径，再交给 `convertFileSrc`。

* **修复图片便签重启后预览恢复**
  > 启动加载后会预水合图片资源地址，避免已持久化的图片便签在重启后直接显示“图片不可用”。

  - 预水合已接入真实启动初始化路径，而不再只依赖运行期缓存。
  - 保持 `ImageNoteBody` 纯同步渲染，不回退到组件内异步加载。

* **补齐图片便签导入与交互边界**
  > 修正图片便签导入、预览、拖入提示、复制反馈与撕下窗口相关边界，使图片便签链路更完整稳定。

  - 旧备份中的文本便签图片引用会按当前规则迁移、剥离或跳过异常条目。
  - 图片便签主图预览、大图预览、撕下窗口同步与复制图片反馈均已补齐测试覆盖。

### 🚀 优化 (Optimizations)

* **优化图片资源地址缓存命中路径**
  > 图片文件路径解析与 `asset URL` 转换会缓存复用，减少重复解析与预览抖动。

  - 创建图片便签时会预热资源地址缓存。
  - 启动加载后会批量预水合已持久化图片便签的资源地址。
  - `ImageNoteBody` 首帧直接同步命中缓存，避免闪烁。

### 🔧 内部改进 (Internal)

* **补强图片便签入口测试覆盖**
  > 新增覆盖剪贴板图片粘贴、图片拖放、图片预览、缺失占位、孤儿扫描和孤儿清理的测试。

  - 覆盖图片文件写入、预览和只读边界。
  - 覆盖一致性清理后 Undo/Redo 历史被正确清空。
  - 覆盖 Trash 中便签引用不会被误判为孤儿图片。

### 🧹 清理 (Cleanup)

* **移除文本便签附件遗留动作**
  > Store 不再暴露“向文本便签添加/移除附件”的领域动作，避免后续实现再次回到旧附件模型。

* **统一图片便签领域语言与发布口径**
  > `CONTEXT.md`、README、路线图与 v1.4.8 历史计划已统一为“图片便签 / 图片文件 / 图片文件引用”语义，降低后续开发歧义。

## [v1.4.7] - 2026-06-05

本版本建立附件 / 图片能力的本地优先存储底座，并补强启动仲裁、自动保存、附件写入和持久化批处理的安全边界，重点避免空数据覆盖、路径穿越、阻塞式文件 I/O 和并发写入造成的数据风险。

### ✨ 新特性 (Features)

* **新增附件引用模型**
  > Note 可保存 `AttachmentRef` 元数据，`data.json` 不写入图片或附件二进制内容。

  - 附件引用包含 hash、文件名、MIME、大小、相对路径和创建时间。
  - 旧版数据会迁移到 schemaVersion 2，并安全补齐附件字段。
  - 畸形附件引用会在加载、导入和导出边界被过滤，hash 与相对路径会被严格校验。

* **新增本地附件文件基础操作**
  > Tauri 后端新增附件写入、存在性检查和元数据读取命令。

  - 附件写入使用流式 SHA-256，不把完整文件一次性读入内存。
  - 文件保存到 `attachments/<sha256>.<ext>`，相同内容可复用已有文件。
  - 路径解析经过规范化与目录边界校验，避免路径穿越。

### 🐛 问题修复 (Bug Fixes)

* **阻止空数据覆盖本地存储**
  > 启动仲裁和自动保存路径会拦截异常空便签数据，避免较新的空磁盘快照覆盖较旧但有效的 WAL 数据。

  - 当磁盘数据为空而 WAL 中仍有便签时，启动会优先恢复非空 WAL。
  - 自动保存、窗口关闭和可见性切换触发的持久化会拒绝写入空便签集合。
  - 旧 store 初始化路径也补充同样的事故回归保护。

* **修复持久化批处理丢写风险**
  > Rust 写入队列不再把不同文件路径的连续保存请求错误折叠为最后一次写入。

  - 连续同路径保存仍合并到最新内容，降低重复写入。
  - 不同路径保存会分别写入并分别返回确认结果。
  - 临时文件和锁文件在成功或失败路径都会清理。

* **修复附件并发写入风险**
  > 同 hash 附件最终落盘会在进程内串行化，避免并发 rename 语义差异造成结果不一致。

  - 临时文件名使用随机值和独占创建，避免时间戳碰撞。
  - 写入失败会自动清理临时文件。
  - 磁盘不再留下 `.lock-*` 物理锁文件。

### 🚀 优化 (Optimizations)

* **隔离重度文件 I/O**
  > 附件命令与持久化写入中的同步文件操作移入阻塞线程，避免占用 Tokio async worker。

  - 附件写入、附件存在性检查和附件元数据读取均隔离同步文件系统访问。
  - `data.json` 写入、文件锁、落盘同步和 rename 也进入阻塞线程。
  - 大文件附件写入仍保持分块处理，降低内存峰值。

### 🔧 内部改进 (Internal)

* **新增附件一致性检测基础**
  > 可从完整 Note 集合收集存活附件引用，并检测缺失引用与孤儿附件。

  - 废纸篓中的 Note 仍计入存活引用，避免误判共享附件。
  - 当前只做检测，不在永久删除时直接删除附件文件。

* **预留附件导入导出结构**
  > JSON 导入导出会保留合法附件引用，并剥离多余字段和二进制残留。

  - 全量备份、单看板导出和选中便签导出都能携带附件引用。
  - 本版本不实现 zip 打包、图片入口、图片预览或 WebDAV 备份。

* **补强数据安全回归测试**
  > 新增覆盖空数据覆盖防护、附件引用清洗、附件一致性检测、附件写入并发和持久化批处理的测试。

  - 覆盖较新空磁盘数据不覆盖较旧非空 WAL 的事故场景。
  - 覆盖附件重复写入、临时文件清理和内存 hash 锁行为。
  - 覆盖不同路径保存不会被批处理错误吞掉。

## [v1.4.6] - 2026-06-04

本版本将“撕下便签”从主窗口内浮层升级为 Tauri 独立悬浮窗口，补齐独立窗口创建、只读快照同步、定位、置顶、贴回、主题同步、窗口恢复、自然尺寸和滚动体验，并修复折叠便签拖拽热区等 v1.4.5 回归问题。

### ✨ 新特性 (Features)

* **撕下便签升级为独立悬浮窗口**
  > 撕下便签现在会以 Tauri 独立窗口显示，不再占用主窗口画布空间。

  - 每个 Note 使用稳定的 `detached-note-{noteId}` 窗口标识。
  - 同一 Note 重复撕下会聚焦已有窗口，不创建重复镜像。
  - 撕下窗口仍是只读镜像，不复制 Note，不写入 Domain state。

* **新增独立窗口操作能力**
  > 撕下窗口支持定位原便签、OS 级置顶和贴回画布。

  - “定位到画布所在”会唤醒主窗口并定位原便签。
  - “置顶”升级为操作系统级 always-on-top。
  - “贴回画布”关闭独立窗口，不修改原便签。

* **新增托盘打捞悬浮便签入口**
  > 托盘菜单新增“显示所有悬浮便签”，可恢复被 Win+D 或系统操作收起的撕下窗口。

### 🐛 问题修复 (Bug Fixes)

* **修复撕下窗口任务栏污染与白屏闪烁**
  > 撕下窗口不会出现在任务栏，也不会先显示空白 WebView。

* **修复撕下便签重复显示**
  > 撕下后不再同时出现主窗口旧浮层和独立窗口。

* **修复主窗口恢复与定位反馈**
  > 主窗口被 Win+D 或托盘隐藏后，定位按钮和托盘 / 快捷键恢复路径会正确唤醒窗口。

* **修复撕下窗口主题不同步**
  > 深色、浅色和跟随系统的主题变化会同步到已打开的撕下窗口。

* **修复折叠便签拖拽热区**
  > v1.4.5 抽取展示组件后导致折叠便签只有底部一小段可拖的问题已修复。

* **修复撕下窗口尺寸与滚动抖动**
  > 长正文进入内部滚动后不再因为窗口尺寸测量循环而抖动。

### 🚀 优化 (Optimizations)

* **撕下窗口更贴近便签本体**
  > 独立窗口恢复圆角、拖拽手感和便签自然尺寸，减少“外壳感”。

* **优化首帧显示速度**
  > 撕下窗口挂载完成后通过 READY 握手立即获取当前快照，减少等待 100ms 节流带来的延迟。

* **优化滚动条显示**
  > 长正文滚动条默认收敛，鼠标移入或聚焦时再显现，避免常驻滚动条干扰便签观感。

* **优化撕下窗口出现位置**
  > 新窗口优先靠近原便签位置出现，并夹在当前显示器范围内。

### 🔧 内部改进 (Internal)

* **新增独立撕下窗口入口**
  > 新增 `detached.html` 与独立 React 入口，避免加载主窗口 App、画布、持久化监听和托盘逻辑。

* **新增定向快照与生命周期事件**
  > 通过 typed events 同步快照、主题、READY、missing、closed 等跨窗口事件。

* **补强撕下窗口测试覆盖**
  > 覆盖窗口入口、按钮行为、快照同步、READY 首帧、主题同步、定位唤醒、尺寸测量和折叠拖拽回归。

## [v1.4.5] - 2026-06-02

本版本聚焦“撕下便签”主窗口内浮层原型，新增只读镜像视图、浮层拖拽、定位、置顶、贴回画布和实时同步能力，并修复浮层层级、事件隔离、无标题遮挡与拖拽监听器清理问题。

### ✨ 新特性 (Features)

* **新增撕下便签浮层**
  > 用户现在可以从画布便签右键菜单中将便签撕下为主窗口内的只读浮层镜像，便于临时悬浮查看。

  - 同一便签不会重复打开多个撕下视图，重复触发会聚焦已有浮层。
  - 撕下视图不创建第二份便签数据，不写入持久化数据。
  - 撕下视图只读，编辑仍回到画布中的原始便签完成。

* **新增浮层操作入口**
  > 撕下便签支持定位到画布所在、置顶或取消置顶、贴回画布三个操作。

  - “定位到画布所在”会切回对应看板，定位、选中并高亮原便签。
  - “置顶”仅影响主窗口内浮层层级，不涉及操作系统级窗口置顶。
  - “贴回画布”只关闭只读镜像，不修改原便签。

* **撕下视图同步原便签状态**
  > 原便签的标题、正文、颜色和折叠状态变化后，撕下视图会同步更新；便签进入废纸篓后，撕下视图会自动关闭。

### 🐛 问题修复 (Bug Fixes)

* **修复撕下便签浮层交互串扰**
  > 浮层事件会被隔离，不再误触发画布选择、拖拽、右键菜单或其他底层交互。

* **修复撕下便签堆叠顺序**
  > 多个撕下视图之间会按聚焦和置顶状态维护正确层级，避免后打开或被聚焦的浮层被压在下方。

* **修复无标题便签控制区遮挡正文**
  > 内嵌控制按钮收敛到便签表面标题区域，避免无标题便签的正文被右上角按钮遮挡。

* **修复拖拽监听器内存泄漏**
  > 拖拽过程中的全局鼠标监听器改为组件生命周期托管，撕下视图在拖拽中被关闭或软删除时也会自动清理监听器。

### 🚀 优化 (Optimizations)

* **控制按钮内嵌到便签表面**
  > 撕下便签的控制区从独立外置工具条改为内嵌式按钮层，视觉上更接近原版便签本体。

* **抽取便签纯展示组件**
  > 画布便签与撕下视图共享同一套只读视觉渲染能力，减少便签材质、颜色、折叠态和正文展示逻辑分叉。

### 🔧 内部改进 (Internal)

* **新增撕下便签运行态 UI state**
  > 新增撕下视图的运行态记录和操作入口，管理位置、置顶状态、聚焦顺序与关闭行为。

* **补强撕下便签测试覆盖**
  > 新增浮层渲染、入口操作、状态同步、定位、置顶、事件隔离、拖拽、自动关闭和监听器清理相关测试。

## [v1.4.4] - 2026-06-01

本版本聚焦 Undo/Redo 批量与复杂操作扩展，将撤销重做能力从单便签操作推进到软删除、废纸篓还原、批量操作、归拢、拆分、合并，并将便签尺寸调整收敛为仅在编辑状态生效的编辑尺寸偏好，避免污染看板布局、碰撞检测和 MiniMap。

### ⚠️ 重大变更 (Breaking Changes)

* **拆分与合并改为替换式领域操作**
  > 拆分会用拆分后的便签替换原便签，合并会用合并便签替换原便签组；撤销与重做会恢复同一组便签 ID，保证历史重放稳定。

  - 拆分 / 合并不再保留原便签与结果便签同时存在的旧行为。
  - 该变更让拆分、合并与 Undo/Redo 的领域语义保持一致。

* **移除旧归拢撤销入口**
  > 归拢、拆分、合并不再显示独立 toast 撤销按钮，统一通过通用 Undo/Redo 快捷键回滚或重做。

### ✨ 新特性 (Features)

* **批量操作支持撤销与重做**
  > 多选便签后的批量移动、批量改色和批量软删除现在会作为一次用户意图整体入栈，一次撤销即可完整回滚。

  - 批量操作会跳过无效或未变化的便签。
  - Undo/Redo 不拆成逐张便签的中间历史，保持操作粒度清晰。

* **软删除与废纸篓还原支持撤销与重做**
  > 从看板移入废纸篓、从废纸篓还原便签均纳入通用历史，降低误操作成本。

  - 软删除撤销会将便签恢复到原看板关系。
  - 废纸篓还原撤销会将便签重新放回废纸篓。
  - 永久删除仍不提供撤销承诺。

* **归拢、拆分、合并接入通用 Undo/Redo**
  > 组织类操作现在与普通领域操作共用同一套历史栈，支持连续撤销与重做。

  - 归拢会记录参与便签前后位置。
  - 拆分与合并会稳定恢复原始 UUID，不生成内容等价但 ID 不同的新便签。

* **新增编辑尺寸偏好**
  > 用户可以在选中或编辑便签时调整编辑尺寸偏好；普通看板布局仍保持默认便签尺寸。

  - 编辑尺寸偏好仅在选中或编辑状态显示。
  - 该偏好不影响群组拖拽、碰撞检测、归拢和 MiniMap。
  - 调整编辑尺寸可通过通用 Undo/Redo 回滚。

### 🐛 问题修复 (Bug Fixes)

* **修复失败 Patch 消费历史栈**
  > 当 Undo/Redo 的 Patch 因目标缺失或状态不满足预期而无法应用时，不再错误弹出历史记录，避免用户失去下一次恢复机会。

* **修复编辑尺寸污染拖拽边界**
  > 边缘推挤与拖拽边界现在使用逻辑默认尺寸，不再读取选中态 DOM 尺寸，拖拽手感更稳定。

* **修复编辑尺寸切换闪现**
  > 选中或退出选中时会平滑过渡编辑尺寸；拖拽期间会禁用尺寸过渡，避免动画与拖拽 transform 冲突。

### 🔧 内部改进 (Internal)

* **新增复合 DomainPatch 能力**
  > `compound-patch` 可用有序子 Patch 表达批量与复杂操作，并在应用失败时保持领域状态不变。

  - `UpdateFieldsPatch` 支持 `deletedAt`、`editingWidth` 和 `editingHeight`。
  - 复合 Patch 按顺序在一次原子计算中应用，避免部分成功导致状态不一致。

* **清理旧局部撤销机制**
  > 移除 `arrangeUndoToast` 与 `undoLastArrange` 相关运行态、控制器入口、UI store 字段和旧测试，减少双重撤销风险。

* **补充 v1.4.4 实施规划与测试覆盖**
  > 新增 v1.4.4 批量与复杂操作规划文档，并补强领域 Patch、运行态历史、批量操作、组织操作和编辑尺寸偏好的测试覆盖。

## [v1.4.3] - 2026-05-31

本版本聚焦 Undo/Redo 基础能力，新增便签新建、编辑、移动、改色、折叠/展开的撤销与重做支持，并补齐快捷键、跨看板重放可见性与输入框原生撤销保护。

### ✨ 新特性 (Features)

* **新增基础撤销与重做能力**
  > 看板视图中现在可以撤销或重做常见单便签操作，减少误操作后的恢复成本。

  - 支持新建便签、新建带内容便签、编辑标题、编辑正文、移动便签、改变颜色、折叠/展开。
  - 撤销新建便签会将该便签从看板中完整移除，不进入废纸篓。
  - 重做新建便签会恢复同一个便签 ID 与原始看板关系。

* **新增 Undo/Redo 快捷键**
  > 看板视图支持通过键盘快速撤销与重做领域操作。

  - 撤销：`Ctrl + Z` / `Cmd + Z`。
  - 重做：`Ctrl + Y` / `Cmd + Y`，或 `Ctrl + Shift + Z` / `Cmd + Shift + Z`。
  - 输入框聚焦时保留系统原生文本撤销与重做，不劫持逐字编辑体验。
  - 废纸篓视图、Spotlight 打开时、快速捕获打开时不会触发画布 Undo/Redo。

### 🐛 问题修复 (Bug Fixes)

* **修复跨看板重放后便签不可见**
  > 当撤销或重做影响其他看板中的便签时，应用会自动切回对应看板并定位到目标便签，避免数据已恢复但用户看不见。

* **修复同看板撤销重做导致视口跳动**
  > 对当前可见便签执行撤销或重做时，不再强制重置视口位置，只保持目标便签选中。

* **修复定位折叠便签污染撤销历史**
  > 通过 Spotlight 或定位入口展开折叠便签时，不再写入 Undo/Redo 历史，避免下一次撤销误撤销辅助展开动作。

* **修复撤销重做快捷键测试触发不稳定**
  > 快捷键测试现在按 `react-hotkeys-hook` 的实际匹配方式派发键盘事件，避免 CI 中出现测试未真正触发快捷键处理器的问题。

### 🔧 内部改进 (Internal)

* **新增 Undo/Redo 历史模型**
  > 新增纯函数历史栈模型，为后续扩展更多领域操作提供基础。

  - 默认历史容量为 100 条。
  - 新操作入栈会清空 redo 栈。
  - 历史不持久化，避免跨启动恢复带来额外语义风险。

* **新增领域状态 Patch 应用层**
  > 新增单便签领域 Patch 应用能力，统一维护 normalized 状态与 `layoutNotesById` 的一致性。

  - 支持新增、移除、字段更新、位置更新。
  - 保持运行态写入目标为 legacy `useStore`，避免过渡期引入双向 store 同步风险。

* **补强 Undo/Redo 测试覆盖**
  > 新增历史模型、领域 Patch、运行态撤销重做、快捷键边界、跨看板重放和定位历史污染测试。

## [v1.4.2] - 2026-05-31

本版本聚焦托盘入口与快速捕获体验，修复快速捕获层与窗口圆角不对齐的问题，增强托盘 tooltip 的状态提示，并补齐快速捕获打开、输入、关闭与焦点循环的关键交互细节。

### ✨ 新特性 (Features)

* **托盘 tooltip 展示当前看板与保存异常状态**
  > 托盘图标悬浮提示现在会显示当前看板名称；当数据处于保存中或保存失败状态时，会追加对应状态提示，便于不展开窗口时快速感知应用状态。

  - 正常状态下 tooltip 保持简洁，不再显示“已保存”。
  - 保存失败时会显示截断后的错误信息，避免提示过长。

### 🐛 问题修复 (Bug Fixes)

* **修复快速捕获层溢出窗口圆角**
  > `Ctrl+Alt+N` 打开的快速捕获层现在纳入窗口壳层裁剪域，遮罩不再突破窗口圆角边界。

* **完善快速捕获键盘交互**
  > 快速捕获输入框聚焦时，普通 `Escape` 可关闭浮层；输入法组字过程中按 `Escape` 不会误关闭，降低中文输入中断风险。

  - `Enter` 继续用于创建便签。
  - `Shift+Enter` 继续用于输入换行。
  - `Tab` 会在快速捕获对话框控件内循环，不再逃逸到浮层外。

* **优化快速捕获剪贴板预填**
  > 打开快速捕获时会读取当前剪贴板文本并预填到输入框，同时自动全选，用户可直接按 `Enter` 创建、按 `Delete` 清空，或直接输入覆盖。

* **降低托盘 tooltip 状态噪音**
  > tooltip 正常状态下只显示当前看板名称，仅在保存中或保存失败时追加保存状态。

### 🔧 内部改进 (Internal)

* **新增 v1.4.2 实施规划**
  > 在 `docs/plans/` 下新增 v1.4.2 托盘与快速捕获修复计划，并将后续本地优先捕获、备份与恢复路线收拢到独立规划文档。

## [v1.4.1] - 2026-05-30

本版本聚焦菜单与操作入口的视觉统一、颜色配置收敛和文案规范化，消除右键菜单、多选操作栏和废纸篓确认弹窗中残留的英文注释、半角标点和开发者风格文案。

### ✨ 新特性 (Features)

* **便签 UI 色板收敛**
  > 从持久化 13 色中提取 6 色 UI 调色板 `NOTE_UI_COLORS`，右键菜单、多选条和快捷切换颜色统一使用同一套色板，降低颜色选择的视觉干扰。

* **右键菜单操作入口收敛**
  > 颜色从扁平行内色块改为悬停展开的二级菜单；emoji 图标全部替换为 `lucide-react`；去掉英文开发者注释，保留中文菜单文本。

* **多选操作栏改色入口收起**
  > 多选条不再常驻 6 个色点，改为点击"改色"按钮弹出气泡色板，工具栏视觉更紧凑。

### 🐛 问题修复 (Bug Fixes)

* **统一删除确认文案**
  > 废纸篓、看板和便签中的确认弹窗从半角问号改为全角问号，软删除与永久删除使用统一措辞。

* **清理界面英文残留**
  > `Unknown Board` 改为 `未知看板`；`取消钉住 (Unpin)` 去掉英文括注；`还原笔记` 改为 `还原便签`；默认看板名从 `主板 (Main)` 改为 `主板`。

* **统一中文省略号**
  > 界面中的半角三点 `...` 全部替换为全角省略号 `…`。

### 🔧 内部改进 (Internal)

* **新增 v1.4.1 规划文档**
  > 在 `docs/plans/` 下新增 `v1.4.1-menu-entry-consolidation.md`，记录菜单与操作入口整合的完整设计决策与实施步骤。

---

## [v1.4.0] - 2026-05-29

本版本聚焦技术债治理与发布前稳定性，完成状态边界、持久化服务、导入导出服务与 Canvas 交互生命周期的阶段性解耦，并修复发布前手测发现的关键交互回归。旧 `useStore` 仍作为过渡期运行时主路径保留，相关边界与后续退出条件已记录在 ADR 与实施计划中。

### 🐛 问题修复 (Bug Fixes)

* **修复 Edge Push 到画布原点时便签外飞**
  > 从右下方向左上边缘推动到 `(0,0)` 时，便签不再继续累积超出实际视口位移的补偿量，避免拖拽预览外飞并在松手时回弹。

  - Edge Push 补偿改用钳制后的实际视口位移。
  - 保留正常边缘推动与拖拽落位行为。

* **修复废纸篓返回看板后视口回到原点**
  > 从废纸篓返回看板时，会保留离开看板前的视口位置，不再因 Canvas 重挂载重新水合旧视口而回到 `(0,0)`。

  - 进入废纸篓前保存当前看板视口。
  - Canvas 重挂载时避免重复执行初始化水合。

* **改善小地图拖拽跟手性**
  > 拖拽小地图视口时临时禁用过渡动画，减少拖拽过程中因 CSS transition 带来的滞后感。

  - 当前仍保留轻量视觉过渡。
  - 小地图丝滑度仍可作为后续体验优化项继续打磨。

* **修复多选快捷操作条在窄窗口下溢出**
  > 多选快捷气泡在托盘窗口较窄时会按操作分组换行，避免最右侧“复制”按钮溢出窗口边界。

  - 基础操作、颜色操作与尾部操作分组排列。
  - 缩小按钮与色点尺寸以适配默认窗口宽度。

### 🚀 优化 (Optimizations)

* **收口快捷键、全局菜单与跨层操作入口**
  > 快捷键、Spotlight、看板切换、托盘事件、右键菜单等跨层意图统一进入 `appController`，减少组件直接穿透多个状态层。

  - 快捷键入口改为意图发射器。
  - Spotlight 跨看板定位由控制器统一编排。
  - 右键菜单写动作统一收口到控制器方法。

* **增强 Canvas 交互生命周期**
  > Canvas 内部的全局监听与 RAF 循环迁出到交互引擎和专用 hook，降低 Canvas 渲染组件的职责复杂度。

  - 新增每 Canvas 实例独立的 `CanvasEngine`。
  - 全局 `blur` 与 `visibilitychange` 会统一重置交互暂态。
  - sticky drag 落位不再从 DOM `style.transform` 反推状态。

* **提升持久化与导入导出可靠性**
  > 新增 StorageService 与 DataTransferService，收口 WAL、磁盘写入、导入导出与失败回滚路径，为后续彻底退出旧 store 铺路。

  - 持久化写入支持 WAL 节流、磁盘防抖与强制 flush。
  - 导入失败时按快照回滚，降低数据污染风险。
  - 旧路径仍保留为过渡期兼容方案。

### 🔧 内部改进 (Internal)

* **建立分层 store 过渡架构**
  > 新增 domainStore、viewportStore、uiStore 与桥接层，逐步区分领域状态、视口状态与界面状态。

  - `domainStore` 承接领域状态骨架与首批写入动作。
  - `viewportStore` 承接视口与交互状态。
  - `uiStore` 承接视图模式、选区、菜单等界面状态。
  - 旧 `useStore` 保留并通过桥接同步，避免一次性大迁移带来的回归风险。

* **记录 v1.4.0 实际落地差异**
  > ADR 与计划文档补充说明旧 `useStore`、桥接层和部分组件直连的保留原因，明确后续删除前提。

  - 本版本为阶段性解耦，不宣称彻底移除旧 store。
  - 后续应优先迁移 `appController` 内部写入来源、Canvas/NoteCard 拖拽链路与剩余单 store 写动作。

* **补强架构与交互测试覆盖**
  > 新增和扩展 StorageService、DataTransferService、domainStore、桥接层、appController、CanvasEngine、MiniMap、SelectionActionBar 等测试，覆盖关键迁移路径与发布前回归场景。

## [v1.3.9] - 2026-05-28

本版本聚焦废纸篓体验收口，新增废纸篓内搜索，修复回收站视图下的安全边界问题，并优化导航栏在回收站下的行为。

### ✨ 新特性 (Features)

* **新增废纸篓内搜索**
  > 废纸篓页面顶部新增搜索输入框，支持按标题和正文关键词过滤已删除便签，大小写不敏感，清空输入恢复全量列表。
  - 搜索仅在废纸篓视图内生效，不影响主画布的 Spotlight 全局搜索。

* **回收站视图安全收口**
  > 进入废纸篓后，画布级快捷键（全选、删除、复制、粘贴新建等）自动禁用，右键菜单不再弹出看板操作，托盘新建/剪贴板创建/快速捕获会先切回看板再执行。
  - 切换到废纸篓时自动清理选区、拆分气泡、拖拽、平移模式等交互残留，避免状态串扰。

### 🐛 问题修复 (Bug Fixes)

* **修复回收站下 Dock 强制渲染**
  > 废纸篓视图下 Dock 不再无视收起状态强制显示；进入废纸篓时 Dock 默认收起，用户仍可手动展开。

### 🚀 优化 (Optimizations)

* **废纸篓便签禁用右键菜单**
  > 废纸篓列表中的便签不再响应右键菜单，避免出现不适用的看板操作入口。

* **SmartPasteSplitBubble 回收站下隐藏**
  > 切换到废纸篓视图时，若拆分气泡因残留状态被挂载，会自动隐藏。

## [v1.3.8] - 2026-05-27

本版本聚焦轻量整理闭环，新增便签合并、拆分、多选操作条等整理能力，扩展轻撤销覆盖范围，并优化搜索体验与看板信息展示。

### ✨ 新特性 (Features)

* **新增便签合并**
  > 多选便签后可通过右键菜单或底部操作条"合并为一张"，按画布坐标排序拼接内容，合并后自动选中新便签并支持撤销。

* **新增便签按段拆分**
  > 右键单张便签可"按段拆分"（按空行分段），拆分后保留原便签并选中全部结果，复用智能粘贴偏移策略，支持撤销。

* **新增多选快捷操作条**
  > 画布底部居中显示操作条，提供合并、删除、改色、折叠、归拢、复制等批量操作入口，与右键菜单共享同一套 store action。

* **轻撤销覆盖合并与拆分**
  > 撤销 Toast 从仅支持归拢扩展为同时支持合并与拆分场景，合并撤销删除结果并恢复原始便签，拆分撤销删除结果并恢复原始便签。

### 🐛 问题修复 (Bug Fixes)

* **修复搜索索引更新残留**
  > 便签内容更新时先清除旧索引再写入新索引，避免旧分词残留影响搜索结果。

* **修复搜索分组重复**
  > 内容匹配分组排除当前看板项，同一便签不再同时出现在"当前看板"和"内容匹配"两个分组。

* **修复撤销快照割裂**
  > 合并/拆分撤销时完整恢复原始便签（内容、坐标、看板归属、选择集），不再只删除新建结果而丢失原始状态。

* **防护跨看板合并**
  > 合并操作校验选中便签必须属于同一看板，防止未来扩展场景下的跨看板污染。

### 🚀 优化 (Optimizations)

* **搜索范围收敛为两态**
  > Spotlight 搜索范围从三态循环简化为"全部看板 / 当前看板"两个明确按钮，移除语义不清的"已删除"入口。

* **看板 Dock 去掉常驻角标**
  > 移除看板按钮右上角常驻计数角标，保留 hover tooltip 中的便签数量信息，降低视觉焦虑。

### ⚡ 性能优化 (Performance)

* **优化操作条订阅粒度**
  > SelectionActionBar 改用 useMemo 缓存选中 ID 列表，减少画布操作时的不必要重渲染。

* **操作条焦点防护**
  > 操作条容器增加 pointerdown 拦截，避免点击按钮时触发画布焦点抢占。

## [v1.3.7] - 2026-05-26

本版本聚焦窗口管理可靠性与便签操作体验补强，修复多显示器定位、DPI 缩放边距、窗口瞬移等桌面级问题，新增临时高亮、归拢增强与统一新建落点。

### ✨ 新特性 (Features)

* **新增临时定位高亮**
  > Spotlight 跳转、编辑后失焦、新建便签时，目标便签会短暂高亮，降低"便签在哪里"的感知成本。

* **增强一键归拢策略**
  > 一键归拢新增"按更新时间"和"按颜色"两种排列方式，右键菜单可直接选择。

* **新增归拢撤销提示**
  > 执行一键归拢后，底部 Toast 提供 6 秒内一键撤销能力，误操作可快速回退。

* **统一非鼠标新建落点**
  > 托盘入口、快速捕获、剪贴板创建统一使用视口中心偏上落点，连续创建时轻微错位避免重叠。

### 🐛 问题修复 (Bug Fixes)

* **修复编辑便签时粘贴误创建新便签**
  > 编辑便签、Spotlight 搜索等输入框内的粘贴不再冒泡到画布触发 Smart Paste。

* **修复窗口强制归位**
  > 快速捕获、托盘新建等操作不再无条件移动窗口位置，已可见的窗口只提升层级和捕获焦点。

* **修复多显示器重置窗口位置错误**
  > 重置窗口时正确计算当前显示器的全局坐标偏移，窗口不再飞回主屏幕。

* **修复高 DPI 屏幕边距不一致**
  > 窗口定位统一使用物理像素换算，4K 等高 DPI 屏幕下边距与 1080P 保持一致。

* **修复失焦隐藏在拉伸窗口时误触**
  > 窗口失焦后增加 150ms 防抖，拉伸窗口过程中短暂失焦不再触发隐藏。

* **修复归拢包含已删除便签**
  > 一键归拢不再拉出软删除便签，"空看板"归拢不再产生不可见的便签重排。

* **修复归拢作用域与高亮清理**
  > 删除便签时同步清理临时高亮和新建反馈，归拢 scope 正确区分选中项与当前看板。

* **修复新建落点连续创建重叠**
  > 落点循环步数从 6 增加到 15，连续创建更多便签后才重叠。

### 🚀 优化 (Optimizations)

* **失焦隐藏下沉 Rust 层**
  > 窗口失焦隐藏逻辑从前端迁移到 Rust 层，利用 tokio 异步任务实现防抖，前端代码保持简洁。

### 🧹 清理 (Cleanup)

* **移除"显示全部便签"功能**
  > 该功能在便签分散时视口内无便签，设计存在根本矛盾，已移除。

## [v1.3.6] - 2026-05-24

本版本聚焦轻量桌面效率闭环，在不引入重依赖的前提下补强快速捕获、托盘入口、纯文本智能粘贴、搜索跳转与快捷键稳定性。

### ✨ 新特性 (Features)

* **新增 Quick Capture Lite 快速捕获**
  > 通过 `Ctrl + Alt + N` 或托盘菜单快速打开输入浮层，输入后按 Enter 即可在当前看板创建便签，Shift+Enter 保留换行。

  - 快速捕获复用现有便签创建与智能粘贴路径，不新增独立窗口。
  - 多行内容默认保留在同一张便签中，避免未经确认拆散记录。
  - Esc、点击遮罩或输入完成后可快速关闭捕获浮层。
  - 快速捕获打开期间会屏蔽画布级快捷键，避免输入时误触全选、删除、重置视口或 Spotlight。

* **新增托盘快速动作**
  > 托盘菜单从显隐入口升级为桌面效率入口，支持快速捕获、从剪贴板创建便签和新建空白便签。

  - 保留既有钉住窗口、重置窗口和退出能力。
  - 移除语义重复的“回到上次位置”入口。
  - `Ctrl + Alt + S` 与托盘左键点击统一为“窗口可见即隐藏，不可见才显示”。
  - 全局快捷键注册失败时会在应用内显示提示，便于识别快捷键占用或系统权限问题。

* **新增 Smart Paste Lite 拆分气泡**
  > 画布粘贴、右键“粘贴并新建”和快捷键粘贴默认先创建一张便签，再在便签旁提供轻量拆分气泡。

  - 单段文本创建一张便签。
  - 多行或多段文本默认保留为一张便签，避免自动拆解用户内容。
  - 用户可在便签旁的拆分气泡中选择按行拆分或按段拆分。
  - URL 仅作为普通链接文本创建，不抓取网页信息、不缓存图片、不引入网络解析依赖。

* **新增新建便签反馈高亮**
  > 新建、快速捕获和粘贴创建的便签会短暂高亮并自动选中，降低“便签生成在哪里”的感知成本。

### 🐛 问题修复 (Bug Fixes)

* **修复 Spotlight 跨看板搜索结果定位不稳定**
  > 搜索结果跨板跳转不再依赖即将卸载的 Spotlight 本地状态，而是在切板后直接使用 Store 状态确认目标便签并完成定位、选中和置顶。

* **修复 Spotlight 关闭后的搜索状态残留**
  > 关闭 Spotlight 时会清理搜索状态，减少后台搜索结果回流造成的时序干扰。

* **修复快速捕获输入事件串扰**
  > 快速捕获输入期间会阻断 Escape 和画布级快捷键穿透，避免输入法取消、删除键或全选快捷键误作用到底层画布。

* **修复托盘窗口切换语义**
  > 托盘左键和 `Ctrl + Alt + S` 不再要求窗口已聚焦才隐藏，窗口只要可见就会隐藏。

* **增加全局快捷键失败提示**
  > 当系统快捷键注册失败时，前端会展示可见提示，避免用户误以为快速捕获或显隐快捷键失效。

### 🚀 优化 (Optimizations)

* **增强 Board Resume 看板位置恢复**
  > 切换看板时会保存旧看板视口并恢复目标看板的上次位置，同时将视口记忆落盘，降低多看板切换时的迷失感。

### 🔧 内部改进 (Internal)

* **补强快速捕获与智能粘贴测试覆盖**
  > 新增 Smart Paste 解析与拆分测试、画布粘贴测试、Store 批量创建与拆分测试、Spotlight 跨板定位测试，并扩展 Tauri 事件监听与全局快捷键失败提示契约测试。

* **补充全局快捷键与托盘权限配置**
  > 为快速捕获、窗口显隐和托盘快速动作补齐 Tauri 权限与前后端事件链路。

## [v1.3.5] - 2026-05-24

本版本聚焦 v1.3.x 以来的画布交互回归与深色便签视觉稳定性，集中修复边缘推动、拖拽中断、虚拟化裁剪、sticky 拖拽、小地图清理和深色便签毛玻璃伪影问题，并补强相关测试覆盖与发布链路稳定性。

### 🐛 问题修复 (Bug Fixes)

* **修复边缘推动拖拽回归**
  > 边缘推动拖拽重新以原始指针坐标和统一拖拽会话驱动，避免画布推动、便签位置和 DOM 预览之间出现偏移。

  - 修复边缘拖拽推动画布回归。
  - 修复边缘推动期间便签不跟随的问题。
  - 拖拽 Leader 改为由 DOM 接管，减少 React 高频更新带来的状态竞争。
  - 收敛边缘推动拖拽状态链路，停止拖拽时统一提交最终位置。

* **修复 Space 拖动画布方向反向**
  > 使用 Space 拖动画布时，视口移动方向重新与用户拖动方向保持一致。

* **修复拖动画布时视口状态不同步**
  > 拖动画布过程中实时同步视口状态，降低 MiniMap、边缘推动和画布渲染之间的状态延迟。

* **修复框选误选与边缘推动冲突**
  > 框选与边缘推动同时参与交互时，便签命中和跟随行为更加稳定。

* **修复拖拽期间虚拟化裁剪便签**
  > 普通拖拽、组选拖拽和边缘推动拖拽期间会保留必要便签节点，避免便签在拖拽过程中被虚拟化卸载。

  - 拖拽会话期间禁用视口虚拟化裁剪。
  - 防止拖拽中的便签因为离开视口缓冲区而丢失 DOM 预览。
  - 降低拖拽中断、位置未提交和视觉残留风险。

* **修复 sticky 拖拽与小地图清理语义**
  > sticky 拖拽、小地图和普通便签拖拽的清理边界进一步对齐，减少拖拽结束或中断后的状态残留。

  - sticky 拖拽贴边语义与普通拖拽保持一致。
  - 小地图相关拖拽清理逻辑更加明确。
  - 拖拽暂停、恢复和提交路径更稳定。

* **修复普通便签拖拽异常中断状态残留**
  > 普通便签拖拽现在在窗口失焦、切换看板、组件卸载和正常松手时走统一收口逻辑，避免 `body.is-dragging`、DOM 临时 transform 或 Store 坐标残留不同步。

  - 新增活动普通便签拖拽收口注册机制。
  - `window blur` 会先触发普通便签拖拽收口，再执行画布级全局清理。
  - 切换看板前会先提交或清理活动普通便签拖拽。
  - 卸载拖拽中的便签时会统一收口，而不是直接丢弃拖拽会话。

* **修复深色便签不规则块状伪影**
  > 深色便签不再使用真实 `backdrop-filter` 采样背后画布内容，避免 Chromium / Tauri 环境下出现不规则透底、块状光斑或局部遮罩错觉。

  - 移除便签卡片主层真实 `backdrop-blur` / `backdrop-saturate` 依赖。
  - 保留玻璃感视觉语义，但不再采样背后像素。
  - 避免邻近卡片、网格、dock、tooltip 或背景图层被采样进便签表面。

### 🚀 优化 (Optimizations)

* **迁移为有色亚克力便签视觉**
  > 深色便签改为“有色实体底 + 同色径向柔光 + 色相边框 + 内高光 + 轻微 accent glow”的稳定亚克力视觉语言，在避免真毛玻璃伪影的同时保留材质层次。

  - 保留浅色便签颜色作为持久化单一事实来源。
  - 深色模式新增 `DarkSpectrum`，将背景、边框和强调色解耦。
  - hover、selected、dragging 等状态通过 React 状态与 inline 材质参数驱动。
  - 不恢复卡片级 CSS `hover:shadow-*` 或 `dark:hover:bg-*`。

* **统一便签可视高度语义**
  > 便签在普通画布、小地图、sticky 拖拽和边缘推动中的可视尺寸计算更加一致。

* **收敛拖拽坐标与布局提交链路**
  > 拖拽过程使用原始指针坐标和拖拽会话记录位置，结束后集中提交到 Store，减少多套坐标来源之间的偏差。

### ⚡ 性能优化 (Performance)

* **降低拖拽过程 React 高频更新**
  > 拖拽预览继续通过 DOM transform 驱动，最终位置在拖拽结束或异常收口时统一提交，提升多便签拖拽与边缘推动场景的流畅度。

* **优化 CI 测试运行稳定性**
  > 调整 Spotlight 测试运行策略，降低 CI 内存压力，保持主构建链路稳定。

### 🔧 内部改进 (Internal)

* **补强拖拽与边缘推动测试覆盖**
  > 新增和扩展 Canvas、MiniMap、useEdgePush、edgePushDragCompensation、activeNoteDrag 等测试，覆盖拖拽中断、边缘推动、虚拟化裁剪、切板收口和窗口失焦路径。

* **同步发布说明与路线图文档**
  > 统一发布说明写作风格，归档历史发布计划，并更新路线图与发布流程说明。

### 🧹 清理 (Cleanup)

* **归档历史发布计划**
  > 将旧版本发布计划移动到归档目录，减少根目录规划文件噪音。

## [v1.3.4] - 2026-04-25

本版本聚焦搜索性能与测试稳定性，将 Spotlight 搜索索引下沉到后台线程，并修复 CI 环境下的测试内存溢出问题。

### ✨ 新特性 (Features)

* **搜索结果支持分组与筛选**
  > Spotlight 搜索现在可以按结果类型组织结果，并支持在当前看板与全部看板之间切换搜索范围。

  - 支持按看板、标题匹配、正文匹配分组展示。
  - 支持当前看板 / 全部看板筛选。
  - 默认排除已删除便签，避免废纸篓内容干扰正常搜索。

### ⚡ 性能优化 (Performance)

* **搜索索引迁移到后台线程**
  > 搜索索引构建、匹配与高亮计算迁移到 Web Worker，减少大量便签场景下的主线程阻塞。

  - 面向 1000+ 便签场景优化搜索输入响应。
  - 降低 Spotlight 打开与输入搜索词时对画布交互的影响。
  - 保持现有数据结构兼容，无需用户迁移数据。

### 🔧 内部改进 (Internal)

* **修复 CI 测试内存溢出**
  > 调整 Vitest 运行策略，避免 Spotlight 测试在 CI 环境中触发内存上限。

  - 测试池改用更适合当前用例隔离方式的运行配置。
  - 限定 `vi.useFakeTimers()` 拦截范围，避免劫持 `performance` 与 `queueMicrotask`。
  - 移除 Vitest 4 已废弃的 `poolOptions` 配置。
  - 用户影响：无功能变化，仅提升构建与发布链路稳定性。

## [v1.3.3] - 2026-04-24

本版本聚焦批量操作、框选稳定性、废纸篓管理与拖拽性能，补齐多选场景下的高频管理能力。

### ✨ 新特性 (Features)

* **框选碰撞检测更准确**
  > 框选逻辑从 DOM 读取重构为基于布局数据的 AABB 碰撞检测，提升大画布与视口外便签的选择稳定性。

  - 支持选中视口外但与框选区域相交的便签。
  - 避免 `getBoundingClientRect()` 造成的布局抖动。
  - Shift+框选会追加到现有选区，而不是覆盖已有选择。

* **批量管理操作增强**
  > 多选便签后可以批量折叠 / 展开、置顶 / 置底，减少整理大量便签时的重复操作。

  - 批量置顶会保留选中便签组内部的相对层级顺序。
  - 批量置底会整体移动到当前画布层级下方。
  - 折叠 / 展开会根据当前选区状态自动选择更符合预期的方向。

* **选中便签支持精准导出**
  > 右键菜单支持将当前便签或选中便签导出为 JSON，复用现有导出链路并保持与全量导入兼容。

  - 单选时可直接导出当前便签。
  - 多选时导出完整选区。
  - 导出数据可通过现有导入能力恢复。

* **废纸篓支持批量操作**
  > 废纸篓视图新增多选能力，支持批量还原和批量永久删除。

  - 支持 Ctrl / Shift 点击进行多选。
  - 批量还原会尽量回到原看板；原看板缺失时回退到当前看板。
  - 批量永久删除带二次确认，降低误删风险。

### 🐛 问题修复 (Bug Fixes)

* **修复框选追加逻辑**
  > 修复 Shift+框选启动时旧选区被覆盖的问题，现在会在框选结束时正确合并新旧选区。

### ⚡ 性能优化 (Performance)

* **组拖拽性能显著提升**
  > 组拖拽期间使用元素注册表缓存 DOM 引用，消除每帧 `querySelector` 查询，降低多便签拖拽时的主线程开销。

  - 停止拖拽时改为单次 Store 批量提交。
  - 50 便签组拖拽场景下帧率约提升 12 倍。
  - 拖拽过程继续避免 React 高频重渲染。

* **拖拽结束后延迟持久化**
  > `finalizeLayoutChange` 改为 2 秒防抖写盘，与其他状态更新保持一致，避免停止拖拽瞬间阻塞界面。

### 🔧 内部改进 (Internal)

* **布局常量统一**
  > 框选相关代码统一使用 `LAYOUT.NOTE_WIDTH` 与 `LAYOUT.NOTE_MIN_HEIGHT`，减少硬编码布局值带来的维护风险。

## [v1.3.2] - 2026-04-24

本版本聚焦状态结构与渲染订阅收敛，通过规范化数据模型和轻量布局投影降低画布级联重渲染。

### ⚡ 性能优化 (Performance)

* **主状态切换为规范化模型**
  > 运行时状态从 `notes: Note[]` 切换为 `notesById + allNoteIds + boardNoteIds`，减少单便签更新导致的列表引用扩散。

  - 单便签编辑不再迫使整张便签列表引用变化。
  - 看板与便签之间的关系更清晰。
  - 为后续大规模便签场景的性能优化打下基础。

* **新增轻量布局投影**
  > 新增 `layoutNotesById`，让 Canvas、MiniMap 等组件只订阅坐标、尺寸、层级等布局字段。

  - 避免拖拽、平移、看板切换时读取完整便签对象。
  - 降低正文、标题、颜色等非布局字段变化造成的重绘影响。
  - 为框选、拖拽等纯布局计算提供稳定数据源。

* **拖拽过程脱离 React 高频更新**
  > 单卡拖拽、多选拖拽、粘性拖拽与边缘推送统一改为直接 DOM transform，结束后再一次性提交到 Store。

  - 拖拽过程不再每帧触发 React 状态更新。
  - 多选拖拽更跟手。
  - 状态提交时机更集中，便于后续优化和测试。

* **Canvas 订阅粒度收敛**
  > Canvas 从全量 Store 解构改为多个独立 selector，并在事件处理器中按需读取最新状态。

  - 无关状态变化不再触发整个 Canvas 重绘。
  - 交互处理逻辑与渲染订阅边界更清晰。
  - 降低大画布场景下的渲染压力。

### 🧹 清理 (Cleanup)

* **移除 Immer 浅拷贝反模式**
  > 清理 Immer 中间件中的 `updateLayoutNote` 浅拷贝调用，改为直接修改 draft 属性。

  - 与 Immer 的不可变语义保持一致。
  - 减少不必要对象创建。
  - 降低后续维护者误判状态更新方式的风险。

## [v1.3.1] - 2026-04-22

本版本建立性能诊断基线，补齐大规模便签压测、性能预算、FPS 监控与保存链路诊断能力。

### ✨ 新特性 (Features)

* **建立可重复的性能基线**
  > 新增固定规模性能样本，用于复现 100 / 500 / 1000 / 3000 条便签场景下的真实渲染与交互表现。

  - 支持密集、稀疏、长文本、废纸篓等数据场景。
  - 便于对比不同优化前后的真实收益。
  - 为后续性能回归提供稳定输入。

* **新增性能预算与监控能力**
  > 定义渲染、IPC、交互三类预算阈值，并接入真实耗时采样。

  - React.Profiler 用于测量真实渲染耗时。
  - `performance.now()` 用于拆解 IPC 序列化与传输耗时。
  - 支持 FPS 与掉帧监控，便于定位交互卡顿。

* **支持跨端时钟对齐**
  > Rust 侧返回真实 I/O 自测耗时，前端可以区分 JS 序列化开销与文件系统瓶颈。

  - 保存性能诊断更准确。
  - 避免把磁盘 I/O 问题误判为前端渲染问题。

* **新增轻量诊断面板**
  > 新增脱离 React 主渲染管线的诊断面板，实时展示便签统计、保存耗时、FPS 与慢路径告警。

  - 使用 memo 与 ref 直接操作，避免诊断面板本身造成性能反噬。
  - 支持压测注入入口与运行时统计展示。

* **Profiling 构建隔离**
  > 新增 `dev:profiling` 与 `build:profiling` 脚本，仅在需要诊断时启用 React profiling 模式。

  - 生产构建默认不启用 profiling。
  - 性能诊断与正式发布产物相互隔离。

### 🐛 问题修复 (Bug Fixes)

* **修复 Performance API 竞态警告**
  > 使用 `performance.now()` 替代 User Timing API 的 mark / measure 组合，避免并发保存时出现 mark 不存在的警告。

* **修复诊断面板数据断线**
  > 接通 FPS 探针、便签统计同步与压测注入入口，保证诊断面板展示的数据完整可用。

* **修复测试桩递归问题**
  > 修正 `CanvasWithProfiler`、FPS 监控与 diagnostics 测试桩，避免 fake timers 与 `requestAnimationFrame` 互相递归。

* **修复高并发保存状态错乱**
  > `isSaving` 清理加入世代校验，避免旧保存请求在 finally 阶段误清理新一代保存状态。

  - 补齐 Rust `success:false` 分支测试。
  - 高并发保存时 loading 状态更准确。

### ⚡ 性能优化 (Performance)

* **画布虚拟化节流**
  > 引入视口外圈缓冲与坐标百位取整，降低平移场景下 O(N) 遍历重算频率。

  - 视口缓冲范围为 500px。
  - 降低拖拽与平移时的频繁重算。
  - 更容易保持 60fps 交互目标。

### 🧹 清理 (Cleanup)

* **移除残留 Performance 清理代码**
  > 清理迁移到 `performance.now()` 后遗留的 `performance.clearMarks` 调用，减少过期实现痕迹。

## [v1.3.0] - 2026-04-20

本版本聚焦持久化安全，引入写入队列、排他锁、事务备份与代际 ACK，降低并发保存和导入恢复场景下的数据风险。

### ✨ 新特性 (Features)

* **持久化写入进入单通道队列**
  > Rust 侧新增单通道意图队列，统一接管保存、导入与恢复操作，避免多入口并发写入互相覆盖。

  - 连续保存请求会自动折叠。
  - 写入背压与状态错乱风险降低。
  - 保存、导入、恢复使用同一套执行顺序。

* **新增排他锁与事务保护**
  > 基于 `fs4` 引入 OS 级文件排他锁，并在导入与恢复前自动生成 `.bak` 备份。

  - 支持指数退避重试，缓解杀毒扫描或多实例启动导致的锁冲突。
  - 崩溃或进程中断后可自动恢复。
  - 导入恢复不再裸写主数据文件。

* **新增状态代际 ACK**
  > 使用单调递增的 `generationId` 对齐 UI 状态回执，避免旧保存请求覆盖最新保存状态。

  - 减少“幽灵已保存”提示。
  - 高并发写入时保存状态更可信。

### 🔧 内部改进 (Internal)

* **明确存储引擎演进条件**
  > 新增存储引擎演进决策文档，明确继续使用 JSON 架构的性能条件，以及迁移到 SQLite / libSQL 的触发阈值。

* **补强高并发回归保障**
  > 新增高频保存压力测试、并发锁竞争测试、代际 ACK 验证与状态机覆盖，保护核心持久化链路。

## [v1.2.9] - 2026-04-18

本版本聚焦画布空转、钉住状态、浮层层级与保存反馈，提升长时间使用时的稳定性与可观测性。

### 🐛 问题修复 (Bug Fixes)

* **修复画布平移空转**
  > 背景平移不再在静止状态持续请求动画帧，改为事件驱动唤醒。

  - 交互停止、窗口失焦、组件卸载路径都会显式停机。
  - 减少无交互状态下的资源占用。

* **修复钉住状态回显丢失**
  > BOARD / TRASH 视图切换后，Pin 按钮状态现在会稳定回显。

  - 钉住状态上收至全局 Store。
  - App 常驻监听系统事件并同步状态。

* **修复壳内浮层遮挡**
  > 壳内浮层不再被高层级便签或拖拽态便签遮挡。

  - 补齐统一层级常量。
  - 固定壳层 stacking context，保证浮层可见性与命中稳定。

* **修复保存失败缺少界面反馈**
  > 保存异常不再只出现在控制台，数据管理区会展示保存中、已保存、保存失败及失败原因。

### 🚀 优化 (Optimizations)

* **保存状态扩展为可观测状态机**
  > 存储层新增 `saveStatus`、`saveError` 与 `lastSavedAt`，提升保存过程的可诊断性。

  - 用户能看到保存失败原因。
  - 后续测试可以覆盖更明确的保存状态合同。

### 🔧 内部改进 (Internal)

* **补强关键交互回归测试**
  > 更新 `Canvas`、`App`、`PinFab`、`WindowShell`、`BoardDock`、`useStore` 测试，覆盖事件停机、状态同步、层级合同与保存反馈。

## [v1.2.8] - 2026-04-16

本版本聚焦前端壳层回归，修复透明无边框窗口下的白框、尺寸同步、浮层命中与菜单边界问题。

### 🐛 问题修复 (Bug Fixes)

* **修复 WindowShell 壳层回归**
  > 主窗口外轮廓收回前端 `WindowShell` 后，透明无边框窗口场景下的白框和壳坐标错位问题得到收口。

  - 内容区尺寸同步更稳定。
  - 主界面视觉边界与交互边界重新对齐。

* **修复壳内浮层命中丢失**
  > `Spotlight`、`PinFab`、`MiniMap` 与 `BoardDock` 在壳层内可以稳定点击，避免浮层失效或误触画布。

* **修复右键菜单边界定位**
  > 右键菜单及子菜单在壳层边界附近会正确定位、翻转并限制可用高度。

  - 减少菜单越界。
  - 降低边缘区域难以操作的问题。

### 🚀 优化 (Optimizations)

* **统一交互坐标基准**
  > 主内容区、浮层与菜单统一使用壳层坐标基准，减少窗口边界变化导致的定位偏移。

### 🔧 内部改进 (Internal)

* **补齐壳层回归保障**
  > 更新 `App`、`WindowShell`、`Canvas`、`ContextMenu`、`Spotlight`、`BoardDock`、`MiniMap`、`PinFab` 测试，覆盖尺寸同步、命中区域、坐标换算与菜单边界。

### 🧹 清理 (Cleanup)

* **移除不稳定原生窗口效果**
  > 移除窗口 `Mica` 效果配置，不再依赖系统圆角或原生窗口特性，由前端壳层统一收口视觉与交互边界。

## [v1.2.7] - 2026-04-13

本版本修复 Dock 缩放场景下的菜单定位与导入后的看板顺序合同，并继续收敛深色模式细节。

### 🐛 问题修复 (Bug Fixes)

* **修复 Dock 菜单定位偏移**
  > 看板 Dock 在缩放容器下打开右键菜单时，现在会按真实渲染坐标对齐目标看板。

* **修复恢复备份后的看板顺序**
  > 恢复备份后会保留导入批次内部相对顺序，并整体追加到本地看板末尾。

  - 旧版自动补建的默认看板保持在导入批次前部。
  - 导入后的看板排序合同更明确。

### 🚀 优化 (Optimizations)

* **深色模式一致性优化**
  > 收敛 `NoteCard` 在暗色模式下的选区文本、折叠标题亮度与 placeholder 层级，让标题与正文表现更统一。

### 🔧 内部改进 (Internal)

* **补齐导入与深色模式回归测试**
  > 更新 `BoardDock`、`dataTransfer`、`useStore` 与 `NoteCard` 测试，覆盖缩放定位、导入顺序合同与暗色模式一致性。

## [v1.2.6] - 2026-04-13

本版本增强备份恢复兼容性，补齐导入反馈、失败回滚与旧版数据迁移路径。

### ✨ 新特性 (Features)

* **旧版备份支持兼容恢复**
  > 缺失版本字段的旧版 SoNotes 备份现在可以被识别，并按当前数据契约补齐必要字段后导入。

  - 降低历史备份无法恢复的风险。
  - 导入时会进行版本分流与合法性校验。

* **恢复备份结果可见化**
  > 数据管理区会展示恢复备份后的成功、取消、失败、回滚与兼容迁移状态。

  - 用户可以直接确认导入是否完成。
  - 失败和回滚信息不再只隐藏在内部状态中。

### 🐛 问题修复 (Bug Fixes)

* **修复导入失败留下中间态**
  > 导入写入阶段失败时会回滚到导入前状态，不再留下部分写入结果。

  - 覆盖本地 JSON 持久化失败。
  - 覆盖 WAL 写入失败。

* **修复回滚反馈语义**
  > 回滚场景下不再混入导入计数摘要，避免用户误以为部分内容已经成功导入。

### 🚀 优化 (Optimizations)

* **导入兼容路径更可预期**
  > 恢复备份过程补齐同名看板重命名、主板回退与部分成功摘要，让导入结果更容易理解。

### 🔧 内部改进 (Internal)

* **补齐导入恢复回归测试**
  > 更新 `dataTransfer`、`useStore` 与 `BoardDock` 测试，覆盖旧版迁移、部分成功、WAL / 磁盘失败回滚与导入反馈展示。

## [v1.2.5] - 2026-04-13

本版本集中修复深色模式下的便签可读性、选中态可见性、框选对比度与折叠触发范围。

### 🐛 问题修复 (Bug Fixes)

* **修复深色模式正文对比度**
  > 深色模式下浅色便签正文与占位符不再发灰或发白，白色、黄色、粉色、橙色等便签更容易阅读。

  - 重新收敛深色便签底色映射。
  - 未知旧颜色会使用更中性的深色兜底。

* **修复文本选中高亮过弱**
  > 标题输入框与正文 textarea 在深色模式下的选中文本高亮更清晰。

* **修复便签双击折叠范围过大**
  > 双击折叠或展开现在仅由便签头部拖拽区触发，不再因为双击正文或其他区域误折叠。

### 🚀 优化 (Optimizations)

* **增强便签选中态可见性**
  > 深色模式下单选与多选的 ring、border 与焦点反馈更明显，减少玻璃化背景中的识别成本。

* **增强框选矩形可见性**
  > 深色背景和深色便签上，框选矩形的填充、描边与投影对比度更高。

### 🔧 内部改进 (Internal)

* **补齐深色模式回归测试**
  > 更新 `NoteCard`、`Canvas` 与 `types` 测试，覆盖深色底色映射、可见性类名与折叠触发范围。

## [v1.2.4] - 2026-04-12

本版本修复 Dock 菜单锚点和浮层层级冲突，并建立共享浮层层级合同。

### 🐛 问题修复 (Bug Fixes)

* **修复 BoardDock 右键菜单锚点**
  > 看板 Dock 右键菜单不再依赖默认坐标，能够对齐真实点击的看板项。

  - 锚点改为基于 Dock 容器内的局部布局坐标计算。
  - 缩放场景下菜单定位更稳定。

* **修复浮层层级冲突**
  > Dock、ContextMenu、Tooltip、Spotlight 与 MiniMap 不再各自硬编码层级，遮挡关系回到统一合同。

### 🚀 优化 (Optimizations)

* **建立共享浮层层级合同**
  > `layout.ts` 新增 app-level overlay token，统一 `MINIMAP < DOCK_BACKDROP < DOCK < TOOLTIP < MENU < SPOTLIGHT` 的层级顺序。

### 🔧 内部改进 (Internal)

* **补齐 Dock 菜单回归测试**
  > 新增 BoardDock 锚点与层级顺序测试，覆盖真实点击项定位与缩放容器场景。

## [v1.2.3] - 2026-04-12

本版本回退并修正便签头部交互方案，稳定拖拽、折叠、空白画布命中与 Spotlight 跨板跳转。

### 🐛 问题修复 (Bug Fixes)

* **修复便签头部交互回归**
  > 撤回 v1.2.2 中基于 DOM 区域拆分的头部交互方案，改用全局拖拽锁与复合条件渲染。

  - 拖拽时不再因为选中态切换导致正文异常展开。
  - 拖拽期间不会误触头部动作。
  - 折叠态继续隐藏复制按钮，并收敛多余头部控件。

* **修复拖拽中的空白画布误判**
  > 拖拽锁开启时，空白画布双击新建、空白按下清空选择与框选起始都会被抑制。

* **修复 Spotlight 跨板跳转抖动**
  > 跨看板搜索跳转从 `setTimeout` 触发链改为状态驱动的单次稳定落点，降低切板后的定位抖动与时序竞态。

### 🚀 优化 (Optimizations)

* **MiniMap 视口写入更稳定**
  > 收敛小地图键盘微调、背景跳转与视口拖拽的写入路径，让视口更新统一经过保护逻辑。

  - 当前看板隔离保持不变。
  - 空看板与跨板跳转场景更稳定。

* **恢复头部拖拽手柄表现**
  > 恢复中置、非按钮式拖拽手柄，并补回抓手光标与折叠标题的中心对齐。

### 🔧 内部改进 (Internal)

* **补齐头部交互与 MiniMap 测试**
  > 更新 NoteCard、Canvas、MiniMap 测试，覆盖拖拽锁、空白命中保护、折叠态按钮与小地图看板隔离。

### 🧹 清理 (Cleanup)

* **同步历史发布文案**
  > 同步整理 v1.1.9 至 v1.2.2 的 CHANGELOG / GitHub Release Notes 表述，保持线上线下说明一致。

## [v1.2.2] - 2026-04-12

本版本尝试拆分便签头部热区，减少拖拽、折叠、按钮点击与空白画布操作之间的误触。

### 🐛 问题修复 (Bug Fixes)

* **修复便签头部多操作串扰**
  > 便签头部拖拽、折叠与按钮点击不再共用同一热区，减少误触和操作打断。

  - 拖拽把手、折叠触发区、头部按钮区分离。
  - 降低误触拖拽、误触折叠与头部操作互相打断的问题。

* **修复折叠态复制按钮显示**
  > 便签折叠后不再显示复制按钮，头部可操作性与当前状态保持一致。

* **修复头部操作被误判为空白画布**
  > 双击头部不再触发空白画布新建，点击头部按钮区也不会清空当前选择。

### 🚀 优化 (Optimizations)

* **头部显隐规则更一致**
  > 标题输入、头部控件与折叠标题的显示规则更收敛，悬浮、编辑与折叠切换时状态更稳定。

### 🔧 内部改进 (Internal)

* **补齐头部命中回归测试**
  > 新增 NoteCard 与 Canvas 交互测试，覆盖头部命中边界、折叠态按钮显示与空白画布判定。

## [v1.2.1] - 2026-04-11

本版本修复 MiniMap 跨看板混入、边界计算与隐藏态交互问题，让小地图重新聚焦当前看板。

### 🐛 问题修复 (Bug Fixes)

* **修复 MiniMap 看板隔离**
  > 小地图不再混入其他看板便签，概览范围重新聚焦当前看板。

  - 仅显示当前看板且未删除的便签。
  - 避免跨看板内容干扰当前视图。

* **修复 MiniMap 边界计算**
  > 小地图缩放边界不再被其他看板内容放大，切换看板后当前视口与概览范围对应更准确。

* **修复 MiniMap 隐藏态交互遮挡**
  > 小地图隐藏时不再占据右下角交互区域，避免误挡画布点击。

### 🚀 优化 (Optimizations)

* **空看板与切板反馈更稳定**
  > 空看板和看板切换场景下，小地图视口显示逻辑更稳定，即使无便签也保持一致反馈。

### 🔧 内部改进 (Internal)

* **补齐 MiniMap 回归测试**
  > 新增 MiniMap 测试，覆盖当前看板过滤、切换看板更新与空看板场景。

## [v1.2.0] - 2026-02-12

本版本引入完整深色模式与设置中心，补齐主题切换、数据管理入口，并修复平移、小地图和拖拽状态相关问题。

### ✨ 新特性 (Features)

* **深色模式完整支持**
  > 为夜间工作场景补齐主题支持，便签在深色环境下仍保持清晰层次与配色辨识度。

  - 支持浅色、深色、跟随系统三种主题模式。
  - 补齐深色便签颜色映射。
  - 辅助界面同步适配玻璃化视觉效果。

* **设置中心重新整理**
  > Dock 设置菜单重新分层，主题模式与数据管理入口更清晰。

  - 主题切换集中到设置中心。
  - 数据管理能力与主题设置分离展示。

### 🐛 问题修复 (Bug Fixes)

* **修复平移模式仍选中便签**
  > 平移模式下点击便签不再触发选中，避免拖动画布时被便签交互打断。

* **修复小地图被便签遮挡**
  > 小地图层级更稳定，概览视图始终可见。

* **修复拖拽状态残留**
  > 通过小地图跳转视口后，鼠标不再残留在拖拽状态。

### 🚀 优化 (Optimizations)

* **统一深色模式视觉语言**
  > 右键菜单、Spotlight、废纸篓等辅助界面的颜色与层级更协调。

* **改善启动防闪烁**
  > 优化应用启动时的样式注入逻辑，降低深色模式下的白屏闪烁感。

* **恢复 Spotlight 搜索权重**
  > 搜索结果排序逻辑重新接入并优化，目标内容更容易被命中。

## [v1.1.9] - 2026-02-12

本版本新增便签一键复制与浏览器原生行为守卫，并继续优化拖拽性能和头部布局。

### ✨ 新特性 (Features)

* **便签支持一键复制**
  > 点击便签顶部复制按钮即可快速提取当前便签内容。

  - 有标题时按“标题 + 换行 + 正文”写入剪贴板。
  - 复制成功后提供即时反馈，减少操作打断。

* **屏蔽易误触的浏览器原生行为**
  > 在桌面应用场景中拦截 `F5`、`Ctrl + R`、`Ctrl + S`、`Ctrl + F`、`Ctrl + 滚轮` 等浏览器行为。

  - 非编辑区域禁用原生右键菜单。
  - 避免刷新、缩放、默认查找打断便签操作。

### 🚀 优化 (Optimizations)

* **优化拖拽性能**
  > 调整拖拽过程中的渲染与交互控制逻辑，降低多便签场景下的卡顿与重绘开销。

* **修复头部布局不对称**
  > 便签头部按钮布局更均衡，标题区域更容易保持居中。

## [v1.1.8] - 2026-02-11

本版本新增 Spotlight 全局搜索，支持键盘唤起、跨看板跳转与按需渲染。

### ✨ 新特性 (Features)

* **新增 Spotlight 全局搜索**
  > 按下 `Ctrl + P`（macOS 为 `Cmd + P`）即可快速搜索便签内容。

  - 支持跨看板搜索。
  - 点击结果后自动切换看板并移动到目标便签。
  - 支持 `↑` / `↓` 选择与 `Enter` 跳转。
  - 使用大气毛玻璃视觉，让搜索界面与主界面风格一致。

### 🐛 问题修复 (Bug Fixes)

* **修复拖拽时 Tooltip 残留**
  > 拖拽便签时，悬浮提示框不再错误滞留在屏幕上。

* **修复群组拖拽边界回弹**
  > 群组拖拽撞击屏幕边缘时，越界便签会被独立压回可见范围。

  - 拖动群组中任意便签时，整个组保持队形。
  - 不再出现部分便签遗留在屏幕外的情况。

### ⚡ 性能优化 (Performance)

* **Spotlight 按需渲染**
  > 全局搜索组件关闭时完全销毁，避免对画布拖拽性能产生持续影响。

## [v1.1.7] - 2026-02-10

本版本深度优化 MiniMap 与画布平移性能，并新增快捷键系统。

### ✨ 新特性 (Features)

* **新增快捷键系统**
  > 高频操作可以直接通过键盘完成，减少鼠标操作路径。

  - `Delete` / `Backspace`：删除选中便签。
  - `Ctrl + A` / `Cmd + A`：全选当前看板便签。
  - `Ctrl + D` / `Cmd + D`：创建选中便签副本。
  - `Ctrl + 0` / `Cmd + 0`：重置视口位置。

### ⚡ 性能优化 (Performance)

* **MiniMap 渲染深度重构**
  > MiniMap 使用原子化组件与 `React.memo`，解决拖拽便签时触发 O(N) 全量重绘的问题。

  - 大量便签场景下拖拽更流畅。
  - 小地图便签项更新粒度更细。

* **MiniMap 视口拖拽零延迟**
  > 视口红框改为直接 DOM 操作并结合 RAF 节流，减少 React 渲染循环带来的滞后。

* **Canvas 平移节流**
  > 画布视口平移从高频 React 更新中解耦，改用 `requestAnimationFrame` 循环驱动。

### 🔧 内部改进 (Internal)

* **Store 订阅粒度优化**
  > 全面应用 `useShallow` 进行细粒度状态订阅，减少父组件因无关状态变化产生的无效重绘。

## [v1.1.6] - 2026-02-10

本版本新增交互式小地图与无限画布体验优化，并修复折叠、布局和小地图显示问题。

### ✨ 新特性 (Features)

* **新增交互式小地图**
  > 小地图支持点击跳转、视口拖拽与常驻显示，方便在大画布中快速定位。

  - 点击小地图任意位置可平滑跳转到对应坐标。
  - 支持直接拖拽视口框快速漫游。
  - 闲置时以半透明状态常驻，便于随时定位。

* **优化无限画布漫游体验**
  > 修复负坐标区域无法导航的问题，用户可以自由探索画布各个方向。

### 🐛 问题修复 (Bug Fixes)

* **修复折叠标题宽度回归**
  > 折叠模式下便签恢复舒适阅读宽度，避免标题被过度截断。

* **修复小地图尺寸计算错误**
  > 小地图在特定情况下不再因为尺寸计算异常而不可见。

### ⚡ 性能优化 (Performance)

* **优化 MiniMap 渲染与资源占用**
  > 重构 MiniMap 渲染逻辑，移除冗余计算，降低拖拽便签时的卡顿与长列表内存占用。

### 🧹 清理 (Cleanup)

* **统一布局常量**
  > 提取 `src/constants/layout.ts` 管理布局常量，让布局尺寸来源更清晰。

## [v1.1.5] - 2026-02-09

本版本引入无限画布、三轨交互模式与全局导航小地图，让便签空间不再受窗口边界限制。

### ✨ 新特性 (Features)

* **新增无限画布**
  > 便签可以拖拽到窗口之外的更大空间，支持持续扩展工作区域。

* **新增三轨交互模式**
  > 支持安全模式、推墙扩展与摄像机模式，应对不同画布操作习惯。

* **新增全局导航小地图**
  > 推墙或平移时自动浮现小地图，方便快速定位大画布内容。

* **支持视口记忆与快速归位**
  > 切换看板时自动保存视口位置，并支持双击 Space 或托盘菜单重置视口。

### 🚀 优化 (Optimizations)

* **统一拖拽边界逻辑**
  > 修复窗口拉伸适配问题，推墙与拖拽边界行为更一致。

* **优化推墙拖拽性能**
  > 降低推墙过程中的抖动，让画布扩展更稳定。

## [v1.1.4] - 2026-02-07

本版本新增看板数据导入导出，并整理设置入口与上下文菜单。

### ✨ 新特性 (Features)

* **新增看板数据导入导出**
  > 用户可以导出看板数据进行备份或迁移，并通过导入恢复到应用中。

  - 支持全量导出所有看板、便签与设置数据为 JSON。
  - 支持仅导出当前看板。
  - 导入数据会重新生成 ID，避免覆盖现有内容。
  - 导入看板会标记为 Imported，便于识别。

* **设置与菜单入口更集中**
  > Dock 栏新增设置菜单，整合导入导出能力，并精简画布右键菜单。

  - 数据管理入口更容易找到。
  - 画布菜单保留显示菜单、新建便签等核心操作。

### 🐛 问题修复 (Bug Fixes)

* **修复导出文件权限问题**
  > 导出文件时不再因权限不足触发 `fs.write_text_file` 错误。

* **修复 Store 类型定义混淆**
  > 区分 Action 与 Data 类型，提升状态定义的类型安全性。

## [v1.1.3] - 2026-02-07

本版本新增废纸篓，删除操作改为软删除，并提供还原、永久删除与专属入口。

### ✨ 新特性 (Features)

* **新增废纸篓**
  > 删除便签后会先进入废纸篓，用户可以在真正清除前找回误删内容。

  - 已删除便签以网格视图展示。
  - 删除操作改为软删除，不立即销毁数据。
  - 支持将便签还原到原看板；原看板缺失时还原到当前看板。
  - 支持永久删除废纸篓中的便签。

* **新增废纸篓入口**
  > 看板栏底部新增垃圾桶图标，点击即可进入废纸篓视图。

### 🚀 优化 (Optimizations)

* **视图切换时清理选中状态**
  > 看板视图与废纸篓视图切换时会自动清除当前选中，避免跨视图残留造成误操作。

* **废纸篓视觉与本地化优化**
  > 废纸篓视图完成中文适配，并用半透明灰色卡片表达不可拖拽、不可直接编辑的状态。

  - BoardDock 在废纸篓模式下不再被透明遮罩阻挡。
  - 废纸篓卡片禁用拖拽，减少误操作。

## [v1.1.2] - 2026-02-06

本版本增强批量管理与看板排序，并修复拖拽性能、Tooltip、看板指示器和安全删除问题。

### ✨ 新特性 (Features)

* **右键菜单支持批量移动与复制**
  > 多选便签后，可以通过右键菜单批量移动或复制到其他看板。

  - 操作时会自动避让目标位置，减少便签堆叠。
  - 适合整理一组相关便签。

* **支持看板键盘排序**
  > 在看板栏图标上右键进入调整顺序后，可用 `←` / `→` 快速移动看板位置。

### 🐛 问题修复 (Bug Fixes)

* **修复便签拖拽卡顿**
  > `NoteCard` 渲染逻辑改为原子化状态订阅，降低拖拽时的掉帧问题。

* **修复拖拽时 Tooltip 残留**
  > 拖拽便签时提示框会正确消失。

* **修复看板指示器回归**
  > 左上角看板指示器会正确显示并实时刷新名称。

* **增强多便签删除确认**
  > 删除包含多个便签的群组时会要求二次确认，降低误删风险。

## [v1.1.1] - 2026-02-06

本版本升级右键菜单与看板管理，新增副本、跨看板移动复制、原地重命名和更安全的删除机制。

### ✨ 新特性 (Features)

* **右键菜单管理能力升级**
  > 便签右键菜单新增创建副本、移动到看板、复制到看板等能力。

  - 支持在当前位置快速创建便签副本。
  - 支持将便签移动到其他看板，并自动避让目标位置。
  - 支持跨看板复制便签。
  - 二级菜单采用延迟关闭机制，减少误移出导致的菜单消失。

* **支持看板原地重命名**
  > 双击底部 Dock 栏图标，或使用右键菜单，即可直接修改看板名称。

### 🚀 优化 (Optimizations)

* **看板删除更安全**
  > 删除看板时不再隐式移动到默认看板，而是明确删除看板及其下属便签。

  - 删除前会检测看板内便签数量。
  - 有便签时需要二次点击确认。

* **修复一键归拢跨看板吸附**
  > 一键归拢只整理当前视野内便签，不再把其他看板内容错误吸附过来。

## [v1.1.0] - 2026-02-05

本版本新增多看板系统与可视化钉住状态，并修复视口偏移、Dock 布局、拖拽遮挡和焦点抢占问题。

### ✨ 新特性 (Features)

* **新增多看板系统**
  > SoNotes 支持创建多个独立看板，用于区分工作、生活、灵感等不同便签集合。

  - 通过右键菜单或底部 Dock 快速切换看板。
  - Dock 使用悬浮药丸设计，支持进场动画与缩放反馈。
  - 支持创建多个看板，并内置删除保护。

* **新增可视化钉住状态**
  > 窗口钉住后，右上角会显示悬浮按钮，点击即可取消钉住并恢复失焦隐藏模式。

### 🐛 问题修复 (Bug Fixes)

* **修复 Windows 视口偏移**
  > 移除 `100vw` 布局，解决滚动条计算导致的界面横向偏移问题。

* **修复 Dock 拉伸左偏**
  > Dock 采用更稳健的 Flex 隔离布局策略，窗口拉伸时位置更稳定。

* **修复顶部拖拽区域被便签遮挡**
  > 提升窗口拖拽区域层级，置顶便签不再阻碍窗口移动。

* **修复切换看板时输入框抢焦点**
  > 只有刚创建的便签会自动聚焦，切换看板时旧便签不再抢夺焦点。

### 🧹 清理 (Cleanup)

* **收敛浮层架构**
  > 将 `BoardDock`、`PinFab`、`ContextMenu` 移至 App 层级，减少 CSS Filter 导致的层叠上下文问题。

* **移除开发调试痕迹**
  > 清理开发调试日志，并提取图标常量，降低后续维护噪音。

## [v1.0.9] - 2026-02-04

本版本优化视觉细节与智能折叠体验，并修复长文本布局、托盘归位和新建聚焦问题。

### 🐛 问题修复 (Bug Fixes)

* **修复长文本导致视窗偏移**
  > 长文本便签不再导致应用坐标系下移，避免其他便签被顶出屏幕外。

* **修复托盘钉住窗口位置异常**
  > 程序启动后直接点击“钉住窗口”时，窗口会正确出现在右下角。

* **修复粘贴并新建未聚焦标题**
  > 使用粘贴并新建后，标题输入框会自动获得焦点。

### 🚀 优化 (Optimizations)

* **滚动条视觉更精致**
  > 应用内滚动条改为细圆角半透明样式，减少原生滚动条带来的突兀感。

* **未选中便签自动紧凑显示**
  > 未选中便签会自动收起高度并显示底部渐隐遮罩，选中后立即展开。

  - 桌面更整洁。
  - 长内容仍可在选中后完整查看。

## [v1.0.8] - 2026-02-04

本版本重构右键菜单，新增粘贴并新建、一键归拢与群组模式，让桌面整理更直接。

### ✨ 新特性 (Features)

* **右键菜单支持画布模式与群组模式**
  > 右键菜单会根据当前选择状态自动切换操作内容。

  - 无选中或单选时提供新建、粘贴等全局操作。
  - 多选时切换为群组操作，支持批量整理与删除。

* **新增粘贴并新建**
  > 在画布空白处右键选择“粘贴并新建”，即可把剪贴板文本转换为新便签。

  - 新建后标题输入框自动聚焦。

* **新增一键归拢**
  > 画布中的散乱便签可以一键按空间位置重新排列。

  - 基于从左到右、从上到下的视觉顺序整理。
  - 行高自动适配卡片高度。
  - 全局归拢带二次确认，降低误触风险。

### 🚀 优化 (Optimizations)

* **界面入口更清爽**
  > 移除右下角冗余的新建按钮，减少主界面视觉干扰。

* **修复删除后的幽灵选中**
  > 删除便签后右键菜单不再误判旧选中状态。

* **单选菜单逻辑更清晰**
  > 单个便签不再显示“整理选中”这类群组操作。

### 🔧 内部改进 (Internal)

* **引入剪贴板插件**
  > 新增 `@tauri-apps/plugin-clipboard-manager`，用于支持剪贴板读取与粘贴新建。

## [v1.0.7] - 2026-02-03

本版本全面升级画布选择、右键菜单、群组拖拽与边界保护，提升多便签整理能力。

### ✨ 新特性 (Features)

* **新增框选与群组交互**
  > 在画布空白处拖动即可批量选中便签，选中多个便签后可作为一组移动。

  - 支持 Ctrl / Shift 点击加选。
  - 拖动组内任意便签时，整组保持队形移动。

* **右键菜单能力扩展**
  > 右键菜单支持画布与便签两类上下文操作。

  - 画布菜单支持直接新建便签与整理便签。
  - 便签菜单支持改色、置顶、删除与吸附移动入口。

* **新增整理便签**
  > 右键菜单新增整理便签能力，可以将凌乱便签快速对齐到网格。

### 🐛 问题修复 (Bug Fixes)

* **新增单体与群组边界保护**
  > 拖拽便签到屏幕边缘时会保留可见区域，避免便签遗失在屏幕外。

  - 单体拖拽保留 100px 可见区域。
  - 群组拖拽采用分布式边界检查，组内任意便签触边都会整体收拢。

### 🚀 优化 (Optimizations)

* **选中状态更容易识别**
  > 单选使用白色边框，多选使用蓝色边框，便于区分当前选择范围。

* **吸附移动反馈更清晰**
  > Sticky Drag 的视觉反馈更稳定，减少操作状态不明确的问题。

## [v1.0.6] - 2026-02-03

本版本新增托盘钉住模式，并重构窗口自动隐藏逻辑，减少切换应用和拖动窗口时的误隐藏。

### ✨ 新特性 (Features)

* **新增托盘钉住模式**
  > 托盘右键菜单新增“钉住窗口”开关，窗口可以置顶常驻。

  - 开启后窗口不再自动隐藏。
  - 菜单文案会动态切换为“取消钉住”。
  - 当前钉住状态更容易识别。

### 🐛 问题修复 (Bug Fixes)

* **修复窗口自动隐藏死锁**
  > 切换应用时，即使鼠标仍在窗口内，窗口也能正确响应隐藏逻辑。

* **修复拖动窗口时误隐藏**
  > 拖动窗口标题栏时，Windows 系统失焦事件不再导致窗口误判并消失。

### 🚀 优化 (Optimizations)

* **自动隐藏行为更符合预期**
  > 点击外部、切换应用、鼠标移出窗口时，窗口隐藏行为更稳定，减少粘滞或误触。

## [v1.0.5] - 2026-02-02

本版本新增托盘重置窗口能力，并修复数据同步覆盖与数据文件可读性问题。

### ✨ 新特性 (Features)

* **新增托盘重置窗口**
  > 托盘右键菜单新增“重置窗口”按钮，可一键将窗口大小重置为 400x600 并归位到屏幕右下角。

### 🐛 问题修复 (Bug Fixes)

* **修复旧缓存覆盖新数据**
  > 启动时会对比磁盘文件与缓存数据的更新时间，并使用最新版本作为有效数据。

  - 适用于开发版与发布版交替使用场景。
  - 降低多设备或多环境同步时的数据回退风险。

* **优化数据文件格式**
  > `data.json` 保存时启用 2 空格缩进，方便用户直接用文本编辑器阅读和修改。

## [v1.0.4] - 2026-02-02

本版本新增交互式待办、折叠模式与智能标题，并修复 Tooltip 被裁剪的问题。

### ✨ 新特性 (Features)

* **新增交互式待办**
  > Markdown 待办列表中的 `[ ]` 可以直接点击勾选，无需进入编辑模式。

* **新增折叠模式**
  > 双击便签顶部拖拽栏即可收起便签，仅保留标题栏；再次双击即可展开。

  - 适合临时释放桌面空间。
  - 折叠状态仍保留标题信息。

* **标题输入更克制**
  > 标题输入框仅在鼠标悬停或已有标题时显示，降低界面干扰。

  - 折叠且无标题时显示“未命名便签”。
  - 避免折叠后内容失去识别线索。

### 🐛 问题修复 (Bug Fixes)

* **修复 Tooltip 裁剪遮挡**
  > Tooltip 改用 React Portal 渲染，折叠模式或窗口边缘时不再被父容器裁剪。

## [v1.0.3] - 2026-02-02

本版本完成品牌重命名，调整数据存储路径，并增强持久化安全与启动恢复能力。

### ⚠️ 重大变更 (Breaking Changes)

* **项目名称更改为 SoNotes**
  > 项目名称由 TrayNotes 更名为 SoNotes（随心记）。

* **数据存储路径变更**
  > 数据目录从 `Documents/TrayNotes` 变更为 `Documents/SoNotes`。

  - 旧版本用户需要手动移动原文件夹内的 `data.json` 与 `assets` 目录。
  - 完成移动后即可保留历史数据。

### ✨ 新特性 (Features)

* **新增边界卫士**
  > 便签被意外拖拽到负坐标区域时，会自动吸附回可见范围，避免便签离开屏幕。

### 🐛 问题修复 (Bug Fixes)

* **增强数据持久化安全**
  > 写入数据时使用原子写入与回退机制，遇到文件锁定时会自动降级处理，降低数据丢失风险。

* **修复启动偶发空白**
  > 应用启动时会优先检查 IndexedDB；若为空，则自动从磁盘备份读取数据。

## [v1.0.2] - 2026-02-01

本版本新增右键吸附拖拽与操作提示，并修复便签高度、层级和空白双击新建问题。

### ✨ 新特性 (Features)

* **新增右键吸附拖拽**
  > 触控板用户无需一直按住鼠标，在拖动栏右键即可进入跟随模式，再次点击即可放置便签。

* **新增操作提示**
  > 拖动栏增加延迟 1 秒的悬浮提示，帮助新用户发现隐藏操作。

### 🐛 问题修复 (Bug Fixes)

* **修复启动时便签高度被压缩**
  > 便签启动后会根据内容正确展开高度。

* **修复点击文字区域无法置顶**
  > 点击便签文字区域时，当前便签会正确提升层级。

* **恢复双击空白新建便签**
  > 双击背景空白处可以再次创建新便签。

### ⚡ 性能优化 (Performance)

* **减小安装包体积**
  > 开启 LTO 和符号表剔除，安装包体积减少约 30%。

## [v1.0.1] - 2026-02-01

本版本聚焦视觉升级，启用 Windows 11 风格的窗口效果、柔光显示与多彩配色。

### ✨ 新特性 (Features)

* **启用云母效果**
  > Windows 11 环境下启用原生磨砂玻璃背景，让应用更贴近系统视觉风格。

* **新增智能柔光**
  > 深色模式下自动调整不透明度和滤镜，夜间使用更柔和。

* **新增多彩配色**
  > 新增 6 种莫兰迪色系，共 13 种颜色可选。

### 🚀 优化 (Optimizations)

* **无边框窗口更精致**
  > 移除原生阴影，改用 1px 内描边，减少黑角伪影。

* **完成界面中文化**
  > 界面与托盘菜单完成中文化，本地使用体验更自然。

## [v1.0.0] - 2026-02-01

SoNotes（随心记）首次发布，提供本地优先的桌面便签体验。

### ✨ 新特性 (Features)

* **原生桌面便签体验**
  > 应用采用 Windows 11 风格视觉，并以托盘入口、双击新建、自动保存作为核心交互。

  - 支持极简托盘入口。
  - 支持双击快速新建便签。
  - 支持自动保存，降低手动管理成本。

* **本地隐私保护**
  > 数据完全保存在本地，不依赖云端同步或上传。
