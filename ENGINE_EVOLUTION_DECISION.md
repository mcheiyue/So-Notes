# 存储引擎演进闸门决策 (v1.3.x+)

> 生成日期：2026-04-20
> 生成背景：v1.3.0 完成了单写入口、OS 级排他锁和代际状态 ACK，但 JSON 架构在大规模数据下的读写性能瓶颈依然存在。

---

## 1. 选项对照表

| 维度 | 继续演进纯 JSON (WAL + 队列) | 迁移至 SQLite / libSQL |
|---|---|---|
| **架构复杂度** | 低。基于文件系统的标准原子操作。 | 高。引入 C 依赖 (sqlite3) 或 WASM 运行时 (libSQL/Turso)。 |
| **并发写模型** | 依赖文件锁 + 队列串行化，随并发数上升锁竞争成本增加。 | 原生支持 WAL 模式和高并发并发读取。写入串行但读取可并行。 |
| **数据一致性** | 最终一致性；崩溃时需回滚或依靠 WAL 重放。 | ACID 事务级；系统崩溃后自动恢复，无需手动重放逻辑。 |
| **迁移成本** | 无。当前数据即为纯 JSON。 | 中高。需编写一次性全量导入脚本，并处理未来数据结构变更的版本兼容性。 |
| **生态与工具** | 通用性强，易于备份和手动修复。 | 依赖专门的 DB 浏览器和 SQL 查询工具。利于扩展索引/搜索。 |
| **包体积负担** | 几乎为零。 | SQLite 增加约 500KB - 1MB；libSQL 可能更大。 |

---

## 2. 触发条件（闸门度量）

当满足以下**任意两项**条件时，必须在下一里程碑启动 SQLite/libSQL 迁移（预计为 v1.3.1 或 v1.4.0）：

1. **数据规模阈值**：单看板便签数量 > 5000，或整体 `data.json` 大小 > 20MB，且保存操作在标准硬件上耗时突破 200ms。
2. **并发故障率**：在极端高频保存下（如测试套模拟的 100次/秒 写入），锁重试耗尽率（Lock Contention Exhaustion） > 1%。
3. **复杂查询需求**：引入全文搜索、跨看板高级过滤或模板系统，导致内存中线性遍历 `notes[]` 的性能开销 > 50ms。
4. **维护成本超标**：WAL 重放和崩溃恢复的回归测试用例数 > 30，且每季度引发 > 2 个生产环境 Bug。

---

## 3. 决策结论

**当前状态 (v1.3.0)：维持 JSON 架构。**

- 当前的单通道意图队列 (Workflows A) 和指数退避锁 (Workflow B) 已经吸收了 95% 以上的常规并发写冲突风险。
- Tauri 架构对轻量级桌面应用而言，JSON 序列化和原子文件重命名（tmp -> rename）足以应对未来 1-2 年的用户规模增长。

**下一阶段建议 (v1.3.x)：实施 `libSQL` 预研沙盒。**

- 不直接替换主存储。建立一个平行的 `libSQL` 沙盒模块 (可选启用)。
- 验证 Tauri 2.x 环境下的 WASM SQLite 加载开销与沙盒事务性能。
- 如果沙盒表现优异且上述触发条件达到，则 v1.4.0 执行全量无缝迁移。

---

## 4. 实施路线图建议

- **v1.3.1**：完善当前 JSON 队列的诊断日志，建立“保存耗时”与“锁重试次数”的指标遥测（为阈值 1 & 2 提供数据支撑）。
- **v1.3.2**：开发 `libSQL` 原型集成分支，仅实现基础的读写操作并对比 JSON 方案。
- **v1.4.0** (条件触发)：如果沙盒验证通过，则开发“一键无缝迁移向导”，提供从 `data.json` 到 `notes.db` 的全量转换工具。
