# 存储引擎演进闸门决策

> 初始生成日期：2026-04-20
> 状态校准日期：2026-05-21
> 当前结论：继续维持 JSON 主存储；SQLite / libSQL 仅保留为触发条件满足后的预研方向。

---

## 1. 背景

v1.3.0 完成了单写入口、OS 级排他锁、事务备份和代际状态 ACK，解决了此前持久化链路中的主要一致性风险。

随后 v1.3.x 又完成了几项缓解大规模数据压力的基础工作：

- v1.3.1 建立性能基线、性能预算、FPS 监控与轻量诊断能力。
- v1.3.2 将状态结构规范化，并收敛 Canvas 渲染订阅。
- v1.3.3 优化组拖拽路径，减少 DOM 查询与 Store 多次写入。
- v1.3.4 将 Spotlight 搜索索引下沉到 Web Worker，避免搜索阻塞主线程。

因此，早期“v1.3.2 开发 libSQL 原型、v1.4.0 条件触发全量迁移”的预测已经不再符合当前路线。当前应继续使用闸门判断，而不是按版本号强行迁移。

---

## 2. 选项对照表

| 维度 | 继续演进 JSON 架构 | 迁移至 SQLite / libSQL |
|---|---|---|
| 架构复杂度 | 低。基于现有文件持久化、原子写入和事务备份。 | 高。需要引入数据库运行时、迁移逻辑和新的恢复策略。 |
| 数据可理解性 | 高。JSON 易备份、导出、手动检查和恢复。 | 中。需要数据库工具查看，普通用户不易直接理解。 |
| 写入模型 | 依赖队列、锁与原子替换，适合单机桌面应用。 | 事务能力更强，适合复杂查询和更高并发读写。 |
| 查询能力 | 需要在内存、Worker 或派生索引中处理。 | 可使用索引、FTS 等数据库能力。 |
| 迁移成本 | 无。延续当前数据结构。 | 中高。需要数据导入、版本迁移、回滚和兼容测试。 |
| 包体积与依赖 | 低。无额外数据库依赖。 | 增加运行时、绑定层和构建复杂度。 |

---

## 3. 触发条件（闸门度量）

当满足以下**任意两项**条件时，必须在下一轮架构里程碑启动 SQLite / libSQL 预研；预研通过后，才评估是否进入主存储迁移。

1. **数据规模阈值**：单看板便签数量 > 5000，或整体 `data.json` 大小 > 20MB，且保存操作在标准硬件上持续超过 200ms。
2. **并发故障率**：在极端高频保存压测中，锁重试耗尽率 > 1%。
3. **复杂查询需求**：标签、模板、跨看板高级过滤或全文搜索导致主线程或 Worker 查询开销持续超过 50ms，且无法通过增量索引缓解。
4. **维护成本超标**：持久化恢复、迁移和回归测试复杂度显著上升，且每季度因存储链路引发 2 个以上生产问题。

---

## 4. 当前决策

**当前状态：维持 JSON 主存储。**

理由：

- 现有一致性风险已由 v1.3.0 的持久化链路治理显著降低。
- v1.3.2 的状态规范化降低了渲染和更新成本。
- v1.3.4 的 Web Worker 搜索已经把主要搜索压力从主线程移出。
- 项目短期路线聚焦轻量标签、模板、搜索体验和 Markdown 导出；这些能力可以先在现有 JSON 架构上实现。
- 过早迁移数据库会带来数据迁移、恢复、兼容和发布复杂度，不符合当前小版本迭代节奏。

---

## 5. 后续路线

### 短期：v1.4.x

继续围绕用户可见能力进行小版本迭代：

- 标签 MVP。
- 标签筛选与 Spotlight 集成。
- 标签批量管理与导入导出兼容。
- 模板 MVP 与模板管理增强。
- 搜索体验增强。
- Markdown 导出 MVP。

在此阶段，只记录触发条件相关指标，不启动主存储迁移。

### 中期：条件触发后

如果闸门条件满足，再建立独立预研分支或沙盒模块，对比以下指标：

- 冷启动耗时。
- 保存耗时。
- 搜索与筛选耗时。
- 数据迁移成功率。
- 崩溃恢复路径。
- 包体积与构建复杂度。

预研不得直接替换主存储。只有在对比结果明确优于 JSON 架构，且迁移风险可控时，才进入正式迁移计划。

---

## 6. 决策维护约定

- 本文档记录存储引擎方向，不作为版本发布计划。
- 版本规划以 `ROADMAP.md` 为准。
- 已完成变更以 `CHANGELOG.md` 为准。
- 如果未来触发 SQLite / libSQL 预研，应先更新本文档，再创建对应技术计划。
