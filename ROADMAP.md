# SoNotes 后续更新规划

## 当前版本

- 当前仓库版本：`v1.2.6`
- 版本号来源已确认并保持一致：
  - `package.json`
  - `src-tauri/tauri.conf.json`
  - `src-tauri/Cargo.toml`

## 规划说明

本文档用于记录**未来更新准备**与优先级判断，不替代 `CHANGELOG.md`，也不承担 Release Notes 职责。

当前仓库已经形成了明确的发布习惯：

- 使用 `vX.Y.Z` 版本号连续发版
- 采用小步快跑、按主题成批发布的节奏
- `CHANGELOG.md` 作为发布说明唯一内容来源

因此，本文档只保留**下一次更新准备**与**暂缓项判断**。已经发布的版本，以 `CHANGELOG.md` 为准；这里只保留必要的上下文，避免已完成内容与待办规划混写。

## 已发布补丁回顾

### v1.2.5：深色模式可视性专项修复（已发布）

本次补丁已经完成发布，主要收敛了以下问题：

- 深色模式下正文与占位符对比度不足
- 标题输入框与正文 textarea 的选中文本高亮过弱
- 深色模式下单选、多选与框选矩形反馈不够明显
- 双击便签任意区域都会触发折叠，现已收窄为仅双击头部拖拽区触发

详细发布内容仍以 `CHANGELOG.md` 中的 `v1.2.5` 区块为准。

### v1.2.6：导入兼容增强与恢复反馈（已发布）

本次补丁已经完成发布，主要收敛了以下问题：

- 旧版备份在缺失版本字段时可按兼容路径识别并迁移导入
- 导入流程补齐合法性校验、版本分流、同名看板重命名与主板回退规则
- 局部条目异常允许部分成功，并输出更明确的导入摘要
- 本地 JSON 持久化失败或 WAL 写入失败时，会回滚到导入前状态
- 数据管理区已补齐恢复备份后的成功、失败、取消、回滚与迁移反馈

详细发布内容仍以 `CHANGELOG.md` 中的 `v1.2.6` 区块为准。

## 下一次更新准备

### 暂定 v1.2.7：交互定位与顺序一致性修补

**目标**

- 收敛 Dock 右键菜单在缩放、不同 DPI 与窗口缩放场景下的定位偏移
- 明确导入后的看板顺序策略，让“导入后顺序如何落位”成为可描述、可回归的合同
- 在 `v1.2.5` 已解决严重可读性问题的基础上，继续补齐深色模式的一致性体验

**主要改动范围**

- `src/components/BoardDock.tsx`
- `src/store/useStore.ts`
- `src/utils/dataTransfer.ts`
- `src/components/NoteCard.tsx`
- `src/index.css`

**范围内**

- 修复 Dock 右键菜单在缩放容器中的坐标系不一致问题
- 明确全量导入、单板导入、同名重命名与旧版迁移后的看板顺序落位规则
- 为顺序策略补齐测试，避免“数量正确但顺序不稳定”的隐性回归
- 继续收敛深色模式下标题、正文、占位符、选区与玻璃背景之间的文本层级一致性

**范围外**

- 不重新扩展导入兼容系统为新的数据管理大改版
- 不顺带处理与菜单定位、顺序策略无关的大范围交互重构
- 不把深色模式治理扩成新的完整视觉系统改版
- 不在规则未明确前承诺具体发布日期

**当前判断（基于遗漏清单与现状复核）**

1. **BoardDock 右键菜单定位：仍有缺口**
   - 当前菜单锚点仍依赖局部布局坐标。
   - Dock 容器仍存在缩放态，因此在缩放、DPI 或窗口缩放场景下，菜单仍可能出现轻微偏移。
   - `v1.2.7` 需要把菜单锚点统一到单一坐标系，并补齐缩放相关回归验证。

2. **深色模式文字可读性：主问题已修，但一致性仍可继续收敛**
   - `v1.2.5` 已解决正文对比度、选中文本高亮、选中态与框选可见性等高优先级问题。
   - 当前剩余问题更偏向“不同卡片底色、玻璃背景与文本层级的一致性”而非严重不可读。
   - 该项适合作为 `v1.2.7` 的中优先级体验治理项，而不是紧急补丁问题。

3. **导入兼容与反馈：已由 `v1.2.6` 基本补齐**
   - 旧版迁移、同名看板重命名、默认板兜底、主板回退、部分成功摘要与回滚反馈已具备明确行为。
   - 该部分原则上不再作为 `v1.2.7` 的主目标，除非后续回归发现新问题。

4. **导入后的顺序策略：仍是主要缺口**
   - 当前实现已经能处理兼容与冲突，但“导入后看板顺序如何稳定落位”还没有被正式写成清晰合同。
   - 尤其是全量导入、单板导入、同名重命名与旧版迁移自动补默认板这些场景，仍需要统一排序规则与测试覆盖。

**准备完成标志**

当以下三项被写清并验证后，可认为 `v1.2.7` 的规则准备完成：

- Dock 菜单定位坐标系与缩放场景回归口径明确
- 导入后的顺序策略在全量、单板、重名与迁移场景下都有稳定定义
- 深色模式一致性治理范围收敛为少量高价值项，而不是继续扩成泛化视觉改版

## 暂缓项

### 托盘圆角问题

该问题当前更接近原生窗口行为边界，是否可彻底解决仍取决于：

- 前端圆角裁剪是否只是视觉层问题
- Tauri / Windows 透明窗口是否允许稳定控制真实命中区与窗口形态

在完成根因确认前，不纳入近期补丁版承诺。

## 文档维护约定

- 本文档只记录未来更新准备与优先级判断。
- 真正发布时，仍以 `CHANGELOG.md` 记录已完成变更。
- 若后续补丁顺序发生变化，应先更新本文档，再进入对应版本的发布准备。
