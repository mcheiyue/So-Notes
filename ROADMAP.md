# SoNotes 后续更新规划

## 当前版本

- 当前仓库版本：`v1.3.4`（已发布）
- 版本号来源已确认并保持一致：
  - `package.json`
  - `src-tauri/tauri.conf.json`
  - `src-tauri/Cargo.toml`

## 规划说明

本文档用于记录**未来更新准备**与优先级判断，不替代 `CHANGELOG.md`，也不承担 Release Notes 职责。

当前仓库已经形成了明确的发布习惯：

- 使用 `vX.Y.Z` 版本号连续发版
- 采用小步快跑、单主题、低风险的迭代节奏
- `CHANGELOG.md` 作为发布说明唯一内容来源

因此，本文档只保留**下一阶段规划**与**暂缓项判断**。已经发布的版本，以 `CHANGELOG.md` 为准；历史发布计划归档到 `docs/archive/release-plans/`。

## 已发布版本回顾

### v1.3.x：底座治理与交互补齐（已发布）

v1.3.x 已完成从数据安全到性能治理的连续收敛：

- v1.3.0：持久化单通道、排他锁、事务备份与状态代际 ACK。
- v1.3.1：性能基线、性能预算、FPS 监控与轻量诊断面板。
- v1.3.2：状态规范化、布局投影与 Canvas 订阅收敛。
- v1.3.3：批量操作、AABB 框选、选中项导出、废纸篓批量操作与拖拽性能优化。
- v1.3.4：搜索索引下沉到 Web Worker，并补齐搜索结果分组与范围筛选。

详细发布内容以 `CHANGELOG.md` 对应版本区块为准。

## 下一阶段准备

### 文档与路线校准（不单独发版）

该项作为进入 v1.4.x 前的开发准备，不单独占用版本号，不打 tag，不发布 Release。

**目标**

- 将 `ROADMAP.md` 从 v1.3.4 开发状态更新为 v1.4.x 小版本路线。
- 校准 `ENGINE_EVOLUTION_DECISION.md` 中已经过期的 v1.3.x 预测。
- 将历史发布计划文件归档，避免根目录混杂当前规划与历史草案。

**范围内**

- 文档状态校准。
- 历史计划归档。
- 明确 v1.4.x 小版本拆分。

**范围外**

- 不修改版本号。
- 不打 tag。
- 不发布 Release。
- 不引入用户可见功能。

## v1.4.x 小版本路线

### v1.4.0：标签 MVP

**目标**

- 建立轻量标签能力，为后续筛选、搜索、模板与导出提供组织维度。

**范围内**

- 便签支持添加、删除标签。
- 提供基础标签 UI。
- 导出数据保留标签字段。

**范围外**

- 不做标签树。
- 不做双向链接或知识图谱。
- 不做自动标签。
- 不做云同步。

### v1.4.1：标签筛选与 Spotlight 集成

**目标**

- 让标签进入日常检索路径，而不仅是便签元数据。

**范围内**

- 按标签筛选便签。
- Spotlight 支持标签过滤。
- 在当前看板 / 全部看板范围内进行标签搜索。

### v1.4.2：标签批量管理与兼容性

**目标**

- 补齐多选场景下的标签管理，并保证导入导出兼容。

**范围内**

- 多选便签批量添加、移除标签。
- 导入导出兼容标签字段。
- 补齐标签相关回归测试。

### v1.4.3：模板 MVP

**目标**

- 提供低复杂度模板能力，减少重复创建常见便签结构的成本。

**范围内**

- 内置常用模板。
- 支持从模板新建便签。
- 模板内容使用现有便签能力承载。

**范围外**

- 不做复杂变量系统。
- 不做模板市场。
- 不做模板同步。

### v1.4.4：模板管理增强

**目标**

- 在模板 MVP 稳定后，补齐用户自定义与标签预设。

**范围内**

- 用户自定义模板。
- 模板预设标签。
- 模板导入、导出。

### v1.4.5：搜索体验增强

**目标**

- 在标签进入数据模型后，继续提升搜索结果质量。

**范围内**

- 标签参与搜索排序。
- 搜索结果高亮优化。
- 最近编辑 / 最近访问权重。
- 评估增量索引与索引重建入口。

### v1.4.6：Markdown 导出 MVP

**目标**

- 增强数据可移植性，让便签内容更容易迁移到其他本地优先工具。

**范围内**

- 单便签导出 Markdown。
- 看板批量导出 Markdown。
- 标签写入 frontmatter。
- 保留 JSON 作为完整结构备份格式。

### v1.4.7：自动更新预研或接入

**目标**

- 根据签名密钥与发布流程准备情况，决定是否接入 Tauri 自动更新。

**范围内**

- 预研 GitHub Releases 静态更新源。
- 明确 stable / beta 通道是否需要预留。
- 明确更新前保存状态与失败回退提示。

**范围外**

- 未完成签名密钥管理前，不承诺上线自动更新。

## 暂缓项

### SQLite / libSQL 主存储迁移

当前不进入主线开发。v1.3.2 的状态规范化与 v1.3.4 的搜索 Worker 已缓解主要性能压力；在触发 `ENGINE_EVOLUTION_DECISION.md` 中的闸门条件前，继续维持 JSON 架构。

### 托盘圆角问题

该问题当前更接近原生窗口行为边界，是否可彻底解决仍取决于：

- 前端圆角裁剪是否只是视觉层问题。
- Tauri / Windows 透明窗口是否允许稳定控制真实命中区与窗口形态。

在完成根因确认前，不纳入近期补丁版承诺。

## 文档维护约定

- 本文档只记录未来更新准备与优先级判断。
- 真正发布时，仍以 `CHANGELOG.md` 记录已完成变更。
- 历史发布计划文件应归档到 `docs/archive/release-plans/`。
- 若后续补丁顺序发生变化，应先更新本文档，再进入对应版本的发布准备。
