# SoNotes 后续更新规划

## 当前版本

- 当前仓库版本：`v1.3.4`（开发中）
- 版本号来源已确认并保持一致：
  - `package.json`
  - `src-tauri/tauri.conf.json`
  - `src-tauri/Cargo.toml`

## 规划说明

本文档用于记录**未来更新准备**与优先级判断，不替代 `CHANGELOG.md`，也不承担 Release Notes 职责。

当前仓库已经形成了明确的发布习惯：

- 使用 `vX.Y.Z` 版本号连续发版
- 采用小步快跑、按主题成批发布的节奏
- `CHANGELOG.md` 作为发布说明唯一内容来源

因此，本文档只保留**下一次更新准备**与**暂缓项判断**。已经发布的版本，以 `CHANGELOG.md` 为准；这里只保留必要的上下文，避免已完成内容与待办规划混写。

## 已发布版本回顾

### v1.3.3：交互闭环与架构收割（已发布）

- 框选 AABB 碰撞重构（从 DOM 到纯数学计算）
- 批量视图突变（折叠/展开、置顶/置底）
- 选中项精准导出
- 废纸篓批量操作
- 拖拽性能优化（元素注册表 + 批量 Store 更新，FPS 提升约 12 倍）
- 延迟持久化（2s debounce）

详细发布内容以 `CHANGELOG.md` 中的 `v1.3.3` 区块为准。

## 下一次更新准备

### v1.3.4：搜索性能与结果组织

**目标**

- 将搜索索引构建、匹配、高亮计算从主线程迁移到 Web Worker
- 提升搜索结果的组织效率，支持按看板、标题匹配、正文匹配分组
- 支持基础筛选：当前看板 / 全部看板 / 已删除排除

**主要改动范围**

- `src/components/Spotlight.tsx`（搜索入口）
- `src/workers/searchWorker.ts`（新增，搜索索引 Worker）
- `src/utils/searchIndex.ts`（索引构建与查询逻辑）

**范围内**

- 搜索索引下沉到 Web Worker，避免阻塞主线程
- 搜索结果按看板、标题匹配、正文匹配分组显示
- 支持基础筛选：当前看板 / 全部看板 / 已删除排除
- 索引可重建，索引损坏不影响原始便签数据

**范围外**

- 不做标签 / 轻量知识整理（留到 v1.4.0）
- 不做 Rust 侧索引（Web Worker 作为兜底方案）
- 不做复杂搜索语法或正则支持

**验收口径**

- 1000+ 便签与长文本样本下，输入搜索词不会卡死画布交互
- 索引可以重建，索引损坏不影响原始便签数据
- 搜索结果跳转、跨看板定位、折叠便签展开路径保持可用

## 暂缓项

### 托盘圆角问题

该问题当前更接近原生窗口行为边界，是否可彻底解决仍取决于：

- 前端圆角裁剪是否只是视觉层问题
- Tauri / Windows 透明窗口是否允许稳定控制真实命中区与窗口形态

在完成根因确认前，不纳入近期补丁版承诺。

### 标签 / 轻量知识整理

原规划在 v1.3.4，现已推迟到 v1.4.0（与模板系统一起做更合适）。

## 文档维护约定

- 本文档只记录未来更新准备与优先级判断。
- 真正发布时，仍以 `CHANGELOG.md` 记录已完成变更。
- 若后续补丁顺序发生变化，应先更新本文档，再进入对应版本的发布准备。
